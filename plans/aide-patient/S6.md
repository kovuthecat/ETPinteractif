# aide-patient · S6 — Consolidation : contexte + commits + note déploiement   (rédigé par Opus)

> **Modèle : Haiku · effort : low · Vague : 4 (solo — après toutes les sessions livrables)**
> Exécutant : consolidation seule (aucune logique produit). Doute → STOP, rends la main.

- Date : — · Branche : —

## Lire (commun à la session)

- Tous les `S<k>.md` de ce chantier (bilans + statuts + checklists visuelles) · `index.md`
- `STATUS.md` · `TASKS.md` · `VALIDATION.md` · `DECISIONS.md` · `PROJECT_MAP.md`
- `WORKFLOW.md` §4d (commits en fin de plan)

## Objectif

Resynchroniser le contexte projet sur l'état réel du code, committer **tâche par tâche** (staging explicite), mettre
à jour les checklists visuelles, puis (après validation Thibault) push.

## Étapes

1. **Statuts** : cocher les `S<k>.md` faits + le tableau Sessions de `index.md` + les `T<n>` correspondants dans
   `TASKS.md` (créer la section « App d'aide patient (T16) » si absente).
2. **STATUS.md** : entrée du chantier (nouvelle surface patient : couche `src/content/tabac/`, 2ᵉ entrée Vite/bundle
   isolé, écrans substituts + situations en voix patient, QR unique). Gates verts, points ouverts (URL patient,
   habillage `// à revalider`, maquette non faite).
3. **VALIDATION.md** : reporter les checklists visuelles de chaque session (statut « à valider par Thibault »),
   dont la vérification d'impression du QR (Ctrl+P) et l'identité de rendu consultation après S1.
4. **DECISIONS.md** : inscrire les décisions structurantes — couche `src/content/tabac/` (source unique
   consultation+patient), **bundle patient séparé** (2ᵉ entrée Vite, jamais d'import du registre/module soignant),
   v1-directe **sans maquette** (précédent extensions-tabac), QR statique unique (zéro dépendance), habillage voix
   patient proposé/à revalider, hébergement de l'URL différé au déploiement.
5. **PROJECT_MAP.md** : nouveaux fichiers/zones — `src/content/tabac/` (substituts/situations/outils),
   `patient.html`, `src/patient/` (main + PatientApp + Home + substituts/ + situations/), `public/qr/patient.png`,
   `build.rollupOptions.input` dans `vite.config.ts`. Nouvelle Feature « App d'aide patient (surface séparée) ».
6. **Commits** : un commit **par tâche** (staging explicite, jamais `git add -A`), messages fournis par chaque
   `S<k>.md` (T1→T5). Puis **un commit de contexte** (STATUS/TASKS/VALIDATION/DECISIONS/PROJECT_MAP + `plans/aide-patient/`).
7. **Note déploiement** : consigner dans STATUS.md/DECISIONS.md que l'app patient nécessite, au déploiement, une
   **cible séparée** (2ᵉ projet Vercel avec sa propre URL, ou sous-domaine custom) servant `patient.html` comme
   racine, **sans** exposer l'app de consultation ; et que `public/qr/patient.png` est à **régénérer** à l'URL
   définitive. Ne pas modifier/supprimer un `vercel.json` existant.
8. **Push** : **uniquement après validation visuelle de Thibault** (les deux apps, `/` et `/patient.html`).

## Validation

- Auto : `npx tsc --noEmit` · `npm run build` (2 entrées) · `npm test` verts sur l'ensemble consolidé.
- Humain : Thibault valide les checklists `VALIDATION.md` (les deux apps) avant le push.

### Statut

[ ] à faire · exécuté le : — · commit : —

---

## Fin de session

Dérouler `/fin-de-tache` (mode **solo**, session de consolidation).
