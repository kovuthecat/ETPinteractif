# insuline-affinements-2026-07 · S5 — Rapide : scénario « rapide sans repas » (2) + pont (8b)   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 2 (parallèle : oui, avec S4 — modules disjoints)**
> ✅ **G1 validée (2026-07-21)** — section « rapide sans repas » du `10` validée Thibault. Démarrable
> **après S3** (lib bolus corrigée). Contenu = verbatim du doc validé ; ne pas inventer. Doute → STOP.

- Date : — · Branche : —

## Lire

- `docs/diabete/10-insuline-rapide.md` (**autorité** — section « rapide sans repas » validée G1 ;
  décision G5 = onglet distinct vs intégré au ④)
- `src/features/diabete/insuline-rapide/InsulineRapideModule.tsx` (4 temps, `matriceCumul`,
  `messageCouvrir`, structure des `section`/`tabpanel`, renvoi module 8 l.599-603)
- `src/features/diabete/insuline-rapide/InsulineRapideModule.module.css`
- `src/features/diabete/lib/glycemieCurve.ts` — `sampleRepasAvecBolus` : un **repas de charge 0** +
  un bolus → glycémie = BASELINE − effet = plonge (aucun ajout lib nécessaire, **lecture seule**).

## Hors périmètre

- Ne PAS modifier `glycemieCurve.ts` (le scénario « sans repas » est un cas d'usage de l'API existante,
  après le fix S3). Ne pas afficher de dose/minute.
- Ne PAS toucher au module basale (→ S4). Ne pas casser les temps ①②③④ existants (S2 a déjà retouché ②).
- Fichiers partagés, commit, push : non → S6.

---

## IA6 — Temps « Et si je ne mange pas ? » (item 2) + phrase-pont (8b)

### Objectif
Ajouter le scénario **rapide prise mais repas sauté/retardé** : la courbe plonge sous la cible →
message de sécurité + **renvoi module 8** (15/15). Plus la phrase-pont vers la basale (item 8, côté rapide).

### Décision clé (G5, tranché Thibault 2026-07-21)
**5ᵉ onglet distinct** `⑤ Et si je ne mange pas ?` (PAS une variante du ④ — le ④ traite le cumul).
**Positionnement de l'onglet à caler** : proposer un placement (par défaut : à la suite, après le ④)
et le soumettre à la validation visuelle de Thibault (`// à revalider`). Courbe = `sampleRepas`
charge 0 (plat) vs `sampleRepasAvecBolus` charge 0 + `DOSE_ADEQUATE` (plonge), même grammaire que
les autres temps (fantôme « sans » / principale « avec »). Aucun chiffre.

### Étapes
1. Ajouter l'entrée `⑤` dans `TEMPS_TABS` (5ᵉ onglet, G5). Respecter l'accessibilité tablist
   existante (`role`, `aria-selected`, `id`/`aria-controls`, flèches `handleTabsKeyDown` qui boucle
   sur `TEMPS_TABS.length`). Proposer un **positionnement** (par défaut : à la fin, après le ④) et le
   marquer `// à revalider (Thibault)` pour sa validation visuelle.
2. Construire les courbes : repas charge 0 → `sans` plate à la baseline ; `avec` = même repas +
   bolus `DOSE_ADEQUATE` injecté juste avant → plonge nettement sous la bande. Réutiliser
   `CourbeGlycemie`, `REPAS_MARQUEUR`, `bandes`, `DOMAIN_OPTS`, la légende `— avec / - - sans`.
3. Message (verbatim doc validé) : « rapide sans manger → le sucre plonge : on n'injecte pas la
   rapide si on ne mange pas ; si le repas saute après → on resucre ». Bouton renvoi
   `onNavigate('hypoglycemie')` (même pattern que ④ « Ça ressemble à une hypo → le réflexe »).
4. Option post-prandiale (si le doc la retient) : la présenter comme **exception** (inappétence /
   maladie), pas comme méthode — `// à revalider (Thibault)`.
5. **Pont (item 8b)** : phrase reliant « ce repas » (rapide) à « la journée entière » (basale),
   paire cohérente avec S4 (cf. doc S1).

### Validation
- Auto (bloque le commit) : `npx tsc --noEmit` + `npm run build` (2 entrées) + `npm test` → OK.
- Humain (VALIDATION.md, S6) : le nouveau temps montre une courbe qui plonge sous la cible, message +
  renvoi module 8 OK ; les temps ①②③④ intacts (dont le slider ② de S2) ; phrase-pont cohérente avec
  la basale. Contenu conforme au doc validé G1.

### Si bloqué
G5 impose le 5ᵉ onglet — **ne pas** replier en variante du ④. Si 5 onglets serrent la barre à ~1 m
(SPEC support conso), jouer sur le **positionnement / la mise en forme** (retour à la ligne des
onglets, libellé plus court) et **signaler à Thibault** pour arbitrage visuel, sans changer la
décision G5. Ne pas raccourcir le libellé au point de perdre le sens (« Et si je ne mange pas ? »).

### Message de commit (appliqué en S6)
`feat(diabete): module rapide — scénario « rapide sans repas » (renvoi hypo) + pont vers la basale`

### Statut
[x] fait · exécuté le : 2026-07-21 · commit : 29452b1 (combiné avec IA3/S2, cf. index.md §Clôture)

## Fin de session

**Mode vague parallèle (S4 ‖ S5, fichiers disjoints)** : seul ce fichier a été édité. Aucun
fichier partagé (`STATUS.md`, `TASKS.md`, `index.md`, `VALIDATION.md` racine) touché — S6
consolide. Aucun commit, aucun push.

### Fichiers modifiés

- `src/features/diabete/insuline-rapide/InsulineRapideModule.tsx` — 5ᵉ onglet ⑤, courbes/message/
  bouton du temps ⑤, phrase-pont (8b).
- `src/features/diabete/insuline-rapide/InsulineRapideModule.module.css` — classes `.exceptionNote`,
  `.piedRefrain`, `.pont`.
- `docs/diabete/10-insuline-rapide.md`, `src/features/diabete/lib/glycemieCurve.ts`/`.test.ts` :
  non touchés par S5 (déjà modifiés par S1/S3, hors périmètre de cette session — lecture seule).

### Résumé du 5ᵉ onglet

`⑤ Et si je ne mange pas ?` — **5ᵉ onglet distinct** (G5 respectée, pas une variante du ④).
**Positionnement retenu : à la fin, après le ④** (défaut proposé par le plan) — `// à revalider
(Thibault)`. La barre `.tabs` a déjà `flex-wrap: wrap` : un repli automatique sur 2 lignes se
produit sans changement de code si 5 onglets ne tiennent pas sur une ligne à ~1 m — à valider
visuellement par Thibault, sans quoi ajuster mise en forme/libellé (jamais la décision G5).

Courbes (réutilisent `CourbeGlycemie`/`REPAS_MARQUEUR`/`bandes`/`DOMAIN_OPTS`, aucune
interactivité — scénario fixe et illustratif) :

- **sans** : `sampleRepas` sur un repas de **charge nulle** (`{ charge: 0, frein: 0.35, retard: 0.3 }`)
  → trace plate à la baseline (vérifié numériquement : tous les points = BASELINE).
- **avec** : `sampleRepasAvecBolus` sur ce même repas + bolus `DOSE_ADEQUATE` (0.4) injecté à
  `T_INJECTION_DEFAUT` (−15 min, même timing que les autres temps) → plonge nettement sous la
  bande basse (vérifié numériquement : minimum < `BANDE_CIBLE_DEFAUT.basse`).

Légende : « - - Sans rapide » (fantôme) / « — Avec rapide, injectée quand même » (principale,
libellé légende coloré `legendeVigilance` pour signaler le danger — seule variation par rapport
à la grammaire des autres temps, où le « avec » est la situation recherchée).

Message principal (**verbatim** doc validé G1 + S5.md étape 3) : « Rapide sans manger → le sucre
plonge : on n'injecte pas la rapide si on ne mange pas ; si le repas saute après → on resucre. »
Bouton renvoi module 8 : `onNavigate('hypoglycemie')`, libellé « Ça ressemble à une hypo → le
réflexe » (même pattern que le ④), toujours visible (scénario fixe, pas de condition).

Option post-prandiale (doc §3 temps ⑤, 3ᵉ message) : ajoutée en note discrète sous le message
principal, présentée explicitement comme **exception** (inappétence/maladie, jamais une méthode
de routine) — `// à revalider (Thibault)` (paraphrase du doc, pas un verbatim strict).

### Texte exact de la phrase-pont retenue (item 8b, à comparer avec S4)

```text
La rapide ne couvre que ce repas ; le reste de la journée entière — et la nuit — c'est la lente qui s'en charge.
```

Placée en bas du module, sous le refrain `.filrouge`, dans un nouveau bloc `.piedRefrain` (même
patron que `InsulineModule.module.css` côté basale), style discret (`.pont`, `color-text-soft`,
`font-size-small`) — jamais un chiffre. `// à revalider (Thibault)`.

**Comparaison avec S4** (constaté en fin de session, S4 ayant terminé en parallèle) — phrase-pont
basale (item 8a, `InsulineModule.tsx`, `PONT_TEXTE`) :

```text
La lente couvre toute la journée, du coucher au coucher ; la rapide zoome sur un seul repas.
```

Les deux phrases sont **conceptuellement cohérentes** (même paire de zooms : lente = journée
entière coucher→coucher, rapide = un repas) mais **pas formulées à l'identique** (verbes/ordre
différents). À arbitrer par Thibault : garder les deux formulations telles quelles (chacune côté
de son module, ton légèrement différent) ou les harmoniser mot pour mot.

### Résultat des 3 gates

- `npx tsc --noEmit` → OK (aucune erreur).
- `npm run build` → OK (2 entrées `patient.html` + `consultation.html` générées).
- `npm test` → OK (3 fichiers, 106 tests passés — temps ①②③④ non cassés, dont le slider ② de S2).

### Checklist de validation visuelle humaine (reprise de la section « Validation » — à valider par Thibault)

- [ ] Le nouveau temps ⑤ montre une courbe qui plonge nettement sous la bande-cible (scénario
      « avec »), vs une trace plate (scénario « sans »).
- [ ] Message + bouton de renvoi module 8 (hypoglycémie) OK, visibles et cliquables.
- [ ] Les temps ①②③④ restent intacts (dont le slider ② retouché par S2).
- [ ] La phrase-pont (8b) est cohérente avec celle du module basale (8a, S4) — cf. comparaison
      ci-dessus, arbitrage à trancher.
- [ ] Contenu conforme au doc validé G1 (`docs/diabete/10-insuline-rapide.md` §3 temps ⑤).
- [ ] Positionnement du 5ᵉ onglet (après le ④) et comportement de la barre à 5 onglets à ~1 m —
      arbitrage visuel si la barre serre trop.

**Statut global : à valider par Thibault.**
