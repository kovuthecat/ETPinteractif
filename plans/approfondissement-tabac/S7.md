# approfondissement-tabac · S7 — Consolidation : docs, contexte, commits, push   (rédigé par Fable)

> **Modèle : Haiku · effort : low · Vague : 3 (solo, après S1-S6)**
> Exécutant : UNIQUEMENT la tâche ci-dessous. Doute ou blocage → STOP, signale, rends la main.
> Session solo : c'est la seule session du chantier autorisée à committer/pusher et à toucher
> les fichiers de contexte partagés.

- Date : 2026-07-09 · Branche : —

## Lire (commun à la session)

- `plans/approfondissement-tabac/index.md` + les statuts consignés dans `S1.md` … `S6.md`
  (checklists visuelles, notes, écarts éventuels).
- `git status` / `git diff --stat` pour reconstituer les périmètres réels.

## Hors périmètre

Aucune modification de `src/**` (si une gate échoue ici, STOP et signaler — ne pas « réparer »).

---

## AP7 — Clôture du chantier

### Étapes

1. **Gate globale** : `npx tsc --noEmit` + `npm run build` + `npm test` verts sur l'ensemble.
2. **Commits atomiques**, un par tâche, staging explicite des fichiers du périmètre de chaque
   session, dans l'ordre S1 → S6, avec les messages prévus en fin de chaque `S<k>.md`.
3. **`docs/contenu-modules-tabac.md`** (commit dédié `docs(tabac): contenu modules 9-10 + modèle
   nicotine réaliste`) :
   - §Module 2 et §Module 5 : décrire le nouveau modèle (élimination commune demi-vie ≈ 2 h,
     Bateman, saturation/tolérance, tension dérivée du niveau) et le renvoi aux invariants testés ;
   - nouvelle section **Module 9 — Ce que l'arrêt répare** : objectif, structure (silhouette +
     frise), le tableau des 10 jalons (copie depuis `S5.md`), vigilance (chiffres à revalider,
     ton exclusivement positif), sources ;
   - nouvelle section **Module 10 — Vrai ou faux ?** : objectif, mécanique, les 15 affirmations
     avec verdicts/sources (copie depuis `S6.md`), statut des cartes 14-15 (désactivées, à
     revalider), vigilance (ton non culpabilisant, pas de score).
4. **Fichiers de contexte** :
   - `STATUS.md` : nouvelle entrée de phase (chantier approfondissement-tabac : refonte courbe,
     2 modules, prompts) + points à revalider par Thibault (jalons S5, cartes 4/14/15 S6, mention
     graphe S3, PNG à générer/déposer dans `public/illustrations/tabac/`) ;
   - `TASKS.md` : AP1-AP7 cochées avec résumé de gate ;
   - `plans/approfondissement-tabac/index.md` : colonne Statut cochée ;
   - `VALIDATION.md` : reporter les checklists humaines consignées dans S1-S6 (statut « à valider
     par Thibault ») ;
   - `PROJECT_MAP.md` : bandeau d'état + arborescence (2 nouveaux modules, `tabac/components/`,
     `src/components/SilhouetteCorps`, silhouette diabète en wrapper) ;
   - `DECISIONS.md` : ① promotion `SilhouetteCorps` générique (wrapper diabète iso-API) ;
     ② duplication assumée d'`IllustrationSlot` par thème ; ③ modèle nicotine réaliste
     invariant-testé (API gelée) ; ④ cartes vrai/faux sensibles livrées désactivées.
5. **Un seul push** en fin de session.

### Validation

- Auto : gate globale verte avant le premier commit ; `git log --oneline` montre un commit par
  tâche + le commit docs ; arbre propre après push.

### Message de commit (fichiers de contexte)

`chore(contexte): clôture chantier approfondissement-tabac (statuts, carte, décisions, validation)`

### Statut

- [ ] Fait — notes :
