# S9 — Consolidation : docs, contexte, commits, push · Modèle : Sonnet, effort : low

> Dernière session (solo, après S1-S8). Lire ce fichier + les `S<k>.md` (sections Statut/Commit +
> points signalés) + les fichiers « Modifier ».

## But

Resynchroniser la documentation et les fichiers de contexte avec l'état réel du code, puis
committer (un commit atomique par tâche BO1-BO8, messages fournis dans chaque `S<k>.md`) et
pousser en une fois.

## Modifier

- `docs/contenu-modules-tabac.md`
- `STATUS.md` · `TASKS.md` · `VALIDATION.md` · `PROJECT_MAP.md` · `DECISIONS.md`
- `plans/boite-a-outils/index.md` (statuts)

## Conception (fixée)

### 1. `docs/contenu-modules-tabac.md` (autorité du contenu médical)

- **Module 1 — Composantes** : remplacer la description (tableau exemples/outils) par la nouvelle
  mécanique : situations sélectionnables en menu radial, ids de `situations.ts`, aucune
  description ni solution à l'écran (narration soignant), CTA contextuel vers Stratégies & outils.
- **Module 6 — « Gérer le craving (4D) » devient « Stratégies & outils »** : reprendre la table
  des 14 outils de `S2.md` (id, titre, situations, preuve, renvoi) + un paragraphe par outil
  reprenant `principe` et `proposition`. **C'est ici (et seulement ici) que vivent les chiffres** :
  reporter pour chaque outil les niveaux de preuve chiffrés du rapport OE (si-alors OR 1,70 ;
  exercice aigu SMD −1,64 ; counseling infirmier RR 1,29 ; recyclage post-rechute OR 3,5 ;
  paradoxe tabac-stress SMD −0,37/−0,27 ; etc.) avec le pointeur
  `docs/evidence-tabac/2026-07-10-rapport-openevidence-sevrage.md`.
- **Module Substituts** : ajouter la forme vapoteuse (contenu S5) + le statut réduction des
  risques (HAS/HCSP), mention « à revalider ».
- **Module Plan d'arrêt** : section 7 « Si j'ai un écart » (contenu S6) + rationnel (préparer
  l'écart avant le jour J ; recyclage immédiat).
- **Module Vrai ou faux** : ajouter les 6 cartes S4 (affirmation, verdict, explication, source)
  et la reformulation de `vf-faux-pas` ; noter les sources internationales à revalider.
- Mettre à jour la table des renvois inter-modules (`craving` → `boite-a-outils` partout).

### 2. Fichiers de contexte

- `DECISIONS.md` — consigner (datées 2026-07-10) : fusion Craving → Boîte à outils ;
  contexte de navigation générique (`unknown`) dans le moteur ; situations partagées
  `situations.ts` ; niveaux de preuve à l'écran limités à 2 mentions qualitatives ;
  vapoteuse réintégrée comme outil de réduction des risques (à revalider) ;
  filtre « toniques uniquement » côté diabète (le filtre ne touche jamais la jauge).
- `PROJECT_MAP.md` — bandeau d'état + arborescence (`boite-a-outils/` remplace `craving/`,
  `situations.ts`, `docs/evidence-tabac/`) + Feature 3.
- `STATUS.md` / `TASKS.md` / `VALIDATION.md` — état du chantier, tâches BO1-BO8, consignes de
  validation visuelle humaine en attente (reprendre les sections « visuel » des S).
- `plans/boite-a-outils/index.md` — cocher les statuts, reporter les points ouverts restants.

### 3. Commits & push

- Vérifier le gate global : `npx tsc --noEmit` + `npm run build` + `npm test`.
- Un commit par tâche (messages en bas de chaque `S<k>.md`), ordre BO1 → BO8, puis un commit
  `docs(contexte): resync chantier boite-a-outils (BO9)`. Un seul push à la fin.

## Validation

- Grep final : plus aucune référence à `'craving'` dans `src/` ; `docs/contenu-modules-tabac.md`
  cohérent avec le code (14 outils, 21 cartes, 6 formes de substituts, 7 sections plan d'arrêt).

## Commit

`docs(contexte): resync chantier boite-a-outils (BO9)` (+ les commits par tâche, cf. ci-dessus)

## Statut

[ ] à faire
