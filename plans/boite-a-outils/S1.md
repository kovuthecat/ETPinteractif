# S1 — Moteur : contexte de navigation + registre + situations partagées + stub · Modèle : Sonnet, effort : medium

> Lire UNIQUEMENT ce fichier + les fichiers « Lire ». Conception fixée par Fable. Doute → STOP.
> Vague 1 — pas de commit, pas de fichier partagé ; checklist et statut consignés ici.

## But

Poser tout ce dont S2 et S3 (vague 2) ont besoin : le transport générique d'un `context` de
navigation dans le moteur, la liste canonique des situations du thème tabac, et le remplacement du
module `craving` par `boite-a-outils` dans le registre (avec un stub compilable).

## Lire

- `src/features/types.ts`
- `src/App.tsx`
- `src/features/tabac/registry.ts`
- `src/features/tabac/addiction/AddictionModule.tsx` (liste actuelle des `exemples` par pilier — source des libellés)
- `src/features/tabac/soulagement/SoulagementModule.tsx` + `src/features/tabac/nicotine/NicotineModule.tsx` (portes `ModuleFooterNav` existantes)
- `src/features/tabac/components/IllustrationSlot.tsx` (référence, ne pas modifier)

## Modifier

- `src/features/types.ts` — `ModuleProps`
- `src/App.tsx` — type `View` + `navigateToModule`
- `src/features/tabac/registry.ts` — entrée du module
- `src/features/tabac/situations.ts` — **créer**
- `src/features/tabac/boite-a-outils/BoiteAOutilsModule.tsx` — **créer (stub)**
- `src/features/tabac/soulagement/SoulagementModule.tsx`, `src/features/tabac/nicotine/NicotineModule.tsx`
  — retarget des portes pointant `'craving'`

## Hors périmètre

- Ne PAS supprimer `src/features/tabac/craving/**` (c'est S2, vague 2 — le module reste importable
  mais sort du registre dès S1).
- Ne PAS toucher `addiction/` (S3), `idees-recues/` (S4), `substituts/` (S5), `plan-arret/` (S6).
- Ne PAS toucher `Home.tsx`, `ModuleShell.tsx`, `ModuleFooterNav.tsx` (leur API ne change pas).

## Conception (fixée)

### 1. Contexte de navigation générique

Dans `src/features/types.ts` :

```ts
export interface ModuleProps {
  onNavigate: (id: ModuleId, context?: unknown) => void;
  /** Contexte opaque transmis par le module appelant (le moteur n'en connaît pas la forme). */
  context?: unknown;
}
```

Dans `src/App.tsx` :

- `type View` : la variante `module` gagne `context?: unknown`.
- `navigateToModule = (moduleId: ModuleId, context?: unknown) => …` pousse `context` dans
  l'entrée d'historique (le retour arrière le restaure naturellement).
- Le rendu passe `context={currentView.context}` au `<Component>`.
- `Home` appelle `navigateToModule(id)` sans contexte — signature compatible, zéro changement chez lui.

**Invariant** : aucun type « situations » ni aucun nom de thème dans `types.ts`/`App.tsx` —
`unknown` strictement.

### 2. Situations partagées du thème tabac

Créer `src/features/tabac/situations.ts` (libellés courts = les `exemples` actuels du module
addiction, ids stables en kebab/court) :

```ts
export type PilierId = 'physique' | 'psychologique' | 'comportementale';

export interface SituationDef {
  id: string;
  label: string;
  pilier: PilierId;
}

export const SITUATIONS: SituationDef[] = [
  // Physique — signes du manque
  { id: 'manque', label: 'Manque', pilier: 'physique' },
  { id: 'irritabilite', label: 'Irritabilité', pilier: 'physique' },
  { id: 'nervosite', label: 'Nervosité', pilier: 'physique' },
  { id: 'concentration', label: 'Concentration difficile', pilier: 'physique' },
  { id: 'sommeil', label: 'Sommeil perturbé', pilier: 'physique' },
  { id: 'fringales', label: 'Fringales', pilier: 'physique' },
  { id: 'craving', label: 'Envie irrépressible', pilier: 'physique' },
  // Psychologique — fonctions de la cigarette
  { id: 'stress', label: 'Stress', pilier: 'psychologique' },
  { id: 'anxiete', label: 'Anxiété', pilier: 'psychologique' },
  { id: 'ennui', label: 'Ennui', pilier: 'psychologique' },
  { id: 'plaisir', label: 'Plaisir / récompense', pilier: 'psychologique' },
  { id: 'stimulation', label: 'Stimulation', pilier: 'psychologique' },
  { id: 'deprime', label: '« Anti-déprime »', pilier: 'psychologique' },
  // Comportementale — automatismes
  { id: 'cafe', label: 'Café-clope', pilier: 'comportementale' },
  { id: 'repas', label: 'Après les repas', pilier: 'comportementale' },
  { id: 'pause', label: 'En pause', pilier: 'comportementale' },
  { id: 'voiture', label: 'En voiture', pilier: 'comportementale' },
  { id: 'telephone', label: 'Avec le téléphone', pilier: 'comportementale' },
  { id: 'social', label: 'En société', pilier: 'comportementale' },
  { id: 'alcool', label: "Avec l'alcool", pilier: 'comportementale' },
];

/** Forme (non garantie) du contexte de navigation addiction → boîte à outils. */
export interface SelectionSituations {
  situations: string[];
}

export function parseSelectionSituations(context: unknown): string[] {
  if (
    typeof context === 'object' && context !== null &&
    Array.isArray((context as SelectionSituations).situations)
  ) {
    const valides = new Set(SITUATIONS.map((s) => s.id));
    return (context as SelectionSituations).situations.filter(
      (id): id is string => typeof id === 'string' && valides.has(id),
    );
  }
  return [];
}
```

### 3. Registre tabac

Dans `src/features/tabac/registry.ts`, **remplacer** l'entrée `craving` par :

```ts
{
  id: 'boite-a-outils',
  famille: 'agir',
  titre: 'Stratégies & outils',
  resume: 'Choisir, situation par situation, des techniques simples pour faire face sans fumer — et emporter sa boîte à outils.',
  Icon: Wrench, // lucide-react
  Component: BoiteAOutilsModule,
  hue: 'vigilance',
  sources: [
    'HAS — Arrêt de la consommation de tabac (2014)',
    'Tabac Info Service',
    'ACC — Tobacco Cessation Treatment Pathway (2018)', // à revalider (Thibault)
    'Cochrane — revues sevrage tabagique', // à revalider (Thibault)
  ],
},
```

L'import de `CravingModule` disparaît du registre (le dossier reste, S2 le supprimera).
Position dans le tableau : à la place exacte de `craving` (ordre d'affichage inchangé).

### 4. Stub du module

`src/features/tabac/boite-a-outils/BoiteAOutilsModule.tsx` : composant minimal
(`ModuleProps`, intro d'une phrase « Boîte à outils en construction — contenu livré en S2 »,
pas de CSS dédié). Il compile, il s'affiche, rien de plus.

### 5. Portes retargetées

Dans `soulagement/SoulagementModule.tsx` et `nicotine/NicotineModule.tsx` : toute entrée
`ModuleFooterNav`/navigation avec `id: 'craving'` devient
`{ id: 'boite-a-outils', label: 'Stratégies et outils' }`. **Grep de contrôle** : à la fin de S1,
`'craving'` n'apparaît plus nulle part dans `src/` **sauf** dans `craving/**` (supprimé en S2),
`addiction/` (S3) et `idees-recues/data.ts` (S4).

## Étapes

1. `types.ts` + `App.tsx` (contexte).
2. `situations.ts`.
3. Stub + registre.
4. Portes soulagement/nicotine + grep de contrôle.
5. Gate : `npx tsc --noEmit` + `npm run build`.

## Validation

- **auto :** typecheck + build verts ; grep `'craving'` conforme (étape 4).
- **visuel (→ consigné ici) :** la carte « Stratégies & outils » remplace « Gérer le craving (4D) »
  sur l'accueil tabac ; clic → stub ; portes de fin de Soulagement/Nicotine mènent au stub.

## Si bloqué

Si un module tiers (hors périmètres S2-S6) référence encore `'craving'` → STOP + signaler ici
(ne pas l'éditer soi-même).

## Commit (message pour S9 — ne pas committer en vague 1)

`feat(moteur+tabac): contexte de navigation générique, situations partagées, boite-a-outils remplace craving au registre (BO1)`

## Statut

[x] fait

### Résumé des étapes

1. `src/features/types.ts` — `ModuleProps.onNavigate` gagne `context?: unknown` + champ `context?: unknown`. Fait.
2. `src/App.tsx` — `View` (variante `module`) gagne `context?: unknown` ; `navigateToModule` accepte et propage `context` ; rendu passe `context={currentView.context}`. `Home` inchangé (signature compatible). Fait.
3. `src/features/tabac/situations.ts` — créé, conforme à la Conception fixée (20 situations, 3 piliers, `parseSelectionSituations`). Fait.
4. `src/features/tabac/boite-a-outils/BoiteAOutilsModule.tsx` — stub créé (dossier n'existait pas, créé). Fait.
5. `src/features/tabac/registry.ts` — entrée `craving` remplacée par `boite-a-outils` (même position dans le tableau), import `CravingModule` retiré au profit de `BoiteAOutilsModule`, icône `Wrench`. Fait.
6. Portes retargetées :
   - `soulagement/SoulagementModule.tsx` : entrée `ModuleFooterNav` `{ id: 'craving', label: "Tenir pendant l'envie : les 4D" }` → `{ id: 'boite-a-outils', label: 'Stratégies et outils' }`. Fait.
   - `nicotine/NicotineModule.tsx` : **aucune porte `ModuleFooterNav` ne pointait `'craving'`** dans ce fichier (ses deux portes sont `substituts` et `soulagement` ; la seule occurrence de « Craving » est un libellé de signe de manque, pas un id de navigation). Rien à changer — pas un écart, juste une prémisse du fichier S1 qui ne se vérifiait pas dans ce module précis.

### Validation auto

- `npx tsc --noEmit` : **vert**, aucune erreur.
- `npm run build` : **vert** (`tsc -b && vite build` — 1854 modules transformés, build en ~1,1 s).
- Grep de contrôle `'craving'` (insensible à la casse) dans `src/` : 6 fichiers restants.
  - `src/features/tabac/situations.ts` — attendu (id de situation `craving`, conforme à la Conception fixée).
  - `src/features/tabac/addiction/AddictionModule.tsx` — attendu (S3, hors périmètre, navigation `id: 'craving'` non touchée).
  - `src/features/tabac/craving/CravingModule.tsx` — attendu (dossier conservé, suppression = S2).
  - `src/features/tabac/plan-arret/PlanArretModule.tsx` — un commentaire de code (`// Les 4D — cf. features/tabac/craving/CravingModule.tsx`), pas de référence fonctionnelle `id: 'craving'`. Fichier dans le périmètre S6, non touché (hors périmètre S1).
  - `src/features/tabac/nicotine/NicotineModule.tsx` — occurrence du mot « Craving » comme libellé de signe de manque (texte), pas un id de navigation. Aucune action requise.
  - **`src/features/registry.ts`** (registre générique multi-thèmes, PAS `tabac/registry.ts`) — ligne `description: "Comprendre l'addiction, utiliser les substituts, gérer le craving et la motivation."` : occurrence du mot « craving » dans une phrase descriptive du thème, préexistante à S1, aucun rapport avec l'id de module renommé. Ce fichier n'est ni dans la zone « Modifier » de S1, ni mentionné dans la Conception fixée : non touché, conformément à la règle « pas de fichier hors zone Modifier ». **Écart signalé** : le grep de contrôle littéral de la section Conception (« 'craving' n'apparaît plus nulle part dans src/ sauf … ») ne tient pas compte de cette occurrence textuelle préexistante et hors zone S1 ; à traiter par la session qui a autorité sur `src/features/registry.ts` si une reformulation est souhaitée (aucune n'était prescrite ici).

### Validation visuelle (déclarative — non vérifiée en navigateur, cf. règle projet « validation visuelle = humaine »)

- La carte de l'accueil tabac correspondant à l'entrée `boite-a-outils` du registre affiche désormais titre « Stratégies & outils » / icône `Wrench`, à la place exacte de l'ancienne carte « Gérer le craving (4D) » (même position dans le tableau `MODULES`, famille `agir` inchangée).
- Le clic sur cette carte route vers `BoiteAOutilsModule`, qui affiche la phrase d'intro du stub.
- La porte de fin de `SoulagementModule` mène à `boite-a-outils` (libellé « Stratégies et outils »).
- `NicotineModule` n'avait pas de porte vers `craving` à retargeter (voir ci-dessus) ; ses deux portes existantes (`substituts`, `soulagement`) sont inchangées.
- À confirmer visuellement par Thibault en dev (`npm run dev`) — non exécuté ici (pas de navigateur dans cette session).

### Blocages

Aucun. Pas de condition « Si bloqué » rencontrée : le seul module hors périmètres S2-S6 mentionnant encore `'craving'` (`src/features/registry.ts`) le fait dans une chaîne de description libre, pas comme référence de navigation/id — jugé non bloquant pour la gate de cette session, mais signalé ci-dessus pour traçabilité.
