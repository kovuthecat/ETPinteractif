# S5 — Substituts : forme « Vapoteuse » (réduction des risques) · Modèle : Sonnet, effort : medium

> Lire UNIQUEMENT ce fichier + les fichiers « Lire ». Contenu **verbatim ci-dessous** (rédigé par
> Fable, à revalider par Thibault). Doute → STOP.
> Vague 1 — pas de commit ; checklist et statut consignés ici.

## But

Rétablir la vapoteuse dans le module Substituts comme **6ᵉ forme**, avec sa technique
d'utilisation, ses erreurs classiques, et un traitement visuel distinct qui dit clairement :
outil de réduction des risques, pas un médicament.

## Lire

- `src/features/tabac/substituts/SubstitutsModule.tsx` (structure `FORMES_DATA`, `FormeId`, `FORMES_PONCTUELLES`)
- `src/features/tabac/substituts/TechniqueIllustration.tsx`
- `src/features/tabac/substituts/SubstitutsModule.module.css`

## Modifier

- `src/features/tabac/substituts/SubstitutsModule.tsx`
- `src/features/tabac/substituts/TechniqueIllustration.tsx` (nouveau cas `vapoteuse`)
- `src/features/tabac/substituts/SubstitutsModule.module.css` (style du badge/encart)

## Hors périmètre

- La titration du patch, la fiche « méthode patch » (X3) et le reste du module : inchangés.
- La liste `SUBSTITUTS` de `plan-arret/` : c'est S6.
- Aucun dosage chiffré (invariant du module) — les mg/ml sont **interdits** à l'écran.

## Conception (fixée)

1. `FormeId` : ajouter `'vapoteuse'` (en dernière position de la rangée de formes).
2. `FORMES_DATA.vapoteuse` (verbatim) :

```ts
vapoteuse: {
  label: 'Vapoteuse',
  bonnesPratiques: [
    "Choisir le dosage de nicotine avec un professionnel : assez dosé pour calmer réellement les envies — un dosage trop faible est la première cause de retour à la cigarette.",
    "Tirer lentement, en bouffées longues et espacées : la nicotine arrive plus progressivement qu'avec une cigarette (quelques minutes).",
    "Vapoter dès les premiers signes d'envie, sans attendre que le manque soit intense, autant que nécessaire dans la journée.",
    "Objectif : remplacer complètement le tabac fumé. Ensuite, à distance et sans se presser, réduire progressivement le dosage de nicotine.",
  ],
  erreurs: [
    "Sous-doser la nicotine « pour faire moins fort » : l'envie revient, et la cigarette avec.",
    "Continuer à fumer en parallèle durablement : le bénéfice n'existe que si la vapoteuse remplace complètement le tabac (piège du double usage).",
    "Reproduire les bouffées courtes et rapides de la cigarette.",
    "Acheter du matériel ou des e-liquides hors des circuits contrôlés (normes UE/AFNOR).",
  ],
},
// Contenu vapoteuse rédigé d'après HAS/HCSP + rapport OE — à revalider (Thibault)
```

3. **Encart de statut**, affiché uniquement quand la forme `vapoteuse` est sélectionnée
   (au-dessus des bonnes pratiques, style sobre type note — pas une alerte rouge) :
   « La vapoteuse n'est pas un médicament : c'est un outil de réduction des risques, utile aux
   personnes qui n'ont pas réussi avec les traitements validés. Les substituts restent le premier
   choix. À discuter avec un professionnel. » `// à revalider (Thibault)`
4. **Badge** sur le bouton de sélection de la forme : « Réduction des risques » (petit, sous le
   label — double encodage : libellé + pictogramme lucide `Info` ou équivalent déjà importé, pas
   de couleur seule).
5. `FORMES_PONCTUELLES` : **ne pas** y ajouter la vapoteuse (elle ne rentre pas dans la mécanique
   patch + formes ponctuelles de la titration). Si la titration référence la liste des formes
   ailleurs, vérifier que la vapoteuse n'y apparaît pas.
6. `TechniqueIllustration` : ajouter le cas `vapoteuse` en suivant le pattern des autres formes
   (illustration cible : `public/illustrations/tabac/substitut-vapoteuse.png`, prompt en S7) ;
   fallback identique aux autres formes si l'image manque.
7. Cohérence : la carte `vf-vapoteuse` du Vrai/faux et le module « Nicotine ≠ toxique » parlent
   déjà de la vapoteuse — aucun texte à modifier ailleurs.

## Étapes

1. `FormeId` + `FORMES_DATA` + badge + encart.
2. `TechniqueIllustration`.
3. Gate : `npx tsc --noEmit` + `npm run build`.

## Validation

- **auto :** `npx tsc --noEmit` → OK (aucune sortie). `npm run build` → OK (`tsc -b && vite build`,
  1854 modules, build en 1.34 s).
- **visuel (à confirmer par Thibault en dev, non vérifiable par Claude) :** d'après la lecture du
  code — 6ᵉ forme « Vapoteuse » en dernière position avec badge visible ; sélection → encart de
  statut (bleu neutre, pas rouge) + 4 bonnes pratiques + 4 erreurs ; aucune mention mg/ml dans le
  contenu ajouté ; `FORMES_PONCTUELLES` ne référence pas `vapoteuse` donc la titration du patch et
  la fiche « méthode patch » l'ignorent ; aucun autre fichier du module modifié.
- Contenu vapoteuse et texte de l'encart marqués `à revalider (Thibault)` en commentaire de code,
  comme demandé (pas de dosage chiffré, invariant respecté).

## Si bloqué

Si la rangée de 6 formes ne tient plus en largeur → passer la rangée en 2 lignes (wrap), ne pas
réduire les cibles sous 44 px. Autre blocage → STOP + signaler ici.

## Commit (message pour S9)

`feat(substituts): réintégrer la vapoteuse (réduction des risques) avec technique d'utilisation (BO5)`

## Statut

[x] fait

- Étape 1 : `FormeId` + `FORMES_DATA.vapoteuse` (verbatim) + badge « Réduction des risques »
  (label + icône `Info`, sous le libellé du bouton de forme) + encart de statut (icône `Info` +
  texte verbatim, style sobre `--color-nav`/`--color-nav-soft`, pas de rouge) affiché uniquement
  quand `vapoteuse` est sélectionnée, au-dessus des panneaux bonnes pratiques/erreurs.
  `FORMES_PONCTUELLES` inchangée (vapoteuse n'y apparaît pas → absente de la titration patch et du
  sélecteur « Ajouter une prise ponctuelle »).
- Étape 2 : `TechniqueIllustration` — `FormeId` + `ILLUSTRATIONS.vapoteuse: null` ajoutés (cible
  `public/illustrations/tabac/substitut-vapoteuse.png`, prompt en S7) ; fallback placeholder
  identique aux autres formes via le mécanisme existant, aucune autre modif nécessaire.
- Étape 3 : gate exécutée (voir section Validation ci-dessus).
