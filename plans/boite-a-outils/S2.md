# S2 — Module « Stratégies & outils » (boîte à outils du sevrage) · Modèle : Sonnet, effort : xhigh

> Lire UNIQUEMENT ce fichier + les fichiers « Lire ». Contenu médical **verbatim ci-dessous** —
> ne rien inventer, ne pas re-synthétiser le rapport OE. Doute → STOP.
> Vague 2 (après S1) — pas de commit ; checklist et statut consignés ici.

## But

Construire le module `boite-a-outils` : une grille d'outils illustrés, filtrable par situation
(pré-filtrée quand on arrive depuis le module Composantes), avec un détail par outil (principe +
« comment le proposer »), l'outil interactif vague/4D hérité du module Craving, et une fiche
imprimable « Ma boîte à outils » composée des outils cochés. Supprimer ensuite `craving/**`.

## Lire

- `plans/boite-a-outils/index.md` (décisions 1-4)
- `src/features/tabac/situations.ts` (créé en S1)
- `src/features/tabac/boite-a-outils/BoiteAOutilsModule.tsx` (stub S1, à remplacer)
- `src/features/tabac/craving/CravingModule.tsx` + `.module.css` (code vague/4D + fiche anti-envie à déplacer)
- `src/features/tabac/components/IllustrationSlot.tsx`
- `src/components/FicheOverlay.tsx` (API : eyebrow/titre/footer/onClose/children)
- `src/components/ModuleFooterNav.tsx`
- `src/features/tabac/idees-recues/IdeesRecuesModule.tsx` + `.module.css` (référence de style pour cartes + détail)

## Modifier

- `src/features/tabac/boite-a-outils/` — **créer** : `BoiteAOutilsModule.tsx`,
  `BoiteAOutilsModule.module.css`, `data.ts`, `VagueCraving.tsx` (+ son css si séparé)
- `src/features/tabac/craving/**` — **supprimer** (après déplacement du code vague/4D)

## Hors périmètre

- `addiction/` (S3), `idees-recues/` (S4), `substituts/` (S5), `plan-arret/` (S6), `src/styles/**`,
  `src/components/**`. Aucun nouveau composant générique.

## Conception (fixée)

### Structure du module

1. **Intro** (1 ligne) : « Des techniques simples, à choisir selon vos situations. Touchez un
   outil pour voir comment l'utiliser. »
2. **Barre de filtres** : chips des situations (libellés de `situations.ts`), groupées par pilier
   dans un `<details>` replié « Filtrer selon mes situations » ; les situations actives s'affichent
   dépliées en tête avec un bouton « Tout afficher » (efface le filtre).
   - À l'arrivée : `parseSelectionSituations(context)` (S1). Si non vide → ces chips sont actives
     et le `<details>` est replié ; sinon aucune chip active → tous les outils affichés.
   - Filtre = **union** : un outil est affiché si ∅ de chips actives, s'il est `transverse`, ou si
     `outil.situations ∩ chipsActives ≠ ∅`.
3. **Grille d'outils** : cartes (IllustrationSlot `outil.id` + titre + accroche + case à cocher
   « Dans ma fiche » — la case est un vrai `<input type="checkbox">` cliquable indépendamment).
   Clic sur la carte (hors case) → **panneau de détail** (overlay sobre, même approche que le
   détail des cartes Vrai/faux) :
   - illustration, titre, badges des situations liées (double encodage : pastille couleur pilier
     + libellé), mention de preuve (voir data), bloc « **Comment le proposer** » (la formulation
     patient, en retrait avec guillemets), bouton de renvoi éventuel, bouton « Ajouter à ma fiche ».
   - Pour `outil-vague-4d` : bouton primaire « Lancer l'outil » → vue `VagueCraving` (le code de
     `CravingModule` déplacé quasi tel quel : phases idle/active/done, minuteur 3 min, 4D,
     animation respiration, fiche « Ma carte anti-envie » **conservée** à l'identique).
4. **Pied** : bouton « Imprimer ma boîte à outils (n) » (désactivé si n = 0) → `FicheOverlay` :
   - eyebrow `PROGRAMME ETP · SEVRAGE TABAGIQUE`, titre « Ma boîte à outils », footer fil rouge
     (même phrase que craving actuel).
   - Corps : liste 2 colonnes, un bloc par outil coché — **titre + `consigneFiche`** (1 ligne),
     puis le bloc contact 39 89. Lisible en A4 jusqu'à ~12 outils (taille de police plancher :
     ne pas descendre sous celle de la fiche plan d'arrêt).
   - Aparté « En parler — Tabac Info Service 39 89 » conservé dans le module aussi.
5. **`ModuleFooterNav`** : `[{ id: 'plan-arret', label: "Préparer mon plan d'arrêt" },
   { id: 'motivation', label: "Explorer mes raisons d'arrêter" }]`.
6. Zéro persistance : sélection de fiche et filtres = state React éphémère.

### `data.ts` — modèle

```ts
import type { ModuleId } from '../../types';

export type Preuve = 'demontree' | 'experts';
export const PREUVE_LABELS: Record<Preuve, string> = {
  demontree: 'Efficacité démontrée dans les études',
  experts: 'Recommandé par les experts du sevrage',
};

export interface Outil {
  /** = id d'illustration (public/illustrations/tabac/<id>.png). */
  id: string;
  titre: string;
  accroche: string;            // 1 ligne, carte
  situations: string[];        // ids de situations.ts
  transverse?: boolean;        // affiché quel que soit le filtre
  principe: string;            // 1-2 phrases, panneau détail
  proposition: string;         // « Comment le proposer » — formulation patient
  preuve: Preuve;
  consigneFiche: string;       // 1 ligne, fiche imprimée
  renvoi?: { id: ModuleId; label: string };
  interactif?: 'vague4d';      // ouvre l'outil interactif au lieu d'un simple détail
}
```

### `data.ts` — LES 14 OUTILS (verbatim, adapté du rapport OE)

1. **`outil-vague-4d`** · « Laisser passer la vague — les 4D » · transverse · interactif `vague4d` · preuve `experts`
   - accroche : « L'envie monte, culmine, puis retombe d'elle-même en quelques minutes. »
   - principe : « Une envie de fumer est une vague de 3 à 5 minutes : elle redescend toujours, qu'on fume ou non. Les 4D — Différer, Détourner l'attention, se Détendre, boire De l'eau — aident à tenir pendant le pic. »
   - proposition : « Quand l'envie arrive, ne luttez pas de front : regardez l'heure, et tenez quelques minutes avec un des 4D. Chronométrez si besoin : vous constaterez que l'envie passe réellement. »
   - consigneFiche : « Différer · Détourner · Détendre · D'eau — l'envie passe en 3 à 5 min. »

2. **`outil-si-alors`** · « Mes plans “SI… ALORS…” » · transverse · preuve `demontree` · renvoi `{ id: 'plan-arret', label: 'Les inscrire dans mon plan d'arrêt' }`
   - accroche : « Décider sa parade à l'avance, pour ne pas avoir à décider au moment critique. »
   - principe : « Pré-programmer une réponse précise pour chaque situation à risque court-circuite la décision au moment où l'envie frappe. C'est la technique la mieux démontrée du sevrage comportemental. »
   - proposition : « Formulez 3 à 5 plans précis : “SI un collègue me propose une cigarette, ALORS je réponds : non merci, j'ai arrêté — et je prends un verre d'eau.” “SI l'envie monte après le repas, ALORS je sors marcher 5 minutes.” Écrivez-les, et relisez-les chaque matin les premières semaines. »
   - consigneFiche : « J'écris 3 à 5 plans “SI… ALORS…” et je les relis chaque matin. »

3. **`outil-bouger`** · « Bouger 10 minutes » · situations `['craving','stress','ennui','stimulation','deprime','fringales']` · preuve `demontree`
   - accroche : « L'effet anti-envie le plus rapide : le corps en mouvement. »
   - principe : « Un exercice bref — même 10 minutes de marche rapide — fait nettement baisser l'envie de fumer, avec un effet qui persiste 20 à 30 minutes après l'effort. »
   - proposition : « Quand l'envie est forte : sortez marcher d'un bon pas 10 minutes, ou montez et descendez un escalier 3-4 fois. Si vous ne pouvez pas bouger : serrez fort les poings et les bras 10 secondes, relâchez, recommencez 5 fois. »
   - consigneFiche : « Envie forte → 10 min de marche rapide, un escalier, ou 5 contractions des poings. »

4. **`outil-respiration`** · « Respirer pour redescendre » · situations `['stress','anxiete','nervosite','irritabilite']` · preuve `experts` · renvoi `{ id: 'soulagement', label: 'Pourquoi la cigarette ne détend pas vraiment' }`
   - accroche : « Deux minutes de respiration lente calment plus durablement qu'une cigarette. »
   - principe : « La respiration lente active le système d'apaisement du corps. Rappel utile : la cigarette ne réduit pas le stress — elle soulage le manque qu'elle a elle-même créé ; après le sevrage, le niveau de stress de fond diminue chez la plupart des personnes. »
   - proposition : « Quand le stress monte : inspirez par le nez en comptant jusqu'à 4, retenez jusqu'à 7, soufflez lentement par la bouche jusqu'à 8. Répétez 3 fois — moins de 2 minutes, faisable partout. Autre option : une main sur le ventre, 10 respirations lentes en gonflant le ventre. »
   - consigneFiche : « Inspirez 4 s — retenez 7 s — soufflez 8 s, 3 fois. »

5. **`outil-surfer`** · « Surfer sur l'envie » · situations `['anxiete','craving','manque']` · preuve `experts`
   - accroche : « Observer l'envie sans lutter ni céder — elle s'épuise d'elle-même. »
   - principe : « Observer l'envie comme une vague qu'on regarde passer, sans la juger, affaiblit peu à peu le lien entre la situation et la cigarette. »
   - proposition : « Quand l'envie arrive, dites-vous : “Je ressens une envie. Elle est là. Je la laisse passer.” Observez ce qui se passe dans le corps — gorge serrée, mains agitées — sans réagir. En 2 à 3 minutes, elle redescend seule. Chaque envie surfée sans fumer affaiblit la suivante. »
   - consigneFiche : « J'observe l'envie comme une vague : elle monte, elle redescend seule. »

6. **`outil-place-nette`** · « Faire place nette » · situations `['cafe','repas','voiture']` · preuve `experts`
   - accroche : « Ce qu'on ne voit plus déclenche moins l'envie. »
   - principe : « Chaque objet ou lieu associé à la cigarette est un déclencheur. Les retirer du champ de vision désamorce l'automatisme avant qu'il ne se lance. »
   - proposition : « Avant le jour J : rangez cendriers et briquets hors de vue, ne gardez plus de cigarettes à la maison, videz la voiture (cendrier, boîte à gants) et mettez des pastilles à la menthe à la place. L'objectif est que la situation ne “propose” plus la cigarette. »
   - consigneFiche : « Cendriers, briquets, paquets : hors de vue — maison et voiture. »

7. **`outil-routine`** · « Casser la routine » · situations `['cafe','repas','pause','voiture','telephone']` · preuve `experts`
   - accroche : « Changer un détail du rituel suffit souvent à désamorcer le geste. »
   - principe : « Le café, la fin du repas ou la pause déclenchent la cigarette parce qu'ils forment une séquence automatique. Modifier un élément de la séquence — lieu, ordre, boisson — casse l'enchaînement. »
   - proposition : « Prenez votre café dans une autre pièce, debout, ou remplacez-le par un thé les 2-3 premières semaines. Dès la dernière bouchée du repas, levez-vous de table : brossage de dents, vaisselle, 5 minutes dehors. Prenez vos pauses dans un lieu où on ne peut pas fumer. Au bout de quelques jours, le café ou le repas redeviennent des plaisirs indépendants de la cigarette. »
   - consigneFiche : « Je change un détail de chaque rituel : lieu, ordre, boisson. »

8. **`outil-mains-bouche`** · « Occuper les mains et la bouche » · situations `['fringales','ennui','telephone','craving','cafe']` · preuve `experts`
   - accroche : « Un geste de remplacement, incompatible avec la cigarette. »
   - principe : « La cigarette occupe la main et la bouche : lui substituer un autre geste comble le vide sensoriel pendant que l'envie redescend. »
   - proposition : « Gardez à portée de main : chewing-gum sans sucre, balle anti-stress, grand verre d'eau à boire par petites gorgées, un stylo à manipuler. Occupez la main qui tenait la cigarette. »
   - consigneFiche : « Chewing-gum, balle anti-stress, grand verre d'eau. »

9. **`outil-journal`** · « Une semaine d'observation » · transverse · preuve `experts`
   - accroche : « Avant d'arrêter : repérer ses automatismes pour préparer ses parades. »
   - principe : « Noter chaque cigarette avec son contexte fait apparaître les schémas répétitifs — c'est la première étape recommandée pour planifier des alternatives ciblées. »
   - proposition : « Pendant la semaine qui précède l'arrêt, notez chaque cigarette : l'heure, où vous êtes, ce que vous faites, ce que vous ressentez. Vous verrez vos situations à risque se dessiner — ce sont elles qu'on équipera d'une parade. »
   - consigneFiche : « Avant l'arrêt : je note chaque cigarette (quand, où, avec quoi, en ressentant quoi). »

10. **`outil-refus`** · « Ma phrase de refus » · situations `['social','alcool','pause']` · preuve `experts`
    - accroche : « Une réponse courte, ferme, préparée à l'avance. »
    - principe : « La pression sociale — l'offre de cigarette, l'ambiance festive — est un déclencheur majeur. Une phrase de refus préparée évite d'avoir à improviser au moment vulnérable. Vigilance particulière avec l'alcool, qui baisse la garde : les premières semaines, mieux vaut le limiter. »
    - proposition : « Préparez votre réponse avant la soirée : “Non merci, j'ai arrêté.” Courte, ferme, sans justification. Gardez un verre non alcoolisé en main — ça occupe le geste. Si l'envie monte, éloignez-vous quelques minutes. Chaque soirée tenue affaiblit l'association. »
    - consigneFiche : « “Non merci, j'ai arrêté.” Court, ferme, préparé à l'avance. »

11. **`outil-recompense`** · « Se récompenser — la tirelire » · situations `['plaisir','deprime','stimulation']` · preuve `experts`
    - accroche : « Remplacer la récompense de la cigarette par de vraies récompenses. »
    - principe : « Le cerveau était habitué à une “récompense” nicotinique répétée. Les premières semaines, planifier des plaisirs de remplacement — et matérialiser l'argent économisé — soutient la motivation. Ce n'est pas du luxe, c'est une stratégie. »
    - proposition : « Chaque jour, prévoyez au moins un petit plaisir : un bon repas, un bain, un épisode de votre série, un appel à un ami. Et chaque soir, mettez de côté l'argent des cigarettes du jour ; au bout d'une semaine, offrez-vous quelque chose avec. Voir la somme grandir est un moteur puissant. »
    - consigneFiche : « L'argent des cigarettes va dans une tirelire ; chaque semaine, un plaisir. »

12. **`outil-anti-ennui`** · « La liste anti-ennui » · situations `['ennui','stimulation','telephone']` · preuve `experts`
    - accroche : « L'envie a besoin de vide pour s'installer. »
    - principe : « L'ennui est un piège classique : la cigarette remplissait un vide de temps et de sensations. Une liste d'activités courtes, préparée à l'avance, évite de rester inactif face à une envie. »
    - proposition : « Préparez une liste de 10 activités de 5 à 15 minutes : appeler quelqu'un, ranger un tiroir, arroser les plantes, s'étirer, écouter un podcast, marcher en mâchant un chewing-gum… Affichez-la sur le frigo et dans le téléphone, et piochez dedans dès que l'ennui s'installe. »
    - consigneFiche : « Ma liste de 10 activités courtes, affichée sur le frigo et dans le téléphone. »

13. **`outil-faux-pas`** · « Si j'ai un écart — le plan de secours » · transverse · preuve `experts` · renvoi `{ id: 'plan-arret', label: "Le préparer dans mon plan d'arrêt" }`
    - accroche : « Un faux pas n'est pas une rechute — à condition de savoir quoi faire juste après. »
    - principe : « Fumer une cigarette pendant le sevrage n'efface rien de ce qui est acquis. Ce qui transforme un écart en rechute, c'est la pensée “tout est foutu”. La plupart des ex-fumeurs ont connu des faux pas en chemin ; ce qui compte, c'est le geste d'après. »
    - proposition : « Préparez, avant le jour J, un plan de secours en 3 gestes : 1. Je jette immédiatement le paquet et le briquet. 2. J'appelle quelqu'un — un proche, mon soignant, le 39 89. 3. Je relis mes raisons d'arrêter. Puis je continue mes substituts exactement comme avant : les 24 heures qui suivent sont décisives, et une cigarette ne fait pas de vous un fumeur — c'est acheter un paquet qui le ferait. Comprendre ce qui a déclenché l'écart, sans se blâmer, prépare la suite. »
    - consigneFiche : « Un écart n'est pas une rechute : je jette le paquet, j'appelle, je relis mes raisons. »

14. **`outil-substituts`** · « Traiter le manque — les substituts » · situations `['manque','irritabilite','nervosite','concentration','sommeil','fringales','craving']` · preuve `demontree` · renvoi `{ id: 'substituts', label: 'Voir les substituts et la titration' }`
    - accroche : « Les signes physiques du manque se traitent — c'est le rôle des substituts. »
    - principe : « Irritabilité, nervosité, troubles de la concentration ou du sommeil, fringales : ces signes traduisent le manque de nicotine. Bien dosés, les substituts les font disparaître — et toutes les techniques de cette boîte à outils marchent mieux avec eux. »
    - proposition : « Le patch assure un fond stable toute la journée ; une forme orale (gomme, pastille, spray) répond aux pics d'envie. Le bon dosage est celui qui fait disparaître les signes de manque — il s'ajuste, avec un professionnel. »
    - consigneFiche : « Patch pour le fond + forme orale pour les pics d'envie. »

### Détail vague/4D (`VagueCraving.tsx`)

Déplacer le code de `CravingModule.tsx` (constantes de la vague, minuteur, 4D, animation
respiration, fiche « Ma carte anti-envie » avec `FICHE_D_CONSIGNES`) dans
`boite-a-outils/VagueCraving.tsx`, **sans changer les textes ni la mécanique** (X2/A9 validés).
Seules adaptations : plus de `ModuleFooterNav` interne (il vit au niveau du module), bouton
« ← Retour aux outils » pour revenir à la grille. Le CSS nécessaire migre depuis
`CravingModule.module.css`. Supprimer ensuite `craving/**` et vérifier qu'aucun import n'y pointe.

## Étapes

1. `data.ts` (verbatim ci-dessus) + `situations.ts` en dépendance.
2. Grille + filtres + détail (sans le 4D).
3. `VagueCraving.tsx` (déplacement) + branchement depuis `outil-vague-4d`.
4. Fiche « Ma boîte à outils » (FicheOverlay).
5. Suppression `craving/**` + grep imports.
6. Gate : `npx tsc --noEmit` + `npm run build`.

## Validation

- **auto :** typecheck + build verts ; `craving/` supprimé sans import cassé.
- **visuel (→ consigné ici) :** arrivée sans contexte → 14 outils ; chips « Stress » seule →
  outils 3, 4 + transverses (1, 2, 9, 13) uniquement ; détail lisible à 1 m ; « Lancer l'outil »
  → vague 3 min fonctionnelle, carte anti-envie imprimable ; 5 outils cochés → fiche A4 lisible,
  2 colonnes ; bouton fiche désactivé à 0 coché.

## Si bloqué

- Si la grille de 14 cartes + illustration est illisible en 1024×768 → passer les cartes en
  2 lignes de texte max + illustration réduite, mais **jamais** moins de 44 px de cible.
- Si la fiche déborde de l'A4 au-delà de 10 outils → réduire à consigne seule (sans titre) au-delà
  de 8, ou signaler ici. Ne pas inventer d'autre format.

## Commit (message pour S9)

`feat(tabac): module Stratégies & outils — 14 outils filtrés par situation, vague 4D intégrée, fiche imprimable (BO2)`

## Statut

[x] fait

### Réalisé

- `data.ts` : les 14 outils transcrits verbatim (`Outil`, `Preuve`, `PREUVE_LABELS`).
- `BoiteAOutilsModule.tsx` + `.module.css` : intro, barre de filtres (chips groupées par
  pilier dans un `<details>` replié + rangée des chips actives avec « Tout afficher »),
  filtre en union (∅ actif / transverse / intersection), grille de 14 cartes (illustration +
  titre + accroche + case « Dans ma fiche » indépendante), panneau de détail (illustration,
  titre, badges situations à double encodage pastille-pilier + libellé, mention de preuve,
  bloc « Comment le proposer » en retrait avec guillemets, renvoi éventuel, bouton
  « Lancer l'outil » pour `outil-vague-4d`, bouton « Ajouter à ma fiche » / « Dans ma fiche »),
  pied (aparté 39 89, bouton « Imprimer ma boîte à outils (n) » désactivé à 0, `ModuleFooterNav`
  plan-arrêt/motivation), fiche `FicheOverlay` 2 colonnes (titre + `consigneFiche` par outil,
  puis bloc contact 39 89).
- `VagueCraving.tsx` + `.module.css` : code de `CravingModule` déplacé quasi tel quel (phases
  idle/active/done, minuteur 3 min, 4D, animation respiration, fiche « Ma carte anti-envie »
  identique) ; seules adaptations : suppression du `ModuleFooterNav` interne, ajout du bouton
  « ← Retour aux outils ». Branché depuis le détail de `outil-vague-4d`.
- Suppression de `src/features/tabac/craving/**` ; grep du repo : plus aucun import ne pointe
  dessus (un seul résidu, un commentaire non fonctionnel dans `plan-arret/PlanArretModule.tsx`
  ligne ~25 qui référence l'ancien chemin — hors zone S2, signalé pour S9).
- Couleurs des 3 piliers dérivées des 3 teintes sémantiques existantes (`--color-vigilance` /
  `--color-nav` / `--color-confort`) via des classes CSS locales au module — aucun token global
  ajouté, aucun fichier `src/styles/**` touché.
- Fallback « Si bloqué » (débordement fiche) implémenté de façon déterministe : au-delà de
  10 outils cochés, les blocs à partir du 9e n'affichent plus le titre (consigne seule) — pas
  besoin d'un aller-retour supplémentaire.

### Validation — auto

- `npx tsc --noEmit` : **PASS** (aucune erreur).
- `npm run build` : **PASS** (`tsc -b && vite build` verts, build en ~1s).
- `npm test` : non exécuté (aucun `lib/` touché par cette session).
- `craving/` supprimé, grep de tout `src/` : aucun import cassé (seul résidu : commentaire non
  fonctionnel dans `plan-arret/`, voir ci-dessus).

### Validation — visuel

Pas de navigateur côté Claude (règle projet) : contrôle du code/CSS contre la checklist, pas
d'observation pixel réelle. Points de la checklist :

- Arrivée sans contexte (`activeSituations` vide) → `outilVisible` retourne `true` pour tous :
  les 14 outils s'affichent. Conforme.
- Chip « Stress » seule active → visibles : `outil-bouger` et `outil-respiration` (situations
  incluant `stress`) + les 4 transverses (`outil-vague-4d`, `outil-si-alors`, `outil-journal`,
  `outil-faux-pas`) — soit 6 au total, conforme à l'attendu « outils 3, 4 + transverses (1, 2,
  9, 13) ».
- Détail : illustration 96px, titre serif, badges, preuve, principe, bloc « Comment le
  proposer » en retrait (bordure + fond `--color-nav-soft`), bouton renvoi si présent, actions
  (Lancer l'outil / Ajouter à ma fiche) — lisibilité ~1 m non vérifiée visuellement mais tailles
  de police alignées sur les autres modules (idees-recues, plan-arret).
- « Lancer l'outil » ouvre `VagueCraving` : code identique à l'ancien `CravingModule` (minuteur
  180 s, 4D, respiration, fiche anti-envie) — mécanique non ré-testée en navigateur, mais aucune
  logique modifiée par rapport à l'original (diff = suppression ModuleFooterNav + ajout bouton
  retour).
- Fiche « Ma boîte à outils » : grille CSS 2 colonnes (1 colonne sous 560px), police 15px
  (plancher aligné sur `plan-arret/PlanArretModule.module.css` `.ficheList`), fallback
  « consigne seule au-delà du 8e » implémenté si > 10 outils cochés.
- Bouton fiche désactivé (`disabled`) quand `ficheOutils.length === 0`.
- **À confirmer visuellement par Thibault** : rendu réel du panneau de détail, de la vague/4D
  et de la densité de la fiche à 10-12 outils cochés (au-delà de ce que le code/CSS permet de
  garantir sans rendu navigateur).

### Écarts par rapport à S2.md

- Aucun écart de conception. Deux choix d'implémentation non fixés explicitement par S2.md, tous
  deux dans l'esprit « ne rien inventer au-delà » (réutilisation de patterns existants) :
  - Libellés des 3 groupes de pilier dans le `<details>` (« Signes physiques du manque », « Ce
    que la cigarette apporte », « Automatismes du quotidien ») — non fournis par `situations.ts`
    (qui n'expose que l'id de pilier), dérivés des commentaires déjà présents dans ce fichier.
  - Mapping couleur pilier → teinte sémantique existante (physique = vigilance, psychologique =
    nav, comportementale = confort) : S2.md demande un double encodage (pastille couleur pilier
    et libellé) sans fixer la couleur exacte par pilier.
