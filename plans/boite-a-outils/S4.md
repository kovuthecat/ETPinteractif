# S4 — Vrai/faux : 6 nouvelles cartes + 3 mises à jour + renvois · Modèle : Sonnet, effort : medium

> Lire UNIQUEMENT ce fichier + les fichiers « Lire ». Contenu **verbatim ci-dessous** (adapté du
> rapport OE par Fable) — ne rien inventer, ne pas reformuler. Doute → STOP.
> Vague 1 — pas de commit ; checklist et statut consignés ici.
> ⚠️ Le renvoi `'boite-a-outils'` référence un module posé par S1 dans la même vague : c'est prévu
> (string, pas d'échec de typecheck). Ne pas « corriger ».

## But

Enrichir le module Vrai/faux avec les données des rapports OE poids et cigarette électronique,
reformuler la carte faux pas dans le style demandé par Thibault, et retargeter les renvois
`'craving'` vers `'boite-a-outils'`.

## Lire

- `src/features/tabac/idees-recues/data.ts`
- `docs/evidence-tabac/2026-07-10-rapport-openevidence-sevrage.md` (référence seulement — le contenu à implémenter est ci-dessous)

## Modifier

- `src/features/tabac/idees-recues/data.ts` **uniquement** (pas le composant, pas le CSS).

## Hors périmètre

- `IdeesRecuesModule.tsx` : si les 21 cartes posent un problème d'affichage, le **signaler ici**
  sans le modifier.

## Conception (fixée)

### A. Renvois et libellés

1. `RENVOI_LABELS` : supprimer la clé `craving`, ajouter `'boite-a-outils': 'Stratégies & outils'`.
2. Cartes `vf-faux-pas` et `vf-vague` : `renvoi: 'boite-a-outils'`.

### B. Mises à jour de cartes existantes (verbatim)

1. **`vf-faux-pas`** — reformulation demandée par Thibault + données du rapport :
   - `affirmation` : `'« Si je craque pour une cigarette, c'est un échec — tout est à refaire. »'`
   - `explication` : `"Un écart n'est pas une rechute : il n'efface ni les bénéfices déjà acquis, ni ce que vous avez appris. Il faut en moyenne plusieurs tentatives — souvent 5 ou 6 — avant un arrêt durable, et repartir immédiatement après un écart multiplie les chances de réussite. Ce qui compte : jeter le paquet, continuer les substituts, comprendre le déclencheur."`
   - `source` : `'Tabac Info Service · JAMA 2022'` + `// à revalider (Thibault)`
   - `renvoi` : `'boite-a-outils'` (déjà couvert par A).
2. **`vf-poids`** — aligner les chiffres sur le rapport (2-4 kg → fourchette documentée) :
   - `explication` : `"La prise moyenne est de 3 à 5 kg, très variable : environ une personne sur cinq n'en prend pas, ou en perd. La nicotine augmentait les dépenses d'énergie et coupait l'appétit : le corps se rééquilibre. Substituts bien dosés et activité physique limitent cette prise — et rien n'oblige à mener arrêt et régime de front."`
   - `source` : `'Tabac Info Service · Cochrane'` + `// à revalider (Thibault)`
3. **`vf-vapoteuse`** — inchangée sur le fond, vérifier seulement la cohérence avec les 3
   nouvelles cartes vapoteuse (pas de doublon d'affirmation ; l'existante porte sur la
   dangerosité comparée, les nouvelles sur l'efficacité, le double usage et la « vapeur d'eau »).

### C. Nouvelles cartes (verbatim — insérer les cartes poids après `vf-poids`, les cartes vapoteuse après `vf-vapoteuse`)

```ts
{
  id: 'vf-poids-coeur',
  affirmation: '« Prendre du poids à l'arrêt annule le bénéfice pour le cœur. »',
  verdict: 'faux',
  explication:
    "Même chez ceux qui prennent beaucoup de poids (plus de 10 kg), le risque de mourir d'une maladie cardiovasculaire est réduit de moitié à deux tiers par rapport à ceux qui continuent de fumer. Le cœur gagne toujours au change — quelle que soit la balance.",
  source: 'NEJM 2018', // à revalider (Thibault)
  renvoi: 'benefices-arret',
  actif: true,
},
{
  id: 'vf-fumer-mince',
  affirmation: '« Mieux vaut continuer à fumer que de prendre quelques kilos. »',
  verdict: 'faux',
  explication:
    "Le tabac tue un fumeur régulier sur deux. Les 3 à 5 kg moyens de l'arrêt n'ont aucun impact comparable sur la santé — et ils se stabilisent puis diminuent avec les années. Aucun bénéfice du tabac ne compense ses risques.",
  source: 'Tabac Info Service · NEJM 2018', // à revalider (Thibault)
  renvoi: 'benefices-arret',
  actif: true,
},
{
  id: 'vf-poids-regime',
  affirmation: "« Il faut se mettre au régime en même temps qu'on arrête de fumer. »",
  verdict: 'faux',
  explication:
    "C'est même déconseillé : une restriction stricte mime les sensations de manque et fragilise le sevrage. L'objectif des premiers mois est « maintenir, pas maigrir » : bouger 30 minutes par jour (seule mesure démontrée sur le poids à un an), boire de l'eau, garder des en-cas peu caloriques à portée. On s'occupe du poids plus tard, si besoin.",
  source: 'ACC 2018 · Cochrane', // à revalider (Thibault)
  actif: true,
},
{
  id: 'vf-vape-aide',
  affirmation: '« La vapoteuse peut aider à arrêter de fumer. »',
  verdict: 'vrai',
  nuance: "Ce n'est pas un médicament : les substituts restent le premier choix, la vapoteuse une option à discuter.",
  explication:
    "Les études récentes montrent qu'elle aide au moins autant que les substituts nicotiniques — à condition de basculer complètement, sans continuer à fumer à côté. En France, elle est considérée comme un outil de réduction des risques, pas comme un traitement : à discuter avec un professionnel.",
  source: 'Cochrane 2025 · HCSP', // à revalider (Thibault)
  renvoi: 'substituts',
  actif: true,
},
{
  id: 'vf-double-usage',
  affirmation: '« Vapoter tout en continuant à fumer, c'est déjà bon pour la santé. »',
  verdict: 'faux',
  explication:
    "Le bénéfice de la vapoteuse n'existe que si elle remplace complètement le tabac fumé. Fumer et vapoter en même temps expose au moins autant que fumer seul — c'est le piège du « double usage ». Si on vapote, l'objectif est zéro cigarette.",
  source: 'Cochrane · revues 2022-2026', // à revalider (Thibault)
  renvoi: 'substituts',
  actif: true,
},
{
  id: 'vf-vapeur-eau',
  affirmation: "« La vapeur de la vapoteuse, c'est juste de la vapeur d'eau. »",
  verdict: 'faux',
  explication:
    "L'aérosol contient de la nicotine, du propylène glycol, de la glycérine, des arômes et des traces de substances irritantes. Beaucoup moins que la fumée — sans combustion, ni goudrons ni monoxyde de carbone — mais pas rien : c'est pourquoi l'objectif à terme reste de s'en libérer aussi.",
  source: 'Santé publique France', // à revalider (Thibault)
  renvoi: 'nicotine-toxique',
  actif: true,
},
```

> Note apostrophes : les littéraux ci-dessus utilisent des guillemets français et des apostrophes
> typographiques à l'intérieur des chaînes — reprendre le style d'échappement du fichier existant
> (chaînes simples avec `'` échappé ou doubles quotes, comme les cartes actuelles).

## Étapes

1. A (renvois) → 2. B (mises à jour) → 3. C (nouvelles cartes) →
4. Gate : `npx tsc --noEmit` + `npm run build`.

## Validation

- **auto :** `npx tsc --noEmit` → OK, aucune erreur. `npm run build` → échoue, mais uniquement à
  cause de 2 erreurs TS6133 pré-existantes dans `src/features/diabete/activite/ActiviteModule.tsx`
  (hors zone S4, fichier en cours de modification par une autre session de la même vague —
  `git status` le montre modifié et non commité, sans lien avec `idees-recues`). Aucune erreur
  dans `data.ts` ni dans le reste du module tabac. 21 cartes présentes, ids tous uniques (vérifié
  manuellement), tous les `renvoi` utilisés (`soulagement`, `nicotine-toxique`, `benefices-arret`,
  `substituts`, `addiction`, `boite-a-outils`, `motivation`, `plan-arret`) existent bien dans
  `RENVOI_LABELS`.
- **visuel (→ consigné ici) :** non exécuté (pas de navigateur/Playwright, conformément aux règles
  du projet) — à vérifier par Thibault : les 6 nouvelles cartes s'affichent et se retournent ;
  badge « …et c'est plus nuancé » présent sur `vf-vape-aide` (a un champ `nuance`) ; renvoi de
  `vf-faux-pas` et `vf-vague` mène au libellé « Stratégies & outils » / stub `boite-a-outils`
  (module complet après S2).

## Si bloqué

Deck illisible ou pagination cassée à 21 cartes → STOP + signaler ici (le composant est hors périmètre).

## Commit (message pour S9)

`feat(idees-recues): cartes poids et vapoteuse issues du rapport OE, faux pas reformulé, renvois boîte à outils (BO4)`

## Statut

[x] fait — A (renvois), B (mises à jour vf-faux-pas/vf-poids), C (6 nouvelles cartes) appliqués
dans `src/features/tabac/idees-recues/data.ts` uniquement. `npx tsc --noEmit` vert. `npm run
build` échoue uniquement sur des erreurs pré-existantes hors zone (`diabete/activite`, autre
session de la vague — voir Validation). Validation visuelle non faite (nécessite Thibault).
