# S6 — Plan d'arrêt : section « Si j'ai un écart » + vapoteuse dans les chips · Modèle : Sonnet, effort : medium

> Lire UNIQUEMENT ce fichier + les fichiers « Lire ». Contenu **verbatim ci-dessous**. Doute → STOP.
> Vague 2 (après S1 — uniquement pour éviter tout chevauchement de vague avec S5/substituts,
> aucune dépendance de code) — pas de commit ; checklist et statut consignés ici.

## But

Intégrer la préparation au faux pas dans le plan d'arrêt (recommandation forte du rapport OE :
préparer la réponse à l'écart AVANT le jour J) : une 7ᵉ section « Si j'ai un écart » avec les
3 gestes du plan de secours, reprise sur la fiche imprimée. Ajouter « Vapoteuse » aux chips de
substituts (cohérence avec S5).

## Lire

- `src/features/tabac/plan-arret/PlanArretModule.tsx` + `.module.css`
- `plans/boite-a-outils/S2.md` — outil 13 `outil-faux-pas` (le contenu doit rester cohérent, pas identique mot à mot : ici c'est la version « plan personnel »)

## Modifier

- `src/features/tabac/plan-arret/PlanArretModule.tsx` (+ `.module.css` si besoin)

## Hors périmètre

- `FicheOverlay` (générique, inchangé), les 6 sections existantes (numérotation à décaler
  seulement si nécessaire), `boite-a-outils/` (S2), `substituts/` (S5).

## Conception (fixée)

1. **Chips substituts** : `SUBSTITUTS` gagne `'Vapoteuse'` en fin de liste.
2. **Nouvelle section « 7. Si j'ai un écart »** (après « 6. Autour de moi ») :
   - Une phrase d'ancrage au-dessus des chips :
     « Un écart n'est pas une rechute. Je prépare maintenant mes 3 gestes pour repartir aussitôt. »
   - Un `ChipGroup` (réutiliser le composant local existant, avec saisie libre comme les autres
     sections) pré-alimenté avec les gestes fixes :

     ```ts
     const GESTES_ECART: string[] = [
       'Je jette le paquet et le briquet',
       "J'appelle quelqu'un — un proche ou le 39 89",
       'Je relis mes raisons d'arrêter',
       'Je continue mes substituts comme avant',
     ];
     ```

3. **Fiche imprimée** : nouveau bloc `fiche-bloc` « Si j'ai un écart » après « Mes parades » :
   - eyebrow : `Si j'ai un écart — repartir aussitôt`
   - la phrase : « Un écart n'est pas une rechute : les 24 heures qui suivent comptent. »
   - puis la liste des gestes cochés (même rendu que les autres blocs).
   - Le bloc n'apparaît sur la fiche que si ≥ 1 geste coché (même logique conditionnelle que les
     autres sections).
4. `auMoinsUneSection` : intégrer la nouvelle section au calcul.
5. Ton : aucun vocabulaire d'échec ou de faute (« craquer », « rechute » interdits dans les
   libellés ; « écart » uniquement).

## Étapes

1. Chips substituts → 2. Section 7 → 3. Bloc fiche + `auMoinsUneSection` →
4. Gate : `npx tsc --noEmit` + `npm run build`.

## Validation

- **auto :** typecheck + build verts.
- **visuel (→ consigné ici) :** section 7 cohérente avec les autres (chips + saisie libre) ;
  fiche A4 : bloc « Si j'ai un écart » présent si coché, absent sinon ; la fiche reste sur une
  page avec toutes les sections remplies ; « Vapoteuse » sélectionnable en section 2 et rendue
  sur la fiche.

## Si bloqué

Fiche déborde de l'A4 avec les 7 sections pleines → compacter l'interligne des blocs de fiche
(CSS local), sinon STOP + signaler ici.

## Commit (message pour S9)

`feat(plan-arret): section « Si j'ai un écart » (plan de secours) + vapoteuse dans les substituts (BO6)`

## Statut

[x] fait

**Étapes réalisées :**

1. Chips substituts : `'Vapoteuse'` ajouté en fin de liste `SUBSTITUTS`.
2. Section « 7. Si j'ai un écart » ajoutée après « 6. Autour de moi » : phrase d'ancrage
   (« Un écart n'est pas une rechute. Je prépare maintenant mes 3 gestes pour repartir aussitôt. »)
   et `ChipGroup` pré-alimenté avec `GESTES_ECART` (4 gestes fixes, saisie libre incluse comme les
   autres sections).
3. Bloc fiche « Si j'ai un écart — repartir aussitôt » ajouté après le bloc « Mes parades »
   (eyebrow + phrase « Un écart n'est pas une rechute : les 24 heures qui suivent comptent. » +
   liste des gestes cochés), conditionné à ≥ 1 geste coché.
4. `auMoinsUneSection` intègre désormais `gestesEcartChoisis.length > 0`.
5. Aucun vocabulaire d'échec (« craquer », « rechute ») utilisé dans les libellés ; « écart »
   uniquement — vérifié par relecture du fichier final.

**Validation auto :**

- `npx tsc --noEmit` → OK (aucune erreur).
- `npm run build` → OK (build Vite terminé sans erreur, `tsc -b && vite build` vert).

**Checklist visuelle (à confirmer humainement, non vérifiable sans navigateur) :**

- [ ] Section 7 visuellement cohérente avec les sections 2-5 (chips + saisie libre) dans l'app.
- [ ] Fiche A4 : bloc « Si j'ai un écart » présent si ≥ 1 geste coché, absent sinon.
- [ ] Fiche reste sur une page avec les 7 sections pleines (aucun débordement A4 constaté au build ;
      pas de compactage CSS nécessaire à ce stade — à confirmer visuellement).
- [ ] « Vapoteuse » sélectionnable en section 2 et rendue correctement sur la fiche imprimée.

**Écarts par rapport au fichier :** aucun. Fichiers modifiés : uniquement
`src/features/tabac/plan-arret/PlanArretModule.tsx` et
`src/features/tabac/plan-arret/PlanArretModule.module.css` (ajout de la classe locale
`.ficheEcartPhrase` pour la phrase du bloc fiche, cohérente avec les conventions CSS locales déjà
en usage dans ce module).
