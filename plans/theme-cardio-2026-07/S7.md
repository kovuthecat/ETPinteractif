# theme-cardio · S7 — M4 « La tension » (artère sous pression + règle des 3 + fiche)   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 3 (parallèle : oui — dossier disjoint `tension/`)**
> Exécutant : UNIQUEMENT la tâche ci-dessous ; fichiers sous « Lire » / « Modifier ». Design fixé.
> Doute/blocage → STOP. Squelette = **moule S4/S5**.

- Date : 2026-07-22 · Branche : —

## Lire (commun)

- `docs/cardio/CONTENU_cardio.md` — **§M4** (contenu **validé G1**). Rappels G1 : **AMT < 135/85** (2ᵉ niveau) ;
  message « **une seule mesure ne suffit pas** » ; **pas de « < 140/90 »** ; « limiter le sel » renvoie à M8.
- Prototype `…/ETP Cardio - Prototype.dc.html` **§MODULE 4** (lignes 125-162 + logique 748-760).
- Socle : `ArtereCoupe` `{ encrassement; fragile?; caillot?; renforce?; size?; className? }` ;
  `FicheOverlay` `{ eyebrow; titre; footer?; onClose; children }`. `ModuleShell` (contrat habituel).

## Hors périmètre

- Pas de dramatisation « tueur ». Automesure **valorisante**. **Aucune cible chiffrée imposée à l'écran**
  (survol seulement). Ne pas toucher socle/registry/autres modules/moteur. Pas de dépendance.

## C10 — Module 4 « tension »

### Décision clé
- **Tuyau/artère sous pression** (`ArtereCoupe`, `fragile` selon la pression) : trop de pression permanente →
  paroi qui s'abîme. Curseur/état de pression piloté par les **leviers** activés (sel, activité, poids, alcool,
  antihypertenseur) : plus de leviers actifs → pression baisse → paroi apaisée (proto 748-756).
- **La « règle des 3 »** (3 mesures matin + 3 soir, 3 jours) montrée comme **geste**, pas chiffre. Encart dédié.
- **Fiche outil** (`FicheOverlay`) : « Ma règle des 3 » + un **relevé d'automesure vierge** (grille à remplir chez soi).
- **Renvois inline** : 4→8 (le sel), 4→11 (traitements de la tension).

### Modifier
- Écraser `src/features/cardio/tension/TensionModule.tsx` (stub) + créer `TensionModule.module.css`.
- **Ne pas toucher `registry.ts`.**

### Étapes
1. `ModuleShell` (rendersOwnShell). État local : leviers actifs (`Record<id,bool>`), dérive une « pression » 0-100.
2. `ArtereCoupe` à gauche (`fragile` si pression haute) + badge « paroi fragilisée / apaisée » (tokens).
3. Chips leviers (sel/activité/poids/alcool/antiHTA) — a11y boutons ≥44px, couleur **+ bordure**.
4. Encart « règle des 3 » (3× matin / 3× soir / 3 jours) + bouton fiche → `FicheOverlay` (règle + relevé vierge).
5. Renvois inline 4→8, 4→11.
6. Textes = `CONTENU_cardio.md` §M4 (afficher tel quel un `// à revalider`, ne rien inventer).

### Validation
- Auto : `npx tsc --noEmit` (ignorer erreurs hors `tension/` si autres modules en parallèle) · le gate complet
  est relancé par l'orchestrateur.
- Humain : la pression baisse visiblement quand on active des leviers ; règle des 3 lisible ; fiche imprimable → `VALIDATION.md`.

### Message de commit
`feat(cardio): module 4 « La tension » (artère sous pression + règle des 3 + fiche)`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
`/fin-de-tache` (vague parallèle). Pas de commit ; consigner statut ici.
