# theme-cardio · S12 — M10 « Reconnaître l'alerte » (carte VITE — objet NEUF + fiche)   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 4 (parallèle : oui — dossier disjoint `alerte/`)**
> Exécutant : UNIQUEMENT la tâche ci-dessous. Design fixé. Doute → STOP. Squelette = moule S4/S5.
> ⚠️ Module de **survie** : clarté sous stress > esthétique.

- Date : 2026-07-22 · Branche : —

## Lire (commun)
- `docs/cardio/CONTENU_cardio.md` — **§M10** (validé G1). ⚠️ Rappels G1 **impératifs** :
  **NE PAS mentionner l'aspirine** (ni écran ni survol) ; geste unique = **appeler le 15** ; **formes atypiques**
  (femmes/diabétiques/âgés) capitales ; **15 / 112** sont les seuls chiffres autorisés à l'écran.
- Prototype **§MODULE 10** (445-492 + logique 914-920) — onglets VITE / Infarctus, bandeau 15.
- Socle : `IllustrationSlot` (assets à générer `vite-*`, `infarctus-*` — placeholder natif si absents),
  `FicheOverlay`. `ModuleShell` + onglets `nav`.

## Hors périmètre
- **Aucune mention d'aspirine.** Pas d'auto-prise médicamenteuse. Se lit **sous stress** (gros pictos, peu de mots).
  Ne pas toucher socle/registry/autres modules.

## C16 — Module 10 « alerte »

### Décision clé
- **Objet NEUF : la carte-réflexe VITE** — panneau de sortie de secours (pas une infographie).
  - **Onglet VITE (AVC)** : **V**isage / **I**ncapacité d'un bras / **T**rouble de la parole / **E**n urgence → **15**.
    Grands pictos `IllustrationSlot vite-*`, un par lettre.
  - **Onglet Infarctus** : douleur qui serre > 5 min, irradiations ; **encadré formes atypiques** (femmes,
    diabétiques, âgés : dos/ventre, fatigue intense, nausées). `IllustrationSlot infarctus-*`.
- **Bandeau 15** permanent : « **Appelez le 15** (ou 112). Ne conduisez pas. N'attendez pas que ça passe. »
- **Fiche référence** (`FicheOverlay`) : carte VITE + signes infarctus + 15 (aimant frigo, universelle).

### Modifier
- Écraser `src/features/cardio/alerte/AlerteModule.tsx` + créer `.module.css`. Pas de `registry.ts`.

### Étapes
1. `ModuleShell` + onglets VITE/Infarctus (`nav`).
2. VITE : grille 4 lettres (gros pictos + libellé). Infarctus : signes + encadré atypiques (token ambre/attention).
3. Bandeau 15 (token toxique/alerte, contrasté, gros). Fiche référence via `FicheOverlay`.
4. Textes = §M10 (VERBATIM sur les signes ; **aucune aspirine**).

### Validation
- Auto : `npx tsc --noEmit`. Humain : lisible à ~1 m / sous stress, formes atypiques présentes, **aucune aspirine**,
  15 proéminent, fiche imprimable → `VALIDATION.md`.

### Si bloqué
Assets `vite-*`/`infarctus-*` non générés → placeholder `IllustrationSlot` (normal, non bloquant) ; ne pas
inventer d'illustration. Signale.

### Message de commit
`feat(cardio): module 10 « Reconnaître l'alerte » (carte VITE + infarctus + 15)`

### Statut
[x] fait · exécuté le : 2026-07-22 · commit : — (à renseigner en fin de plan)

## Fin de session
`/fin-de-tache` (vague parallèle). Pas de commit ; statut ici.

### Fichiers modifiés

- `src/features/cardio/alerte/AlerteModule.tsx` (écrasé — stub → module réel)
- `src/features/cardio/alerte/AlerteModule.module.css` (créé)

### Résumé

- `ModuleShell` + `nav` = 2 onglets (`role="tablist"`) : **AVC — VITE** / **Infarctus — signes**
  (même patron que `HypoglycemieModule` : `role="tab"`/`tabpanel`, flèches ←/→).
- Onglet VITE : grille 4 lettres (`viteGrid`, 1→2→4 colonnes selon largeur), chaque carte = lettre
  (badge) + `IllustrationSlot` (`vite-visage|vite-bras|vite-parole|vite-urgence`, 120px) + texte
  verbatim §M10 (« Visage qui tombe », « Incapacité à lever un bras », « Trouble de la parole »,
  « En urgence → 15 »).
- Onglet Infarctus : signe principal (« Une douleur qui serre la poitrine et ne passe pas. »,
  `IllustrationSlot infarctus-douleur`), sous-texte durée (« > 5 min », **non caché derrière un
  survol** — cf. point à revalider ci-dessous), encadré **formes atypiques** (`.callout` global =
  token vigilance/ambre, `IllustrationSlot infarctus-atypique`, texte « surtout femmes,
  diabétiques, personnes âgées » en gras).
- Bandeau **15** permanent (hors des deux `tabpanel`, donc visible quel que soit l'onglet) : fond
  `var(--color-toxique)` plein, texte blanc, « 15 » géant, « Appelez le 15 (ou 112). Ne conduisez
  pas. N'attendez pas que ça passe. ». Fil rouge : « Chaque minute compte. ».
- Fiche référence (`FicheOverlay`) : carte VITE (4 étapes `fiche-signaletique`), signes infarctus,
  bandeau 15 (`fiche-contact`/`fiche-contact-numero`, motif déjà utilisé pour Tabac Info Service).
- Couleurs 100 % tokens (`var(--color-toxique)`, `var(--color-vigilance)` via `.callout` global,
  `var(--color-text*)`) — hue du module = `toxique` (registry, inchangé). Cibles tactiles ≥44px
  (héritage global `button{min-height:var(--target-min)}`, boutons d'onglets et fiche compris).
- Assets `vite-*`/`infarctus-*` non générés : `IllustrationSlot` rend son placeholder natif
  (tuile crème + libellé centré) — état normal et attendu, rien à corriger.
- `registry.ts` non touché (déjà câblé sur `AlerteModule` depuis le stub S2) ; aucune dépendance
  ajoutée ; aucun autre module/socle modifié.

### Tests lancés

- `npx tsc --noEmit` → **0 erreur** (exit 0), sur l'ensemble du projet (pas seulement `alerte/`) —
  aucune erreur sœur à ignorer cette fois.

### Confirmation « aucune aspirine »

- Recherche « aspirine »/« aspirin » (insensible à la casse) sur `src/features/cardio/alerte/` :
  **une seule occurrence**, dans le commentaire JSDoc d'en-tête du fichier (rappel G1 pour la
  maintenance) — **aucune** dans un texte affiché, un `alt`, une fiche ou un survol. Geste unique
  enseigné à l'écran = « Appelez le 15 ».

### Points `// à revalider` (portés du doc, non tranchés ici — design volontairement conservateur)

1. **Formulation exacte des signes atypiques** (équilibre sensibilité/fausses alertes) = jugement
   clinique (Thibault) — texte actuel repris verbatim §M10 (« dos, ventre, grande fatigue, nausées
   — surtout femmes, diabétiques, personnes âgées »), non modifié.
2. **« > 5 min » : écran ou survol ?** §M10 le classe en 2ᵉ niveau (survol) mais s'interroge sur le
   remonter en 1ᵉʳ niveau. Choix fait ici : affiché en sous-texte secondaire, toujours visible (pas
   de survol) — une carte de survie ne se « survole » pas bien sous stress — mais visuellement en
   retrait du signe principal. À trancher par Thibault.

### À valider visuellement (humain — non vérifiable par Claude, pas de navigateur)

- [ ] Lisible à ~1 m / sous stress (gros pictos, peu de mots, contraste du bandeau 15).
- [ ] Formes atypiques bien présentes et visibles (pas une option qu'on peut manquer).
- [ ] Aucune aspirine, nulle part (écran, survol, fiche).
- [ ] « 15 » proéminent en permanence, quel que soit l'onglet actif.
- [ ] Fiche imprimable correcte (carte VITE + signes infarctus + 15).
- [ ] Placeholders `IllustrationSlot` (`vite-*`/`infarctus-*`) cohérents en l'absence d'assets.

### Prochaine action

- Génération des assets `vite-*`/`infarctus-*` (hors périmètre S12) puis validation visuelle
  humaine ci-dessus → `VALIDATION.md` (fait en fin de plan, fichier partagé non touché ici).
