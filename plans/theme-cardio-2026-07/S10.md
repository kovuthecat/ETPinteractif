# theme-cardio · S10 — M8 « Manger pour ses artères » (familles + assiette + fiche)   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 3 (parallèle : oui — dossier disjoint `manger/`)**
> Exécutant : UNIQUEMENT la tâche ci-dessous. Design fixé. Doute → STOP. Squelette = moule S4/S5.

- Date : 2026-07-22 · Branche : —

## Lire (commun)
- `docs/cardio/CONTENU_cardio.md` — **§M8** (validé G1). Rappels : **sel = « limiter le sel », aucun g/j** ;
  diversité culturelle **sans base de données** ; pas de moralisation ; jamais de chiffres imposés.
- Prototype **§MODULE 8** (284-375 + logique 815-888) — familles amis/à limiter, garde-manger, assiette conique.
- Socle : `IllustrationSlot` (assets `illustrations/cardio/aliment-*.png`), `FicheOverlay`. `ModuleShell` + onglets `nav`.

## Hors périmètre
- Pas de régime restrictif punitif, pas de moralisation. Aucun chiffre imposé. Ne pas toucher socle/registry/autres modules.

## C14 — Module 8 « manger »

### Décision clé
- **Onglet Familles** : « amis des artères » vs « à limiter », **détail au clic** d'un aliment (pastille de feu +
  texte court). (proto 292-326.)
- **Onglet Assiette** : **garde-manger** (aliments `aliment-*` cliquables, par catégorie) → **assiette conique**
  ½ légumes / ¼ féculents / ¼ protéines + analyse d'équilibre + repères sel/gras (lien M4 sel, M5 gras). (proto 328-373.)
- **Fiche photographie** (`FicheOverlay`) : l'assiette cardio du patient + repères.
- Renvois inline 8→4 (sel), 8→5 (gras/LDL).

### Modifier
- Écraser `src/features/cardio/manger/MangerModule.tsx` + créer `.module.css` (+ `data.ts` local si les listes
  d'aliments méritent d'être factorisées, comme diabète `alimentation/data.ts`). Pas de `registry.ts`.

### Étapes
1. `ModuleShell` + onglets Familles/Assiette (`nav`).
2. Familles : deux colonnes (amis / à limiter), détail au clic. Feu par aliment (tokens).
3. Assiette : garde-manger `IllustrationSlot aliment-*` → compteurs légumes/féculents/protéines → gradient conique
   (proto `plateGradient`) + analyse (proto `plateAnalysis`) + avertissement sel/gras (« limiter le sel », qualitatif).
4. Fiche photographie via `FicheOverlay`. Textes = §M8.

### Validation
- Auto : `npx tsc --noEmit`. Humain : ajout d'aliments → assiette évolue, détail au clic, fiche imprimable → `VALIDATION.md`.

### Message de commit
`feat(cardio): module 8 « Manger pour ses artères » (familles + assiette + fiche)`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
`/fin-de-tache` (vague parallèle). Pas de commit ; statut ici.
