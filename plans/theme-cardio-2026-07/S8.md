# theme-cardio · S8 — M5 « Cholestérol (LDL) » + M6 « Tabac »   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 3 (parallèle : oui — dossiers disjoints `cholesterol/`, `tabac/`)**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre. Design fixé. Doute → STOP. Squelette = moule S4/S5.

- Date : 2026-07-22 · Branche : —

## Lire (commun)

- `docs/cardio/CONTENU_cardio.md` — **§M5** et **§M6** (validés G1). Rappels : **M5 = jamais de LDL en g/L**
  (feu/curseur qualitatif) ; **M6 = repli porte tabac** (mention visuelle non-navigante en v1).
- Prototype **§MODULE 5** (165-179, logique 762-763) et **§MODULE 6** (182-198, logique 765-769).
- Socle : `ArtereCoupe` (contrat habituel), lib `risqueCardio` (`plaqueGeom`/`plaquePassagePct`).

## Hors périmètre
- M5 : **ne pas moraliser l'assiette** (hépatique/génétique) ; **aucune cible LDL à l'écran**.
- M6 : rester **court** (le « comment arrêter » vit dans le thème Tabac) ; message = arrêt, pas de seuil « sûr ».
- Ne pas toucher socle/registry/autres modules. Pas de dépendance. Pas de porte inter-thèmes réelle (repli seul).

---

## C11 — Module 5 « cholesterol »

### Décision clé
- **Curseur « niveau de LDL »** (0-100, **qualitatif, aucun g/L**) → `ArtereCoupe encrassement={ldl/100}` :
  plus bas → moins de dépôt ; message « plus bas **plus longtemps** = la plaque recule ». Distinction LDL/HDL
  (« l'un dépose, l'autre nettoie »), simple. Message anti-culpabilisant « ce n'est pas que l'assiette ».
- **Fiche : aucune.** Renvois inline 5→8 (alimentation), 5→11 (statines).

### Modifier
- Écraser `src/features/cardio/cholesterol/CholesterolModule.tsx` + créer `.module.css`. Pas de `registry.ts`.

### Étapes
1. `ModuleShell`, état `ldl` (`useState`), `<input type=range>` (label « LDL bas / élevé », pas de chiffre).
2. `ArtereCoupe encrassement={ldl/100}`. Textes = §M5. Renvois 5→8, 5→11.

### Validation
- Auto : `npx tsc --noEmit` (idem note parallèle). Humain : le dépôt suit le curseur, aucun g/L → `VALIDATION.md`.

### Message de commit
`feat(cardio): module 5 « Le cholestérol (LDL) » (curseur qualitatif + artère)`

### Statut
[ ] à faire · exécuté le : — · commit : —

---

## C12 — Module 6 « tabac » *(léger, pont vers le thème Tabac)*

### Décision clé
- Bascule **Fumeur / A arrêté** → barre de risque CV (redescend à l'arrêt) + **frise de réversibilité**
  qualitative (pente qui redescend, aucun pourcentage). Message « le tabac accélère + favorise le caillot »,
  « dès l'arrêt, le cœur se répare ».
- **Repli porte tabac (G-porte)** : mention « → Pour arrêter, un accompagnement complet existe (thème Tabac) »,
  **informative non-navigante en v1** (`// porte inter-thèmes : repli visuel, nav réelle hors v1`).
- **Fiche : aucune.**

### Modifier
- Écraser `src/features/cardio/tabac/TabacModule.tsx` + créer `.module.css`. Pas de `registry.ts`.

### Étapes
1. `ModuleShell`, état `arrete: boolean`, bascule ; barre de risque (largeur/couleur en tokens).
2. Frise de réversibilité qualitative (proto 189-195) ; mention repli thème Tabac. Textes = §M6.

### Validation
- Auto : `npx tsc --noEmit`. Humain : la bascule fait redescendre le risque ; repli visible → `VALIDATION.md`.

### Message de commit
`feat(cardio): module 6 « Le tabac » (réversibilité CV + repli pont thème Tabac)`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
`/fin-de-tache` (vague parallèle). Pas de commit ; statut ici.
