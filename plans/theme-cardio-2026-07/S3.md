# theme-cardio · S3 — Assets cardio (copie réutilisés + prompts nouveaux)   (rédigé par Opus)

> **Modèle : Haiku · effort : low · Vague : 1 (parallèle : oui — avec S2, zones disjointes)**
> Exécutant : UNIQUEMENT les tâches ci-dessous ; fichiers sous « Lire » / « Modifier ».
> Mécanique. Doute → STOP, signale.

- Date : 2026-07-22 · Branche : —

## Lire (commun à la session)

- `plans/theme-cardio-2026-07/index.md` — §D3 (assets).
- Le zip décompressé du handoff (déjà extrait pour lecture) :
  `…/cardio-handoff/nouveau-module-etp-interactif/project/` — contient `artere-saine.png`, `activite-*.png`,
  `aliment-*.png` **et** `public/illustrations/diabete/silhouette.png`.
- `public/illustrations/diabete/` — source de secours des PNG réutilisés (`artere-saine.png`, `silhouette.png`,
  `activite-*.png`, `aliment-*.png`) déjà présents dans le repo.
- `design/illustrations/` — dossier des prompts + pipeline (précédent tabac/diabète : script Pillow, palette
  adaptative 256 couleurs, ré-encodage anti-C2PA/Inkscape).

## Hors périmètre

- Ne toucher **aucun** fichier `src/**` (zone de S2). Ne pas modifier les PNG diabète existants.
- Ne pas générer d'image « à la main » : les nouveaux assets sont **décrits en prompts**, pas produits ici
  (génération = hors chaîne, Thibault). Les modules gèrent l'absence (placeholder `IllustrationSlot`).

---

## C5 — Copier les PNG réutilisés vers `public/illustrations/cardio/`

### Objectif

Isolation par thème : cardio référence `illustrations/cardio/…`, jamais `…/diabete/…`. On y copie les assets
partagés dont le prototype se sert.

### Décision clé (D3)

- Source = `public/illustrations/diabete/` (déjà dans le repo, PNG propres). Copier :
  `artere-saine.png`, `silhouette.png`, tous les `activite-*.png` (13), tous les `aliment-*.png` réellement
  utilisés par M8 (liste `FOODS`, proto lignes 843-870 : baguette, pain-complet, pain-pita, riz-blanc,
  riz-basmati, riz-complet, semoule-couscous, pomme-de-terre, patate-douce, lentilles, pois-chiches,
  pates-completes, haricots-rouges, brocoli, carotte, pomme, banane, pasteque, yaourt, poulet, oeuf, sardine,
  saumon, avocat, huile-olive, noix).
- Copie **binaire à l'identique** (pas de ré-encodage — ce sont des PNG déjà propres).

### Lire / Modifier

- Créer : `public/illustrations/cardio/` + les PNG copiés.

### Étapes

1. Créer le dossier `public/illustrations/cardio/`.
2. Copier les fichiers listés depuis `public/illustrations/diabete/`.
3. Vérifier la présence de `artere-saine.png` et `silhouette.png` (requis par `ArtereCoupe`/`Silhouette` de S2).

### Validation

- Auto (bloque le commit) : lister `public/illustrations/cardio/` → au moins `artere-saine.png`,
  `silhouette.png`, les 13 `activite-*`, les `aliment-*` de la liste. `npm run build` OK (assets statiques).

### Si bloqué
Un PNG de la liste absent de `diabete/` → le récupérer depuis le zip handoff ; si absent des deux → le noter
`// asset manquant` (M8/M7 tolèrent via placeholder), signale.

### Message de commit
`chore(cardio): assets réutilisés copiés dans public/illustrations/cardio/`

### Statut
[ ] à faire · exécuté le : — · commit : —

---

## C6 — Prompts des nouveaux assets cardio

### Objectif

Décrire (prompts, style cohérent avec le lot existant) les assets **spécifiques cardio** que le prototype
marque « à générer », pour génération ultérieure par Thibault.

### Décision clé

- Assets neufs (proto) : **pictos VITE** ×4 (Visage / Incapacité bras / Trouble parole / En urgence→15,
  style **signalétique**, un par lettre — proto ligne 467) ; **signes infarctus** ×3 (proto ligne 483) ;
  **artère/poumon tabac** 2 états (fumeur : artère+caillot+cigarette / arrêté : artère qui se répare —
  proto lignes 769) ; **brassard d'automesure** (M4, proto ligne 150).
- Style aligné sur le lot cardio/diabète (fond crème/kraft, sobre, adulte, **pas de smiley/infographie
  souriante** — brief §1.9). Ids proposés : `vite-visage/bras/parole/urgence`, `infarctus-douleur/irradiation/
  sueurs`, `tabac-artere-fumeur/tabac-artere-arret`, `automesure-brassard`.

### Lire / Modifier

- Modifier : le fichier de prompts d'illustrations existant sous `design/illustrations/` (ajouter une section
  « Cardio — assets neufs »). S'il n'existe pas de fichier générique, créer `design/illustrations/prompts-illustrations-cardio.html`
  ou `.md` sur le modèle du fichier diabète/tabac.

### Étapes

1. Repérer le fichier de prompts existant (précédent : `prompts-illustrations-diabete.html` incluait tabac).
2. Ajouter une section cardio avec un prompt par id ci-dessus (style + cadrage carré 1:1 pour pictos).
3. Noter la convention d'ids dans ce `S3.md` (pour que M4/M6/M10 les référencent).

### Validation

- Auto : — (doc). Humain : Thibault génère puis dépose dans `public/illustrations/cardio/` (hors plan).

### Si bloqué
Pas de fichier de prompts repérable → en créer un neuf sur le modèle, signale l'emplacement choisi.

### Message de commit
`docs(cardio): prompts des illustrations neuves (VITE, infarctus, tabac, brassard)`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session

Dérouler `/fin-de-tache` (mode vague parallèle). Ne pas committer ; consigner statut ici.
