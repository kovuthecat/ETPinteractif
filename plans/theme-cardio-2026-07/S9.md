# theme-cardio · S9 — M7 « Bouger » (2 onglets : volume + régularité)   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 3 (parallèle : oui — dossier disjoint `bouger/`)**
> Exécutant : UNIQUEMENT la tâche ci-dessous. Design fixé. Doute → STOP. Squelette = moule S4/S5.

- Date : 2026-07-22 · Branche : —

## Lire (commun)
- `docs/cardio/CONTENU_cardio.md` — **§M7** (validé G1). Rappels : **jauge SANS plafond**, repère « 150 min »
  discret, **aucun chiffre d'étude à l'écran**, « bouger un peu >> rien ».
- Prototype **§MODULE 7** (201-281 + logique 771-813) — grille d'activités, jauge, calendrier semaine, barres.
- Socle : `IllustrationSlot` `{ id; label; shape?; size? }` (assets `illustrations/cardio/activite-*.png`).
  `ModuleShell` avec **barre d'onglets dans le slot `nav`** (D7).

## Hors périmètre
- Jauge **sans plafond**, pas de « objectif atteint/échoué ». Aucun chiffre d'étude à l'écran (survol seulement).
- Ne pas toucher socle/registry/autres modules. Pas de dépendance.

## C13 — Module 7 « bouger »

### Décision clé
- **Onglet Volume** : ① « rayonnement » (un effort → plusieurs bénéfices : tension/cholestérol/sucre/stress/cœur)
  puis ② **grille d'activités** (marche, vélo, ménage, jardinage, escaliers, porter les courses…) via
  `IllustrationSlot activite-*`, +/− minutes, **jauge ouverte sans maximum** (proto 208-254). Marqueur discret
  « bon pour les muscles » sur certaines.
- **Onglet Régularité** : calendrier semaine cliquable + barres de protection (l'effet s'estompe en 48-72 h) (proto 256-280).
- **Fiche : aucune.** Renvois 7↔2, 7←9 (stress) inline.

### Modifier
- Écraser `src/features/cardio/bouger/BougerModule.tsx` + créer `.module.css`. Pas de `registry.ts`.

### Étapes
1. `ModuleShell` + onglets (`nav`) Volume/Régularité (état `useState`).
2. Volume : liste d'activités (données locales façon proto `ACTIVITIES`), cases + minutes +/−, jauge `activiteGaugePct` sans plafond.
3. Régularité : `weekActive` 7 jours cliquables + barres de protection (proto 802-804).
4. Textes/repères = §M7. a11y (boutons ≥44px, onglets `aria-current`).

### Validation
- Auto : `npx tsc --noEmit`. Humain : jauge sans plafond, illustrations d'activités, calendrier → `VALIDATION.md`.

### Message de commit
`feat(cardio): module 7 « Bouger » (volume sans plafond + régularité)`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
`/fin-de-tache` (vague parallèle). Pas de commit ; statut ici.
