# theme-cardio · S6 — Pilote M3 « Où l'accident frappe » (silhouette + territoires)   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 2 — PILOTE (parallèle : oui — dossier disjoint `territoires/`, gate G-moule)**
> Exécutant : UNIQUEMENT la tâche ci-dessous ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.
> Squelette = **moule S4**.

- Date : 2026-07-22 · Branche : —

## Lire (commun à la session)

- `docs/cardio/CONTENU_cardio.md` — **§M3 uniquement** (texte sobre par territoire, message d'**unité** « un
  seul ennemi, plusieurs adresses », piège : **menace seule proscrite** — « voilà ce qu'on protège »).
- Prototype `…/ETP Cardio - Prototype.dc.html` — **§MODULE 3** (lignes 101-122) + logique (737-746, 981).
- Composant socle (S2) — **contrat exact** :
  - `Silhouette` : `{ zones: SilhouetteZoneState[]; onZoneClick?:(id:ZoneId)=>void; children? }`,
    `ZoneId = 'coeur'|'cerveau'|'jambes'|'reins'`, exporte `SILHOUETTE_ANCHORS`/`SILHOUETTE_VIEWBOX`.
    États de zone (`SilhouetteEtat` du moteur générique) : `actif`/`ouvert`/`verrouille`/`allume`/`masque`.
- `src/components/ModuleShell.tsx` — contrat.

## Hors périmètre

- Ne pas modifier le socle (S2), `registry.ts`, un autre module, le moteur. Pas de dépendance.
- **Menace seule = proscrite.** Message d'**unité** (un ennemi, plusieurs adresses), pas un catalogue
  anxiogène. Pas d'images cliniques crues. Choix **libre** (pas de défilé imposé).

---

## C9 — Module 3 « territoires » (silhouette + 4 zones)

### Objectif

Un seul ennemi (la plaque), plusieurs adresses : cœur/cerveau/jambes/reins. Faire comprendre les territoires
**sans faire peur pour rien**. Contemplatif, on n'ouvre que ce qu'on veut.

### Décision clé

- **Silhouette** (wrapper socle) avec 4 zones cliquables. État local `zoneOuverte: ZoneId | null` (`useState`).
  `zones` = les 4 territoires, l'ouvert en état `ouvert`/`allume`, les autres `actif` ; `onZoneClick(id)` =
  toggle `zoneOuverte`.
- **Panneau latéral** : au clic d'une zone → texte sobre du territoire (de `CONTENU_cardio.md` §M3 :
  cœur→infarctus, cerveau→AVC, jambes→artériopathie, reins→insuffisance rénale silencieuse). État par défaut
  (aucune zone) = invitation neutre.
- **Renvoi inline vers M10** pour **cœur/cerveau** uniquement : « → Reconnaître l'alerte »
  (`onNavigate('alerte')`, proto ligne 116).
- Message d'**unité** en pied (proto ligne 120), texte du CONTENU.
- **Fiche : aucune.**

### Lire / Modifier

- Modifier (écraser) : `src/features/cardio/territoires/TerritoiresModule.tsx` (stub S2, chemin final) + créer
  `src/features/cardio/territoires/TerritoiresModule.module.css`.
- **Ne PAS toucher `registry.ts`** (import déjà en place ; garder le même nom d'export par défaut).

### Étapes

1. `if (!shell) return null;` → `<ModuleShell titre onBack sources wide>`.
2. État `zoneOuverte` ; construire `zones` (états selon l'ouverture) ; `<Silhouette zones onZoneClick />`.
3. Panneau latéral avec le texte du territoire ouvert (ou invitation neutre).
4. Renvoi M10 conditionnel (cœur/cerveau).
5. Pied : message d'unité.
6. a11y : zones = boutons ≥44px (le moteur `SilhouetteCorps` le fait déjà) ; le renvoi = vrai bouton/lien.

### Validation

- Auto (bloque le commit) : `npx tsc --noEmit` OK · `npm run build` OK · `npm test` (127/127, inchangé).
- Humain (**gate G-moule**) : à l'écran — silhouette lisible, plaque « arrive » sur la zone ouverte, ton
  **« ce qu'on protège »** (pas menaçant), renvoi alerte sur cœur/cerveau → `VALIDATION.md`.

### Si bloqué
- Ancrage des zones imprécis → ajuster **les ancres du wrapper `Silhouette`** (S2) seulement si nécessaire,
  marquer `// à revalider` ; ne pas retoucher le moteur `SilhouetteCorps`.
- Texte de territoire absent/`// à revalider` dans CONTENU → afficher tel quel avec le flag, ne pas inventer.

### Message de commit
`feat(cardio): module 3 « Où l'accident frappe » (silhouette + territoires)`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
Dérouler `/fin-de-tache` (mode vague parallèle). Pas de commit ; consigner statut + checklist visuelle ici.
