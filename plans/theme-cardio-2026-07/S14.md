# theme-cardio · S14 — M12 « Mon suivi » (« mes 3 chiffres » + fiche)   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 4 (parallèle : oui — dossier disjoint `suivi/`)**
> Exécutant : UNIQUEMENT la tâche ci-dessous. Design fixé. Doute → STOP. Squelette = moule S4/S5.

- Date : 2026-07-22 · Branche : —

## Lire (commun)
- `docs/cardio/CONTENU_cardio.md` — **§M12** (validé G1). Rappels : **« mes 3 chiffres »** (grille légère, G-M12
  tranché — **pas** le cadran annuel) ; **JAMAIS de rouge** (« pas encore fait » = ambre + horloge) ; fréquences
  de suivi **`// à revalider (Thibault — à confirmer HAS)`**, non bloquant.
- Prototype **§MODULE 12** (544-558 + logique 961-975) — grille de voyants cyclables.
- Socle : `FicheOverlay`, `ModuleShell`.

## Hors périmètre
- **Jamais de rouge** ici (le rouge-santé reste aux modules 2/4/5). Jamais « on est en novembre et vous n'avez
  rien fait ». Ne pas toucher socle/registry/autres modules.

## C18 — Module 12 « suivi »

### Décision clé
- **« Mes 3 chiffres à suivre »** en tête : tension · LDL · tour de taille.
- **Grille de voyants** (stations : automesure tension · bilan lipidique · réévaluation du risque · glycémie ·
  fonction rénale) **cyclables** : `fait` (vert coché) · `à programmer / à rattraper` (**ambre + horloge**) ·
  `espacé` (grisé présent, « prochain : … »). Cadences = §M12 (qualitatives ; fréquences `// à revalider HAS`).
- Message « couverture, pas bilan accablant ». **Fiche photographie/outil** (`FicheOverlay`) : check-list frigo
  (une case par examen + espace pour la vraie date).

### Modifier
- Écraser `src/features/cardio/suivi/SuiviModule.tsx` + créer `.module.css`. Pas de `registry.ts`.

### Étapes
1. `ModuleShell`. État : `Record<stationId, 'attente'|'fait'|'espace'>` ; clic = cycle (proto `cycleSuivi`).
2. Bandeau « mes 3 chiffres » + grille de voyants (tokens : vert/ambre/gris — **jamais rouge**).
3. Fiche frigo via `FicheOverlay`. Textes/cadences = §M12 (marquer les fréquences `// à revalider (Thibault — HAS)`).

### Validation
- Auto : `npx tsc --noEmit`. Humain : cycles d'état sans rouge, « mes 3 chiffres » en tête, fiche imprimable → `VALIDATION.md`.

### Message de commit
`feat(cardio): module 12 « Mon suivi » (mes 3 chiffres + grille de voyants + fiche)`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
`/fin-de-tache` (vague parallèle). Pas de commit ; statut ici.
