# theme-cardio · S13 — M11 « Mes traitements qui protègent » (ordonnance ↔ silhouette)   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 4 (parallèle : oui — dossier disjoint `traitements/`)**
> Exécutant : UNIQUEMENT la tâche ci-dessous. Design fixé. Doute → STOP. Squelette = moule S4/S5.

- Date : 2026-07-22 · Branche : —

## Lire (commun)
- `docs/cardio/CONTENU_cardio.md` — **§M11** (validé G1). ⚠️ Rappels G1 : **aspirine NON mentionnée** (cohérence
  M10) ; **verrou anti-auto-prescription** (on transcrit/explique, on ne compare ni ne choisit) ; corps **pur**
  (halo positif, aucune alerte). En primaire : **tension + cholestérol**.
- Prototype **§MODULE 11** (495-541 + logique 922-959) — ordonnance ligne par ligne, clic → zones protégées.
- Socle : `Silhouette` `{ zones: SilhouetteZoneState[]; onZoneClick?; children? }`, `ZoneId='coeur'|'cerveau'|
  'jambes'|'reins'`, état `'allume'` = zone protégée (halo). **Taille alignée sur l'anatomie Risque CV (560px)**
  — reprendre le dimensionnement de `territoires/TerritoiresModule.module.css` (`.silhouetteCol` 560px, layout ≥1020px).

## Hors périmètre
- **Ne compare ni ne choisit aucune molécule.** Corps pur (aucune alerte dessus). **Aucune aspirine.**
  Ne pas toucher socle/registry/autres modules.

## C17 — Module 11 « traitements »

### Décision clé
- **Gauche — l'ordonnance** transcrite ligne par ligne : ajout de ligne, champ molécule (étiquette libre),
  **sélecteur de classe/rôle** (durable). Classes primaires : **antihypertenseurs (IEC/ARA2, ICa, thiazidiques)**
  et **statines / ézétimibe**. **Retirer l'aspirine / antiagrégant** de la liste (décision G1).
- **Droite — `Silhouette`** (lecture « défendu ») : **clic sur une ligne → zones protégées s'allument** (état
  `'allume'`, halo positif). Antihypertenseurs → cœur/cerveau/reins ; statines → plaque stabilisée partout.
- L'action est attachée à la **classe/rôle**, la molécule = étiquette fine. **Pas de fiche.** Renvois 11↔4, 11↔5, 11↔3.

### Modifier
- Écraser `src/features/cardio/traitements/TraitementsModule.tsx` + créer `.module.css` (+ `data.ts` local pour
  la table classe→zones→message, comme diabète `traitements/data.ts`, **sans aspirine**). Pas de `registry.ts`.

### Étapes
1. `ModuleShell`. État : `lignes[]` (molécule + classId), sélection courante.
2. Colonne ordonnance : add/remove ligne, input molécule, `<select>` classe (table sans aspirine).
3. Colonne `Silhouette` (560px) : `zones` allumées selon la ligne/ordonnance sélectionnée (halo).
4. Textes = §M11 (« gardent vos artères », messages par classe). Verrou : aucune comparaison de molécules.

### Validation
- Auto : `npx tsc --noEmit`. Humain : transcrire une ligne → zones s'allument, halo positif, **aucune aspirine**,
  silhouette à la bonne taille (560px) → `VALIDATION.md`.

### Message de commit
`feat(cardio): module 11 « Mes traitements qui protègent » (ordonnance ↔ silhouette défendue)`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
`/fin-de-tache` (vague parallèle). Pas de commit ; statut ici.
