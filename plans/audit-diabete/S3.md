# S3 — Alimentation : courbe dans la colonne + allègements   (rédigé par Opus)

> **Modèle : Sonnet · effort : xhigh · Vague : 1 (parallèle : oui — zone `alimentation/` uniquement)**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-07-12 · Branche : —

## Lire (commun à la session)

- `src/features/diabete/alimentation/AlimentationModule.tsx` (en entier — 1214 l. ; repères de
  structure ci-dessous)
- `src/features/diabete/alimentation/AlimentationModule.module.css` (classes citées par tâche)
- `Audit/audit-etp-interactif.md` §4, §5, §6-7

> **Carte de structure** (rendu par court-circuit `{defi === N && …}`, état `const [defi] = useState`) :
> `<div .layout>` (l.712) contient `<aside .shelf>` (garde-manger, l.713-735) + `<div .stage>` (colonne
> de contenu du défi, l.737-1023). **La carte glycémie (`CourbeSection`) est rendue HORS de `.stage`**,
> en frère de `.layout`, 5 fois (une par défi) : défi 1 → l.1029-1061, défi 2 (si `d2Revealed`) →
> l.1062-1087, défi 3 → l.1088-1108, défi 4 → l.1109-1141, défi 5 (si assiette non vide) → l.1142-1162.
> Le composant `CourbeSection` est défini l.302-319.

## Hors périmètre

- Ne pas modifier `glycemieCurve.ts` (S5 s'en occupe) ni `CourbeGlycemie` / son `viewBox`.
- Ne pas toucher au garde-manger (`.shelf`, l.713-735) ni à la logique des défis (calculs d'assiette,
  `paramsFromAssiette`, `d2Guess`, etc.).
- Ne pas modifier la `FicheOverlay` (l.1164-1210).

---

## T4 — Structurel : sortir la courbe de dessous `.layout` et la remonter dans `.stage`

### Objectif

Corriger **en une fois** les points 4/5/6/7 de l'audit : la carte « Glycémie après le repas » (résultat
clé) doit vivre **dans la colonne de contenu de chaque défi** (`.stage`), agrandie, au lieu d'un bloc
séparé pleine largeur sous `.layout` (invisible sans scroll). (Audit §6-7, recommandation transversale.)

### Décision clé

Chaque défi a **deux** blocs disjoints dans l'arbre : son contenu dans `.stage` (l.737-1023) et sa carte
glycémie sous `.layout` (l.1029-1162). Il faut **déplacer chaque `<CourbeSection … />`** de sa position
actuelle (frère de `.layout`) **à l'intérieur du bloc `{defi === N && …}` correspondant de `.stage`**,
après le contenu propre au défi. La coquille `.courbeCard` est centrée sur 900 px (`max-width:900px;
margin:0 auto`, CSS l.650-658) → à revoir pour qu'elle occupe la largeur de `.stage`.

### Lire / Modifier

- Modifier : `AlimentationModule.tsx` (déplacer les 5 invocations), `AlimentationModule.module.css`
  (`.courbeCard` l.650-658, `.stage`, `.layout`, `.d1Layout` l.201-209)

### Étapes

1. Pour **chaque** défi N (1,2,3,4,5) : couper le bloc `{defi === N && <CourbeSection … />}` de sa
   position sous `.layout` (l.1029-1162) et le **coller à la fin du bloc `{defi === N && (…)}` de
   `.stage`** (juste avant la fermeture de la branche, à l'intérieur de `.stage`). Conserver à
   l'identique les props passées (`courbes`, `onNavigateActivite`, `animerTrace`) et les conditions
   (`defi === 2 && d2Revealed`, `defi === 5 && synthPlate.length > 0`).
2. Après déplacement, **plus aucune** `CourbeSection` ne doit rester en frère de `.layout` (l.1029-1162
   devient vide → supprimer la zone).
3. **CSS `.courbeCard`** (l.650-658) : retirer `max-width: 900px; margin: 0 auto;` (le centrage pleine
   largeur n'a plus de sens dans `.stage`) → `width: 100%`. Augmenter la hauteur perçue de la courbe via
   le conteneur (padding/gap), **sans** toucher au `viewBox` de `CourbeGlycemie`.
4. **`.stage`** doit pouvoir accueillir contenu + courbe : vérifier qu'elle est bien en `flex-direction:
   column` avec un `gap` correct ; la courbe se place **sous** le contenu interactif du défi.
5. Cas Composition (défi 1) : la courbe se retrouve sous `.d1Layout` (assiette + colonne `side`) →
   exactement le « sous l'assiette » demandé par l'audit §4.4.

### Validation

- Auto (bloque le commit) : `npx tsc --noEmit` → 0 erreur · `npm run build` → OK · `npm test` → vert.
- Humain (VALIDATION.md) : sur les 4 défis (Composition, Qualité, Ordre, Repas complet) la courbe
  « Glycémie après le repas » est **dans la colonne du défi, agrandie, visible sans scroll** ; l'enchaînement
  « je manipule → je vois le résultat juste dessous » est préservé.

### Si bloqué

Si un défi devient trop haut une fois la courbe remontée (scroll) → consigner la hauteur, laisser la
courbe en place, signaler pour arbitrage S6 (zéro scroll). Ne pas rétrécir le `viewBox`.

### Message de commit

`refactor(diabete/alim): remonte la courbe glycémie dans la colonne de chaque défi`

### Statut

[x] fait · exécuté le : 2026-07-12 · commit : —

---

## T5 — Composition : retrait du compteur, reset en icône, assiette agrandie

### Objectif

Audit §4 : supprimer le compteur « Dans l'assiette : … », passer « Recommencer » en icône seule,
agrandir l'assiette.

### Lire / Modifier

- Modifier : `AlimentationModule.tsx` (bloc défi 1, l.750-784), `AlimentationModule.module.css`
  (`.d1Plate` l.211-230, `.d1Layout` l.201-209, `.side` l.174-178, `.linkReset` l.187-197,
  `.d1Chip` l.240-252)

### Étapes

1. **Supprimer** le compteur `<p className={styles.sideLabel}>Dans l'assiette : …</p>` (l.774-777).
   Retirer la classe `.sideLabel` (l.180-185) si plus référencée.
2. **Reset en icône** : le `<button className={styles.linkReset}>Recommencer</button>` (l.778-780)
   devient une **icône seule** `RotateCcw` (lucide) avec `aria-label="Recommencer"` et un `title`
   (tooltip). Ajouter `RotateCcw` à l'import lucide en tête de fichier. Style : bouton icône rond,
   cible ≥ 44 px (nouvelle classe locale `.resetIcon` ou adaptation de `.linkReset`).
   > `.linkReset` est **aussi** utilisé par la synthèse (défi 5, l.1009-1011). Harmoniser : convertir
   > **les deux** « Recommencer » `linkReset` en icône (même composant/classe), pour un rendu cohérent.
3. **Agrandir l'assiette** : `.d1Plate` (l.211-230) passe de `320×320` à ~`400×400` (à caler). La colonne
   `.side` (l.774) n'ayant plus que le bouton icône, `.d1Layout` (l.201-209) peut resserrer la place de
   `.side` au profit de l'assiette. Réajuster `.d1Chip` (l.240-252, actuellement 56×56) si les jetons
   doivent grandir avec l'assiette (rester lisibles, ≤ 10 aliments).
4. Vérifier l'interaction drag&drop (`onDragOver`, `onDrop`, l.753) inchangée.

### Validation

- Auto : `npx tsc --noEmit` → 0 erreur (aucune variable/classe morte) · `npm run build` · `npm test` vert.
- Humain (VALIDATION.md) : Composition = assiette plus grande + icône de reset (sans texte) + **pas** de
  compteur « Dans l'assiette : … » ; la courbe (T4) est juste dessous.

### Si bloqué

Assiette 400 px + courbe dessous ne tiennent pas dans le cadre → garder 360-380 px, consigner, laisser
S6 arbitrer. STOP si le drag&drop se casse.

### Message de commit

`feat(diabete/alim): Composition — assiette agrandie, reset en icône, sans compteur`

### Statut

[x] fait · exécuté le : 2026-07-12 · commit : —

---

## T6 — Qualité : retrait des duels + verdicts textuels, reset en icône

### Objectif

Audit §5 : supprimer la rangée de duels suggérés, les textes « Vous aviez dit : … » et « Bonne
prédiction », passer le reset en icône. (La remontée/agrandissement de la courbe est faite en T4.)

### Décision clé

Bloc défi 2 (l.786-884). À retirer : `.d2Duels` (l.788-803, 4 boutons `.familyTab`), `.verdictOff`
(l.860-863, « Vous aviez dit : {niveau} »), `.verdictOk` (l.856-858, « Bonne prédiction »). À **garder** :
le badge résultat `.verdictBadge` (l.849-854, ex. « Pic haut »), les 2 cartes de comparaison
(`.d2Card`), le bouton « Révéler les courbes » (l.872-876).

### Lire / Modifier

- Modifier : `AlimentationModule.tsx` (bloc défi 2, l.786-884), `AlimentationModule.module.css`
  (`.d2Duels` l.264-269, `.verdictOk/.verdictOff` l.373-389, `.d2Actions` l.391-395)

### Étapes

1. **Supprimer** la rangée `<div className={styles.d2Duels}>…</div>` (l.788-803). Les cartes de
   comparaison remontent d'autant. Retirer la constante `D2_DUELS` (l.75-80) et le handler
   `handleD2Duel` (l.401-406) s'ils ne servent plus qu'à ce bloc (vérifier par grep). Retirer la classe
   `.d2Duels` si morte.
2. **Supprimer** les deux verdicts textuels dans `.d2Verdict` (l.855-864) : garder **uniquement** le
   badge `.verdictBadge`. Retirer les classes `.verdictOk`/`.verdictOff` (l.373-389) et les imports
   d'icônes `Check`/`X` **si** plus utilisés ailleurs dans le fichier (vérifier par grep — `Check`/`X`
   peuvent servir à d'autres défis).
3. **Reset en icône** : le `<button className="btn btn--ghost" onClick={handleD2Reset}>Recommencer</button>`
   (l.878-880, dans `.d2Actions`) devient icône `RotateCcw` + `aria-label="Recommencer"` (même
   traitement qu'en T5 pour la cohérence). Le bouton « Révéler les courbes » (l.872-876) reste tel quel.
4. Vérifier que `d2Guess`/`d2Revealed`/`d2CanReveal` restent cohérents sans les duels (la sélection de
   duel ne doit plus être un préalable au reveal — vérifier `d2CanReveal`).

### Validation

- Auto : `npx tsc --noEmit` → 0 erreur (aucun import/constante mort) · `npm run build` · `npm test` vert.
- Humain (VALIDATION.md) : Qualité = 2 cartes de comparaison remontées (plus de rangée de duels),
  chaque carte garde son badge « Pic … » mais **plus** de « Vous aviez dit » ni « Bonne prédiction » ;
  reset en icône ; la courbe (T4) grande et visible sans scroll.

### Si bloqué

Si retirer `D2_DUELS`/`handleD2Duel` casse une autre partie (préremplissage des slots A/B) → conserver
la logique de slots, ne retirer que le **rendu** de la rangée. STOP si un test rougit.

### Message de commit

`feat(diabete/alim): Qualité — retrait duels/verdicts textuels, reset en icône`

### Statut

[x] fait · exécuté le : 2026-07-12 · commit : —

---

## T7 — Ordre & Repas complet : vérifier la remontée de la courbe + espacement

### Objectif

Audit §6-7 : ces deux défis n'avaient que le problème structurel de la courbe (réglé en T4). Vérifier le
rendu et resserrer l'espace vide au-dessus si besoin.

### Lire / Modifier

- Modifier : `AlimentationModule.tsx` (blocs défi 3 l.886-927, défi 4 l.929-972 dans `.stage`),
  `AlimentationModule.module.css` (`.d3Row` et classes de ces défis, si resserrement nécessaire)

### Étapes

1. Confirmer que la courbe (T4) apparaît bien dans `.stage` sous le contenu des défis 3 (Ordre,
   `.d3Row` — 3 cartes « bouchées ») et 4 (Repas complet).
2. L'audit note de l'espace disponible sous les bouchées (contenu ~272 px) : resserrer le `gap`/padding
   de `.stage` pour ces défis afin que la courbe remonte visuellement (pas d'espace mort). À caler.
3. Aucune suppression de contenu ici (ni compteur, ni verdict) — purement structurel/espacement.

### Validation

- Auto : `npx tsc --noEmit` · `npm run build` · `npm test` vert.
- Humain (VALIDATION.md) : Ordre et Repas complet montrent la courbe agrandie, remontée, sans espace
  mort au-dessus ; pas de scroll ajouté.

### Si bloqué

Rien de spécifique — si la courbe déborde, même consigne que T4 (consigner, laisser S6 arbitrer).

### Message de commit

`refactor(diabete/alim): Ordre/Repas — resserre l'espace au-dessus de la courbe`

### Statut

[x] fait · exécuté le : 2026-07-12 · commit : —

---

## Fin de session

Dérouler `/fin-de-tache` (mode **parallèle** : pas de commit/push, pas de fichiers partagés ;
statut + checklist visuelle dans ce `S3.md`). **Session lourde** : vérifier en fin de session qu'aucun
import, constante (`D2_DUELS`), classe CSS (`.sideLabel`, `.d2Duels`, `.verdict*`) ou handler
(`handleD2Duel`) supprimé ne laisse de référence morte (`tsc` strict + grep).

---

## Bilan de session

**T4, T5, T6, T7 exécutées dans l'ordre le 2026-07-12.** Aucun commit, aucun push (mode vague
parallèle) — diff laissé non commité dans l'arbre de travail. Aucun fichier partagé touché
(`STATUS.md`, `TASKS.md`, `index.md`, `VALIDATION.md` racine non modifiés).

### Fichiers modifiés

- `src/features/diabete/alimentation/AlimentationModule.tsx`
- `src/features/diabete/alimentation/AlimentationModule.module.css`

(`glycemieCurve.ts`, `CourbeGlycemie`, le garde-manger et `FicheOverlay` — hors périmètre — non
touchés, conforme à la consigne.)

### Résumé par tâche

- **T4** — Les 5 invocations de `<CourbeSection …/>` (défis ①-⑤) ont été déplacées de leur position
  de frère de `.layout` (ancien bloc l.1026-1162, supprimé) vers l'intérieur de leur bloc
  `{defi === N && (…)}` respectif dans `.stage`, juste après le contenu propre au défi. Props et
  gardes conservées à l'identique (`defi === 2 && d2Revealed` → simplifié en `d2Revealed &&` une
  fois à l'intérieur du bloc défi 2 ; `defi === 5 && synthPlate.length > 0` → `synthPlate.length > 0
  &&` à l'intérieur du bloc défi 5). CSS `.courbeCard` : `max-width: 900px; margin: 0 auto;` retiré
  au profit de `width: 100%` (occupe la largeur de `.stage`), padding uniformisé à `var(--space-lg)`
  pour une carte plus présente. `.stage` était déjà `flex-direction: column` avec `gap: var(--space-md)`
  — aucun changement structurel nécessaire là.
- **T5** — Défi ① (Composition) : le compteur `<p className={styles.sideLabel}>Dans l'assiette : …</p>`
  supprimé, classe `.sideLabel` retirée du CSS (plus référencée nulle part, vérifié par grep). Bouton
  « Recommencer » converti en icône seule (`RotateCcw`, lucide, déjà ajouté à l'import), avec
  `aria-label="Recommencer"` + `title="Recommencer"`. `.linkReset` reconverti en bouton rond icône
  44×44 (`display:inline-flex; border-radius:50%; border:1.5px solid var(--color-line)`), traitement
  appliqué **aux deux** usages `linkReset` existants (défi ① et synthèse ★), conformément à la note
  d'harmonisation du plan. Assiette `.d1Plate` agrandie 320×320 → 400×400 ; `.d1Chip` 56×56 → 64×64
  (+ taille de l'`IllustrationSlot` interne 44→50 pour garder la même proportion visuelle) ; `.side`
  simplifié (ne porte plus que le bouton, `align-self: center` pour un centrage vertical à côté de
  l'assiette). Drag&drop (`onDragOver`/`onDrop` sur `.d1Plate`) non touché.
- **T6** — Défi ② (Qualité) : rangée `.d2Duels` (4 boutons duels) supprimée du rendu ; constante
  `D2_DUELS` et handler `handleD2Duel` supprimés du code (vérifié par grep : plus aucune référence).
  Classe CSS `.d2Duels` retirée. Verdict : les deux paragraphes texte (« Bonne prédiction » /
  « Vous aviez dit : … », `.verdictOk`/`.verdictOff`) supprimés — seul `.verdictBadge` (« Pic … »)
  reste. Classes `.verdictOk`/`.verdictOff` retirées du CSS. Import `X` (lucide) devenu inutile après
  retrait de `.verdictOff` → retiré de l'import (`Check` conservé, encore utilisé par la synthèse ★
  `principleMarkOk`). Bouton reset `.d2Actions` (`btn btn--ghost` texte) converti en icône `RotateCcw`
  via `styles.linkReset` (même traitement qu'en T5). Vérifié : `d2CanReveal` ne dépendait déjà que de
  `d2Guess.A`/`d2Guess.B` non-null, jamais d'une sélection de duel préalable — aucun changement de
  logique nécessaire.
- **T7** — Confirmé par lecture de code que la courbe (T4) apparaît bien dans `.stage`, sous le
  contenu, pour les défis ③ (Ordre) et ④ (Repas complet/Proportion). Aucune suppression de contenu.
  Sur l'espacement (étape 2, « resserrer si nécessaire ») : analyse mais **aucun changement CSS
  appliqué** — le gap `.stage` (`var(--space-md)` = 16px) est déjà la valeur la plus resserrée du
  système d'espacement utilisée dans ce module, `.d3Row`/`.d4Layout` n'ajoutent aucune marge/padding
  suflémentaire créant un espace mort, et le shell (`ModuleShell.module.css`) n'impose pas de
  min-height sur `.stage` qui forcerait un vide. Le « space mort » documenté par l'audit était la
  distance jusqu'à la carte glycémie placée sous `.layout` (résolu structurellement par T4) plutôt
  qu'un espacement interne aux blocs ③/④. Calibrage fin de l'espacement explicitement différé à S6
  (index.md, décision #1 : allègements d'abord, verrouillage/re-mesure du cadre en dernier) — pas de
  gap arbitraire modifié sans retour visuel.

### Validations automatiques

Relancées après chaque tâche (T4, T5, T6, T7) — toutes vertes à chaque étape, résultat final :

- `npx tsc --noEmit` → 0 erreur (tsconfig.app.json a `noUnusedLocals`/`noUnusedParameters` activés :
  la passe confirme qu'aucun import/constante/variable mort ne subsiste).
- `npm run build` → OK (`tsc -b && vite build`, ~1.2-1.5 s, aucun warning).
- `npm test` → vert, 86/86 tests passés (2 fichiers), aucune régression sur `glycemieCurve.test.ts`
  ni les autres suites.
- Vigilance code mort (demande explicite fin de session) : grep confirmé **aucune** référence
  résiduelle à `D2_DUELS`, `handleD2Duel`, `.d2Duels`, `.sideLabel`, `.verdictOk`, `.verdictOff`.
  Script de recoupement classes CSS ↔ usages JSX (86 classes du module) : **0 classe orpheline**.

### Points résiduels (pour arbitrage S6)

- Aucun défi n'a débordé au point de nécessiter un scroll visible dans le code (pas de hauteur fixe
  forcée détectée) — mais ceci reste **non confirmé visuellement** (jamais de navigateur côté Claude,
  cf. invariant validation). Le défi le plus dense visuellement est probablement ④ (Repas complet) :
  `.d4Pie` 300×300 + `.d4Zones` (3 lignes) + `CourbeSection` agrandie (`width:100%`, padding
  `var(--space-lg)`) — à mesurer en priorité lors de la re-mesure S6.
- Le gap `.stage` (16px) et le padding `.courbeCard` (24px, relevé de 16px lors de T4) n'ont **pas**
  été retouchés au-delà de T4/T6 : si la re-mesure S6 constate un espace mort résiduel au-dessus de la
  courbe pour ③/④, resserrer `.courbeCard` en premier (padding vertical) avant de toucher au gap
  partagé `.stage` (qui impacterait aussi ①/②/⑤).
- Assiette `.d1Plate` portée à 400×400 (valeur de départ suggérée par le plan, non recalée à l'œil) —
  à confirmer que 400px + la courbe agrandie dessous tiennent sans scroll sur 1366×768/1024×768 ; si
  trop juste, repli suggéré par le plan : 360-380px.

### Checklist visuelle (à valider par Thibault à l'écran — PAS Claude)

- [ ] **T4** — Sur les 4 défis (Composition, Qualité, Ordre, Repas complet), la courbe « Glycémie
  après le repas » est dans la colonne du défi, agrandie, visible sans scroll ; l'enchaînement
  « je manipule → je vois le résultat juste dessous » est préservé.
- [ ] **T5** — Composition : assiette plus grande + icône de reset (sans texte) + pas de compteur
  « Dans l'assiette : … » ; la courbe (T4) est juste dessous.
- [ ] **T6** — Qualité : 2 cartes de comparaison remontées (plus de rangée de duels), chaque carte
  garde son badge « Pic … » mais plus de « Vous aviez dit » ni « Bonne prédiction » ; reset en icône ;
  la courbe (T4) grande et visible sans scroll.
- [ ] **T7** — Ordre et Repas complet montrent la courbe agrandie, remontée, sans espace mort
  au-dessus ; pas de scroll ajouté.
