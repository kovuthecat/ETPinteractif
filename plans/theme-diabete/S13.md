# theme-diabete · S13 — Consolidation : contexte, commits, audit, push   (rédigé par Fable)

> **Modèle : Haiku · effort : low · Vague : 3 (parallèle : non — solo, après S4-S12 validées)**
> Exécutant : UNIQUEMENT les étapes ci-dessous, dans l'ordre.
> Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-07-09 · Branche : —

## Lire (commun à la session)

- `plans/theme-diabete/index.md` (table des sessions + messages de commit dans chaque `S<k>.md`).
- Les blocs « Statut » et checklists humaines consignés dans `S1.md` … `S12.md`.
- `C:\Users\kovu\SynologyDrive\Thibault\Projets\Templates\WORKFLOW.md` §4d (commits fin de plan).

## Hors périmètre

Aucune modification de `src/**` (si un fichier de code doit changer → STOP, ce n'est pas une session
de code). Pas d'audit visuel par Claude (Codex/Playwright uniquement, ou Thibault).

---

## D13 — Clore le plan proprement

### Objectif

Commits atomiques par tâche, fichiers de contexte resynchronisés, audit visuel lancé, un seul push.

### Étapes

1. Vérifier la gate globale une dernière fois : `npx tsc --noEmit` + `npm run build` + `npm test`.
2. **Commits tâche par tâche** (D1 → D12, staging explicite des fichiers de chaque tâche — jamais
   `git add -A`), messages = ceux prévus en bas de chaque `S<k>.md`.
3. Statuts : cocher `plans/theme-diabete/index.md` (colonne Statut) et les blocs Statut des `S<k>.md`
   (exécuté le / commit).
4. `TASKS.md` : passer D1-D13 à `[x]` avec le résumé de gate (modèle des chantiers précédents).
5. `STATUS.md` : nouvelle entrée datée — thème diabète câblé (9 modules), lib `glycemieCurve`,
   objets transversaux, fiches ; pointer les « à revalider (Thibault) » (fréquences module 6,
   seuils module 4, CG des aliments ajoutés, phrases cliniques module 7) et les illustrations à
   déposer (`public/illustrations/diabete/`).
6. `PROJECT_MAP.md` : arborescence `src/features/diabete/**` (9 modules + lib + components).
7. `DECISIONS.md`, nouvelles sections : modèle glycémie paramétrique testé (remplace le score
   maquette — demande Thibault) ; silhouette SVG dessinée (pas d'image) ; `IllustrationSlot` +
   convention `public/illustrations/diabete/<id>.png` ; familles diabète (Comprendre / Agir au
   quotidien / Se soigner) ; pas de police Caveat (italique serif).
8. `VALIDATION.md` : reporter les checklists humaines consignées dans chaque `S<k>.md` (une section
   par module, statut « à valider par Thibault »).
9. `docs/diabete/SPEC_outil_ETP_diabete.md` : cocher la colonne Design/statut du tableau §3 pour les
   9 modules (maquette reçue + câblée) — modification minimale, pas de réécriture.
10. **Un seul push.**
11. Demander l'audit visuel **Codex** (Playwright, cf. `AGENTS.md`) sur les 9 modules ; rapport →
    `VALIDATION.md`.

### Validation

- Auto : `git log --oneline` montre 12-13 commits atomiques ; `git status` propre après push.
- Humain : passe visuelle Thibault (checklists VALIDATION.md) — hors session.

### Si bloqué

Un diff ne correspond à aucune tâche (fichier orphelin) → STOP, lister le diff et signaler avant de
committer quoi que ce soit.

### Message de commit

(ceux des tâches D1-D12 + un commit `docs(diabete): consolidation plan theme-diabete — statuts, contexte, validation`)

### Statut

[ ] à faire · exécuté le : — · commit : —

---

## Fin de session

Dérouler `/fin-de-tache` (mode solo — c'est LA session qui commit et push).
