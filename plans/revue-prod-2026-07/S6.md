# revue-prod-2026-07 · S6 — Consolidation   (rédigé par Opus)

> **Modèle : Haiku · effort : minimal · Vague : finale (après S1-S5)**
> Exécutant : consolidation uniquement. Ne pas coder de feature.

- Date : — · Branche : —

## Objectif

Clore le chantier : commits tâche par tâche déjà faits en Vague 1 (S1-S5 en mode parallèle) → ici
staging explicite, statuts, fichiers de contexte, un seul push, puis vérification du redeploy.

## Étapes (§4d WORKFLOW)

1. **Statuts** : passer `[ ]` → `[x]` dans `index.md` (table des sessions) et cocher les tâches
   `RP1…RP6` dans `TASKS.md` (créer les entrées si absentes).
2. **STATUS.md** : ajouter une entrée datée « revue-prod-2026-07 » résumant : outils patient réparés
   (RP1), plan d'arrêt réduit (RP2), livret QR/`+ autre` (RP3), ergonomie consultation (RP4),
   cohérence & finitions tabac (RP6) — en listant les points réellement corrigés vs « non reproduits ».
3. **VALIDATION.md** : reporter les checklists humaines de S1-S5 (dont les points S4/S5 « non
   reproduits » avec la version testée).
4. **DECISIONS.md** : consigner les décisions structurantes de `index.md` (RP1 parité de montage,
   RP2 plan « bornes » + livret via état partagé, RP3 QR honnête, RP4 vérifier-avant-corriger,
   RP6 cohérence silhouette) et les réponses aux gates (G-RP2, G-RP3, G-RP6).
5. **PROJECT_MAP.md** : si le point de montage patient, l'UI du plan ou la silhouette bénéfices ont
   bougé, mettre à jour les pointeurs.
6. **Commits** : vérifier que chaque session a son commit atomique (messages fournis dans S1-S5) ;
   ajouter le commit de consolidation docs.
7. **Push unique** vers `origin/main`.
8. **Redeploy** : vérifier que Vercel a bien redéployé `main` (la prod doit refléter les correctifs ;
   contrôler notamment `patient.html` → un outil interactif se lance).

## Gates à figer

- **G-RP2** : parades (retirées, défaut) ; « + autre » situation dans Composantes (RP2b oui/non).
- **G-RP3** : QR (a) reformulé [défaut] ou (b) encodé.
- **G-RP4** : liste des points d'ergonomie réellement corrigés vs déjà OK.
- **G-RP6 ✅ tranché** : silhouette bénéfices tabac = **(a) surlignage d'organe** (RP6a codé) ; quiz
  Vrai/faux = **(b) posture non-évaluative assumée** (RP6c : aucun code, décision à consigner).

## Rappel — points produit hors périmètre (voir `index.md` § Roadmap)

Non traités par ce chantier (décisions produit à part) : synthèse take-home unifiée diabète + porte
d'entrée « Se motiver » diabète ; agrégation du carnet patient (« repérer mes moments à risque ») ;
app patient diabète.

### Message de commit
`docs: consolidation chantier revue-prod-2026-07`

### Statut
[ ] à faire

## Fin de session
Dérouler `/fin-de-tache` (mode consolidation : statuts, contexte, push, vérif redeploy).
