# refonte-ui · S6 — Restyle : Nicotine ≠ toxique   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 2 (parallèle : oui)**
> UNIQUEMENT cette tâche. Design fixé par `docs/DESIGN_REFONTE.md` — ne reconçois pas. Doute → STOP, signale.

- Date : 2026-07-06 · Branche : — (repo local, main)

## Lire (commun)
- `docs/DESIGN_REFONTE.md` §5, §6 (ligne « Nicotine ≠ toxique »), §7.
- **`maquettes/handoff/…/ETP Tabac.dc.html`** + `dc-script.extracted.js` (`SUBSTANCES_TOXIQUE`, handlers `setViewMalade`/
  `setViewDependance`/`toggleHotspot`) : scène avec **exactement 4 hotspots** — Goudrons (⚠️, x100), Monoxyde de carbone (⚠️, x185),
  Particules fines (⚠️, x270), Nicotine (🧠, x355) ; bascule `toxiqueView` « ⚠ Ce qui rend malade » / « 🧠 Ce qui crée la dépendance »
  (atténue l'autre groupe) ; clic hotspot → étiquette `detail` ancrée près du point (`openHotspot`) ; légende ⚠/🧠.
- Actuel : `src/features/nicotine-toxique/NicotineToxiqueModule.tsx` + `.module.css`.

## Hors périmètre
- Ne pas toucher `src/styles/**`, `registry.ts`, `types.ts`, `App.tsx`, `src/lib/**`. Pas de dépendance.
- **Garder le double encodage** ⚠/🧠 + libellés de rôle en gras et l'ancrage de l'étiquette près du point (R2/A7). Contenu médical inchangé.

---

## T8 — Restyler le module Nicotine ≠ toxique sur la maquette

### Étapes
1. Scène SVG + hotspots restylés via tokens (couleurs de rôle : combustion/toxique = `--color-toxique`, nicotine/dépendance = `--color-nav` ou confort selon maquette).
2. Bascule à deux états = `.btn`/segmented, l'état actif net, l'autre atténué (opacité déjà calibrée A7 — conserver le niveau).
3. Étiquette de hotspot = pop-over `.card`/`--shadow-3` ancré près du point. Légende = `.panel` avec icônes ⚠/🧠 + rôle en gras.
4. Relever couleurs/positions exactes dans `snapshot.html`.

### Validation
- Auto : `tsc -b` + `vite build` → succès.
- Humain (→ `VALIDATION.md`) : hotspots, bascule, étiquette ancrée, légende double encodée conformes à la maquette.

### Si bloqué
- Si le ratio de la scène décale les hotspots → conserver la logique de coordonnées viewBox existante (R2), ne restyler que l'apparence.

### Commit
`feat(refonte): restyle module nicotine ≠ toxique`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
`/fin-de-tache` mode **vague parallèle** (`git add src/features/nicotine-toxique/`, pas de push, statut ici).
