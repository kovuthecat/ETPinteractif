# refonte-ui · S6 — Restyle : Nicotine ≠ toxique   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 2 (parallèle : oui)**
> UNIQUEMENT cette tâche. Design fixé par `docs/DESIGN_REFONTE.md` — ne reconçois pas. Doute → STOP, signale.

- Date : 2026-07-06 · Branche : — (repo local, main)

## Lire (commun)
- `docs/DESIGN_REFONTE.md` §5, §6 (ligne « Nicotine ≠ toxique »), §7.
- **`maquettes/handoff/…/ETP Tabac.dc.html`** + `dc-script.extracted.js` (`SUBSTANCES_TOXIQUE`, handlers `setViewMalade`/
  `setViewDependance`/`toggleHotspot`) : scène avec **exactement 4 hotspots** — Goudrons (⚠️, x100), Monoxyde de carbone (⚠️, x185),
  Particules fines (⚠️, x270), Nicotine (🧠, x355) ; bascule `toxiqueView` « ⚠ Ce qui rend malade » / « 🧠 Ce qui crée la dépendance »
  (atténue l'autre groupe) ; clic hotspot → étiquette `detail` ancrée près du point (`openHotspot`) ; légende ⚠/🧠.
- Actuel : `src/features/nicotine-toxique/NicotineToxiqueModule.tsx` + `.module.css`.

## Hors périmètre
- Ne pas toucher `src/styles/**`, `registry.ts`, `types.ts`, `App.tsx`, `src/lib/**`. Pas de dépendance.
- **Garder le double encodage** ⚠/🧠 + libellés de rôle en gras et l'ancrage de l'étiquette près du point (R2/A7). Contenu médical inchangé.

---

## T8 — Restyler le module Nicotine ≠ toxique sur la maquette

### Étapes
1. Scène SVG + hotspots restylés via tokens (couleurs de rôle : combustion/toxique = `--color-toxique`, nicotine/dépendance = `--color-nav` ou confort selon maquette).
2. Bascule à deux états = `.btn`/segmented, l'état actif net, l'autre atténué (opacité déjà calibrée A7 — conserver le niveau).
3. Étiquette de hotspot = pop-over `.card`/`--shadow-3` ancré près du point. Légende = `.panel` avec icônes ⚠/🧠 + rôle en gras.
4. Relever couleurs/positions exactes dans `snapshot.html`.

### Validation
- Auto : `tsc -b` + `vite build` → succès.
- Humain (→ `VALIDATION.md`) : hotspots, bascule, étiquette ancrée, légende double encodée conformes à la maquette.

### Si bloqué
- Si le ratio de la scène décale les hotspots → conserver la logique de coordonnées viewBox existante (R2), ne restyler que l'apparence.

### Commit
`feat(refonte): restyle module nicotine ≠ toxique`

### Statut
[x] fait · exécuté le : 2026-07-08 · commit : —

## Fin de session
`/fin-de-tache` mode **vague parallèle** (`git add src/features/nicotine-toxique/`, pas de push, statut ici).

### Bilan de session

- **Fichiers modifiés** : `src/features/nicotine-toxique/NicotineToxiqueModule.module.css` (restyle complet).
  `NicotineToxiqueModule.tsx` non touché (logique/markup inchangés, pas nécessaire).
- **Résumé** : couleur du groupe « dépendance/nicotine » passée de `--color-confort` (vert) à `--color-nav`
  (bleu) — vérifié exact avec le handoff (`dependanceButtonStyle: oklch(48% 0.08 230)` = `--color-nav` ;
  `maladeButtonStyle: oklch(55% 0.15 30)` = `--color-toxique`, déjà correct). Bascule à deux états restylée
  en segmented control (actif = fond plein couleur + texte blanc, inactif = fond blanc + `--shadow-1` +
  texte atténué, pill radius), conforme au `viewBtnStyle` du handoff — double encodage conservé (icône ⚠️/🧠
  et bascule Check au clic). Pop-over de détail passé sur `--radius-lg` + `--shadow-3` (proche `.card`).
  Légende et étiquettes de zone alignées sur `--color-nav`/`--color-nav-soft`. Suppression de toutes les
  valeurs de repli hex (`#b4232d`, `#24734b`, etc.) et alignement des tokens espacement/rayon/ombre sur
  `tokens.css` (plus de fallback `, 1rem` / `, 44px`), conforme à la convention observée dans les modules
  déjà restylés (`addiction`). Géométrie viewBox et logique des hotspots non touchées (R2).
- **Tests lancés** : `npx vitest run` → 20/20 verts. `npx tsc --noEmit` → aucune erreur sur
  `nicotine-toxique` (grep ciblé, module non concerné). `vite build` échoue globalement mais uniquement
  sur `features/nicotine/**` et `features/soulagement/**` (API `nicotineCurve.ts` encore désalignée par
  S10, cf. bilan S10 — hors périmètre S6, ces modules sont traités par S4/S7 dans cette même vague).
- **À valider visuellement** : hotspots (4 points, icônes conservées), bascule ⚠/🧠 (état actif net en bleu
  ou rouge selon le groupe, l'autre atténué au même niveau qu'avant), étiquette de détail ancrée près du
  point cliqué avec l'ombre `--shadow-3`, légende avec libellés de rôle en gras.
- **Prochaine action** : une fois S4/S7 terminés (branchement sur la nouvelle API `nicotineCurve.ts`), un
  `vite build` global redeviendra vert ; revalider visuellement ce module dans le navigateur à ce moment-là.
