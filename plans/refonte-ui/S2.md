# refonte-ui · S2 — Chrome : accueil + coquille + carte + Sources   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 2 (parallèle : oui)**
> Exécutant : UNIQUEMENT cette tâche ; fichiers sous « Lire » / « Modifier ». Design fixé par
> `docs/DESIGN_REFONTE.md` — ne reconçois pas. Doute → STOP, signale, rends la main.

- Date : 2026-07-06 · Branche : — (repo local, main)

## Lire (commun)
- `docs/DESIGN_REFONTE.md` §2, §5, §6 (ligne « Accueil »). **`maquettes/handoff/…/ETP Tabac.dc.html`** : bloc
  d'accueil (« Programme ETP · Sevrage tabagique », « Votre accompagnement », familles « Comprendre/Agir/Se motiver »,
  cartes = pastille d'icône teintée + titre + résumé) et en-tête module (bouton « Accueil », bouton « Sources » + pop-over).
- Actuel : `src/components/Home.tsx`(+`.module.css`), `ModuleShell.tsx`(+`.module.css`),
  `ModuleCard.tsx`(+`.module.css`), `Sources.tsx`(+`.module.css`).

## Hors périmètre
- Ne pas modifier `src/styles/**` (figé par S1), `src/features/**`, `App.tsx`, `registry.ts`, `types.ts`.
- Ne pas changer la structure de navigation ni les props des composants.

---

## T4 — Restyler l'accueil et la coquille sur la maquette

### Objectif
Accueil et coquille adoptent le look éditorial de la maquette, à structure/props inchangées.

### Décision clé
Le regroupement en 3 familles existe déjà (`Home.tsx`). On restyle + on ajoute le surtitre/hero de la maquette.
Valeurs exactes (tailles, couleurs de pastille par module) → `snapshot.html`.

### Lire / Modifier
- Modifier : les 4 paires `.tsx`/`.module.css` de `src/components/` ci-dessus.

### Étapes
1. **Home** : conteneur centré `max-width:980px`, padding généreux (§4). En-tête = `.eyebrow`
   « PROGRAMME ETP · SEVRAGE TABAGIQUE » + titre serif « Votre accompagnement ». Chaque famille : eyebrow discret
   (Comprendre / Agir / Se motiver) + grille de cartes. (Copie exacte des libellés depuis `snapshot.html`.)
2. **ModuleCard** : surface `.card`, **pastille d'icône teintée** `38×38 radius:10px` (fond `<hue>/.16`, icône `<hue>` —
   teinte par module lue dans `snapshot.html`), titre `15.5px/600` serif ou sans selon maquette, résumé en `--color-text-soft`.
   Cible ≥44 px, survol = `--shadow-2`.
3. **ModuleShell** : en-tête = bouton **« Accueil »** (flèche + libellé, style `.btn--ghost` discret) à gauche,
   titre de module serif au centre/gauche, bouton **« Sources »** (icône + libellé, `--color-nav`, ≥44 px) à droite.
   Conserver `:focus-visible` net (A4).
4. **Sources** : pop-over sur `--color-surface`, `--shadow-3`, `--radius`, liste des références ; repli « Sources : à compléter ».

### Validation
- Auto : `tsc -b` + `vite build` → succès.
- Humain (→ `VALIDATION.md`) : accueil = 3 familles lisibles, cartes « papier chaud » avec pastille teintée ;
  coquille = Accueil/Sources visibles, pop-over Sources OK ; comparer à `snapshot.html`.

### Si bloqué
- Si une teinte de pastille par module est ambiguë → prendre `--color-nav`/.16 par défaut et signaler.

### Commit
`feat(refonte): accueil + coquille + carte + sources sur la maquette`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
Dérouler `/fin-de-tache` (mode **vague parallèle** : `git add` ciblé sur `src/components/`, **pas** de push,
statut consigné dans ce fichier ; consolidation en T12).
