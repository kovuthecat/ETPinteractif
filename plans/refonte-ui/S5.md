# refonte-ui · S5 — Restyle : Substituts (+ encart technique de prise)   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 2 (parallèle : oui)**
> UNIQUEMENT cette tâche. Design fixé par `docs/DESIGN_REFONTE.md` — ne reconçois pas. Doute → STOP, signale.

- Date : 2026-07-06 · Branche : — (repo local, main)

## Lire (commun)
- `docs/DESIGN_REFONTE.md` §5, §6 (ligne « Substituts »), §7.
- **`maquettes/handoff/…/ETP Tabac.dc.html`** + `dc-script.extracted.js` (`FORMES`, `buildPatchSquares`, handlers
  `selectForme`/`toggleEnvie`/`toggleSurdosage`/`applyPlusQuarter`/`applyMinusQuarter`/`revertToPrevious`) : 7 formes
  (patch/gomme/pastille/comprimé/spray = contenu ; inhaleur/vapoteuse = `hasContent:false` → repli « en rédaction »),
  « Bonnes pratiques — <forme> » / « Erreurs fréquentes », titration en **quarts** (`doseQuarters` 1–8, `+¼ / −¼`),
  bannière « ⚠ Signes de surdosage » + « Revenir à la dose précédente », « ☀ Jour / ☾ Nuit »,
  encart placeholder « Technique de prise — <forme> » (« illustration · technique de prise »).
- Actuel : `src/features/substituts/SubstitutsModule.tsx` + `.module.css`.

## Hors périmètre
- Ne pas toucher `src/styles/**`, `registry.ts`, `types.ts`, `App.tsx`, `src/lib/**`. Pas de dépendance.
- **Aucun dosage chiffré.** Ne pas inventer le contenu manquant (inhaleur/vapoteuse gardent le repli « en rédaction »).

---

## T7 — Restyler Substituts + poser l'encart « Technique de prise »

### Étapes
1. 7 cartes-formes = `.card` cliquables, état actif coloré par token, cible ≥44 px. Panneaux bonnes pratiques / erreurs = `.panel`.
2. Titration : quarts du patch (visuel de fractions), toggles envie/surdosage ; **bannière surdosage = `.alert`** (rouge explicite,
   signes listés) ; bloc « ☀ Jour / ☾ Nuit » ; message permanent « Expérimentez, fiez-vous à votre ressenti ». Mécanique inchangée.
3. **Nouveauté maquette** : encart « Technique de prise — <forme> » = **placeholder sobre** (cadre `.card` + légende
   « illustration · technique de prise »), sans illustration réelle ni contenu inventé. Le poser tel quel, à valider par Thibault.

### Validation
- Auto : `tsc -b` + `vite build` → succès.
- Humain (→ `VALIDATION.md`) : cartes-formes, bannière surdosage, Jour/Nuit, encart technique de prise (placeholder) conformes.

### Si bloqué
- Si l'encart « technique de prise » exige un asset absent → laisser un placeholder texte et signaler. Ne pas générer d'image.

### Commit
`feat(refonte): restyle module substituts + encart technique de prise`

### Statut
[x] fait · exécuté le : 2026-07-08 · commit : — (à faire en fin de plan)

## Fin de session

Fichiers modifiés : `src/features/substituts/SubstitutsModule.tsx`, `src/features/substituts/SubstitutsModule.module.css`.

Résumé : cartes-formes restylées en `.formeCard` (surface + `--radius` + `--shadow-1`, actif =
fond/bordure `--color-confort` + dot plein, double encodage par bordure+fond+dot, pas par la teinte
seule) ; panneaux bonnes pratiques / erreurs = bandeau `--color-confort`/`--color-toxique` (fond soft +
bord gauche 5px) ; bannière surdosage = classe globale `.alert` (`--color-toxique-soft` + bordure) ;
bloc titration dans une carte (`--radius-lg`, `--shadow-1`) avec toggles envie/surdosage/jour-nuit
restylés (`stateCard`) et blocs ☀/☾ inchangés en mécanique. Nouveauté maquette : encart « Technique de
prise — <forme> » ajouté en placeholder sobre (fond hachuré `--color-confort-soft` + légende monospace
« illustration · technique de prise »), affiché pour les formes à contenu hors patch (le patch garde sa
section titration dédiée) — aucune illustration réelle, aucun contenu inventé. Mécanique (quarts,
envie/surdosage, jour/nuit) strictement inchangée.

Tests lancés :
- `npx tsc -b` → 0 erreur dans `src/features/substituts/**` (erreurs restantes = `soulagement` /
  `nicotineCurve.ts`, hors périmètre, dues à la session S10 en cours en parallèle sur le même repo).
- `npx vite build` (mode development) → échoue uniquement sur les mêmes exports manquants côté
  `nicotineCurve.ts`/`soulagement` (S10 non terminée) ; aucune erreur ne référence `substituts`.
- Pas de suite Vitest dédiée au module substituts (aucun test existant pour ce module).

À valider visuellement (checklist, → à recopier dans `VALIDATION.md` en fin de plan) :
- [ ] 7 cartes-formes : lisibles, cible ≥44 px, état actif visuellement distinct (bordure + fond +
  dot, pas juste la teinte).
- [ ] Panneaux bonnes pratiques (vert) / erreurs fréquentes (rouge) lisibles, bandeau gauche coloré.
- [ ] Fiche « en rédaction » (inhaleur/vapoteuse) = cadre pointillé sobre, pas de contenu inventé.
- [ ] Encart « Technique de prise — <forme> » : placeholder hachuré + légende monospace, absent pour le
  patch, absent pour inhaleur/vapoteuse.
- [ ] Bannière surdosage = rouge explicite (`.alert`), bouton « Revenir en arrière (− ¼) » fonctionnel.
- [ ] Bloc titration : quarts du patch, boutons +¼/−¼, bloc ☀ Jour / ☾ Nuit, message permanent.

Prochaine action : validation visuelle humaine par Thibault (checklist ci-dessus), puis report dans
`VALIDATION.md` en fin de plan (mode vague parallèle : pas de commit/push ici, pas de modif des fichiers
partagés STATUS.md/TASKS.md/index.md/VALIDATION.md).
