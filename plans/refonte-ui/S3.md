# refonte-ui · S3 — Restyle : Addiction   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 2 (parallèle : oui)**
> UNIQUEMENT cette tâche. Design fixé par `docs/DESIGN_REFONTE.md` — ne reconçois pas. Doute → STOP, signale.

- Date : 2026-07-06 · Branche : — (repo local, main)

## Lire (commun)
- `docs/DESIGN_REFONTE.md` §5 (primitives), §6 (ligne « Addiction »), §7 (ce qui ne change pas).
- **`maquettes/handoff/…/ETP Tabac.dc.html`** : section « Les trois dimensions de l'addiction » — Venn de **3 cercles chevauchants**
  (Physique = ambre `oklch(65% 0.13 80)` cx210,cy160 ; Psychologique = bleu `oklch(48% 0.08 230)` cx390,cy160 ; Comportementale =
  vert `oklch(58% 0.09 145)` cx300,cy300 ; r130), sélection = **fill-opacity + stroke-width**, panneau à onglets « De quoi parle-t-on ? »
  (`tab='quoi'`) / « Outils & stratégies — <pilier> » (`tab='outils'`), note « Ces dimensions s'alimentent entre elles ».
  Données piliers (exemples/outils) dans `dc-script.extracted.js`.
- Actuel : `src/features/addiction/AddictionModule.tsx` + `.module.css`.

## Hors périmètre
- Ne pas toucher `src/styles/**`, `registry.ts`, `types.ts`, `App.tsx`, `src/lib/**`. **Pas** de nouvelle dépendance.
- Ne pas modifier la géométrie des 3 cercles ni le mécanisme de sélection (fill-opacity + stroke) ni le double encodage/légende.

---

## T5 — Restyler le module Addiction sur la maquette

### Objectif
Appliquer tokens + primitives à la scène Venn et au panneau d'exploration, aligner libellés/espacements sur la maquette.

### Étapes
1. Fond/typo/rayons/ombres via tokens ; titres serif ; texte secondaire `--color-text-soft`.
2. Panneau « De quoi parle-t-on ? » / « Outils & stratégies — <pilier> » = `.panel`/`.card`, chips = `.chip`,
   boutons de renvoi = `.btn--ghost`. Halo des libellés de cercle conservé pour la lisibilité sur chevauchement.
3. Comparer disposition/tailles au `snapshot.html` (cercles lisibles à ~1 m). Ne rien inventer côté contenu.

### Validation
- Auto : `tsc -b` + `vite build` → succès.
- Humain (→ `VALIDATION.md`) : Venn + panneau conformes à la maquette ; renvois fonctionnels ; double encodage intact.

### Si bloqué
- Divergence de contenu maquette vs `docs/contenu-modules.md` → garder le contenu actuel, signaler.

### Commit
`feat(refonte): restyle module addiction`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
`/fin-de-tache` mode **vague parallèle** (`git add src/features/addiction/`, pas de push, statut ici).
