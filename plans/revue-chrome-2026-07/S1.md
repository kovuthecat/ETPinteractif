# revue-chrome-2026-07 · S1 — Diabète : donut→camembert, ordonnance vide, bulle cardio   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 1 (parallèle : oui)**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-07-15 · Branche : —

## Lire (commun à la session)

- `plans/revue-chrome-2026-07/index.md` §Décisions structurantes (repérage seulement)
- `docs/diabete/` (autorité contenu diabète) — uniquement pour C4 (textes des zones)

## Hors périmètre

- Ne toucher AUCUN fichier partagé (`STATUS.md`, `TASKS.md`, `index.md`, `VALIDATION.md`) — vague parallèle.
- Ne pas toucher au module Insuline ni Hypoglycémie (autres sessions).
- Pas de commit ni push (consolidation en S17).

---

## C1 — Donut → camembert (assiette pleine)

### Objectif
Le graphique de répartition de l'assiette doit être un camembert plein, pas un anneau.

### Décision clé
La correction n'a jamais été appliquée ; il n'y a **aucun composant en double**. Un seul cercle
central perce le donut.

### Lire / Modifier
- Modifier : `src/features/diabete/alimentation/AlimentationModule.tsx`

### Étapes
1. Supprimer le dernier enfant du `<svg>` du camembert : `<circle cx="100" cy="100" r="34"
   fill="var(--color-bg)" />` (≈ ligne 981, juste après le `.map()` de `d4PieSlices`).
2. Ne rien changer d'autre : les 3 secteurs (Légumes/Protéines/Féculents) sont déjà gérés ; un
   secteur à 0 % ne se trace pas (comportement voulu, pas un bug).

### Validation
- Auto (bloque le commit) : `npx tsc --noEmit` puis `npm run build` → OK.
- Humain (non bloquant) : onglet « Proportion » → assiette pleine, 3 parts colorées quand les 3
  compteurs > 0 → `VALIDATION.md`.

### Si bloqué
Si un 2ᵉ générateur d'arcs existait ailleurs (non attendu) → STOP, signaler.

### Message de commit
`fix(diabete): remove donut hole to render full pie in Alimentation`

### Statut
[ ] à faire · exécuté le : — · commit : —

---

## C3 — Ordonnance vide par défaut + état vide

### Objectif
À l'ouverture, l'ordonnance ne contient plus les 2 lignes pré-remplies ; un état vide léger guide l'ajout.

### Lire / Modifier
- Lire : `src/features/diabete/traitements/TraitementsModule.tsx` (rendu de la liste `lignes`,
  ≈ l.154-240), `data.ts` (`lignesInitiales`, `newLigne`)
- Modifier : `src/features/diabete/traitements/data.ts`, `TraitementsModule.tsx`

### Étapes
1. `data.ts` `lignesInitiales()` (≈ l.129-131) → `return [];`.
2. `TraitementsModule.tsx` : quand `lignes.length === 0`, afficher sous l'en-tête un état vide léger
   (ex. `<li>`/`<p>` « Aucune ligne — cliquez sur "+ Ajouter une ligne". »), en gardant en-tête
   « Ordonnance », entêtes Dr/Patient/Date et le bouton « + Ajouter une ligne ».
3. Vérifier : numérotation déjà dynamique (`i + 1`) repart de 1 ; « Ajouter » crée une ligne vierge
   (`newLigne('', <classe par défaut>)`) — ne pas copier d'anciennes valeurs.

### Validation
- Auto : `npx tsc --noEmit` + `npm run build` → OK.
- Humain : page Traitements → 0 ligne + message ; ajout crée une ligne vierge numérotée 1 → `VALIDATION.md`.

### Si bloqué
La valeur par défaut du `<select>` de classe n'est pas évidente → prendre la 1ʳᵉ option, signaler.

### Message de commit
`fix(diabete): start prescription empty with empty-state hint`

### Statut
[ ] à faire · exécuté le : — · commit : —

---

## C4 — Bulle cardio : retirer le fond blanc + hiérarchiser le résultat

### Objectif
La bulle de description des zones (onglet « L'anatomie ») s'intègre au fond crème ; le terme-résultat
(AVC / infarctus / artériopathie) est mis en avant, en gras.

### Décision clé
Le fond blanc + l'ombre viennent de la classe globale `.card` accolée à `.zoneDescPanel`. Le
mot-résultat est aujourd'hui **noyé en fin de phrase** dans le champ `desc`. Zones réelles du code :
**Cou → AVC · Cœur → infarctus · Jambes → artériopathie** (« cerveau » du rapport = « Cou »).

### Lire / Modifier
- Modifier : `src/features/diabete/risque-cardio/RisqueCardioModule.tsx`,
  `src/features/diabete/risque-cardio/RisqueCardioModule.module.css`

### Étapes
1. Données `ZONES` (≈ l.81-97) : scinder chaque entrée en `{ mecanisme, resultat }` (ex. cœur :
   mecanisme = « Une artère du cœur bouchée l'empêche de recevoir assez de sang : c'est
   l'… », resultat = « infarctus »). Conserver les phrases actuelles, mot-résultat isolé.
2. Rendu de la bulle (≈ l.262-268) : afficher le mécanisme puis le résultat en `<strong>` (ex.
   « … c'est l'**infarctus**. »). Placeholder inchangé si aucune zone active.
3. CSS : retirer la classe `card` du `<div className={`${styles.zoneDescPanel} card`}>` (rendre
   `zoneDescPanel` seul) — ou surcharger `.zoneDescPanel { background: transparent; box-shadow: none;
   border: none; }`. Le texte doit reposer sur le fond crème de la page.

### Validation
- Auto : `npx tsc --noEmit` + `npm run build` → OK.
- Humain : clic Cou/Cœur/Jambes → pas de carte blanche, résultat en gras (AVC/infarctus/artériopathie) → `VALIDATION.md`.

### Si bloqué
Un doute clinique sur le découpage mécanisme/résultat → garder la phrase, ne pas inventer, signaler.

### Message de commit
`fix(diabete): flatten cardio zone bubble and bold the outcome term`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
Dérouler `/fin-de-tache` (mode parallèle : ne pas toucher STATUS/TASKS/index/VALIDATION, pas de push).
