# revue-chrome-2026-07 · S17 — Consolidation   (rédigé par Opus)

> **Modèle : Haiku · effort : minimal · Vague : finale (solo)**
> Exécutant : consolidation uniquement, après que toutes les sessions exécutées soient validées
> (build + typecheck + tests + visuel Thibault). Doute → STOP.

- Date : 2026-07-15 · Branche : —

## Lire (commun à la session)

- `plans/revue-chrome-2026-07/index.md` (statuts, table des sessions)
- `WORKFLOW.md` §4d (commits & parallélisation) — chemin absolu Templates
- Chaque `S<k>.md` exécuté (messages de commit, statuts, fichiers de chaque tâche)

## Hors périmètre

- Ne rien réimplémenter. Ne consolider QUE les sessions réellement exécutées et validées.

---

## Consolidation (§4d WORKFLOW)

### Étapes
1. **Commits tâche par tâche**, staging explicite (`git add <fichiers de la tâche>` — jamais
   `git add -A` / `git commit -a`), message = celui prévu dans chaque `T<code>`. Ordre = vagues.
2. Mettre à jour les statuts : `index.md` (colonne Statut → `[x]`), `TASKS.md` (entrées de ce chantier),
   cocher chaque tâche dans son `S<k>.md` (`[x]` + date + hash court).
3. `STATUS.md` : état d'avancement du chantier. `VALIDATION.md` : consigner les validations visuelles
   (déjà reportées par session).
4. `DECISIONS.md` : ajouter les décisions structurantes de l'index (A5 partout ; localStorage patient ;
   dose titration en SelectionContext session ; composant TitrationPatch partagé ; patch page dédiée ;
   situations par composante ; bande basale {25,60} + repères Coucher/Réveil ; priorisation outils par
   pilier ; reformulation « Émotions propices au tabac » ; visuels produit vs schémas), format habituel
   (Décision / Contexte / Alternatives / Raison / Conséquences / Impact IA).
5. **`CLAUDE.md` invariant #1** : **déjà amendé au cadrage (2026-07-15)** — persistance scopée
   (consultation = zéro / app patient = localStorage local). La consolidation n'a qu'à **committer**
   cette modif avec le reste (vérifier qu'elle est bien présente).
6. `PROJECT_MAP.md` : référencer les nouveaux fichiers (`components/TitrationPatch`, `patient/lib/storage`,
   `components/RespirationGuidee`, `patient/carnet`, assets `public/illustrations/tabac/produit-*.png`).
7. **Un seul `git push`** pour l'ensemble du plan.

### Validation
- Auto : `git status` propre après commits ; `npm run build` + `npm test` verts une dernière fois.
- Humain : Thibault confirme le périmètre poussé.

### Si bloqué
Une session non validée visuellement → ne pas committer cette tâche, la laisser `[ ]`, signaler.

### Statut
[ ] à faire · exécuté le : — · commit : —
