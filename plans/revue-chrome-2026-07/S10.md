# revue-chrome-2026-07 · S10 — Livret : ordre + « 4D » + situations par composante   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 2 (parallèle : oui)**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-07-15 · Branche : —

## Lire (commun à la session)

- `src/features/tabac/plan-arret/livretSections.tsx` (`buildLivretSections` ordre ≈ l.215-236 ;
  `comprendreBody` chips à plat ≈ l.27-51 ; `outilsParadesBody` sous-blocs « Mes outils »/« Mes
  parades » ≈ l.84-124)
- `src/content/tabac/situations.ts` (`SituationDef.pilier` — physique/psychologique/comportementale)
- `src/features/tabac/addiction/AddictionModule.tsx` (libellés des 3 composantes/piliers ≈ l.35-57) — **lecture seule**
- `src/features/tabac/plan-arret/PlanArretModule.tsx` (`PARADES` = les 4D ≈ l.38-39)

## Hors périmètre

- Ne pas toucher aux illustrations/dose des substituts (S13) ni au CSS print (S9).
- Ne pas inventer de contenu : réutiliser libellés existants (composantes = module Addiction).
- Fichiers partagés / commit / push : non (S17).

---

## D3 — Réordonner les sections (logique aide-mémoire d'usage)

### Objectif
Nouvel ordre : 1) situations à risque · 2) substituts · 3) outils (4D) · 4) si j'ai un écart ·
5) motivations/raisons · 6) ce que l'arrêt répare · 7) contacts.

### Lire / Modifier
- Modifier : `src/features/tabac/plan-arret/livretSections.tsx` (séquence dans `buildLivretSections`)

### Étapes
1. Réordonner le tableau de sections (≈ l.215-236) selon l'ordre ci-dessus. Vérifier cohérence
   eyebrow ↔ position.
2. Coordonner avec la pagination (S9) : le nouvel ordre conditionne le découpage A5 — ne pas ajouter de
   saut forcé ici.

### Validation
- Auto : `npx tsc --noEmit` + `npm run build` + `npm test` → OK.
- Humain : livret → ordre conforme (outils d'usage d'abord, renforcement ensuite, contacts en clôture) → `VALIDATION.md`.

### Message de commit
`feat(livret): reorder sections into usage aide-mémoire logic`

### Statut
[ ] à faire · exécuté le : — · commit : —

---

## D6 — Renommer « parades » → « les 4D »

### Objectif
Supprimer la notion de « parade » ; présenter les 4D comme un outil unique et identifiable.

### Décision clé
Les 4D vivent dans `PlanArretModule.tsx:39` (`PARADES = ['Différer', "Détourner l'attention", 'Se
détendre — respirez', "D'eau"]`). Le livret a un sous-bloc `subLabel` « Mes parades » (≈ l.112).

### Lire / Modifier
- Modifier : `src/features/tabac/plan-arret/livretSections.tsx` (+ libellé de section « Mes parades &
  mes outils » si présent)

### Étapes
1. Renommer le sous-bloc « Mes parades » → présentation « Les 4D » (outil unique : Différer, Détourner
   l'attention, se Détendre/respirer, boire de l'eau — « D'eau »).
2. Adapter le titre de section « Mes parades & mes outils » → intitulé centré sur les outils / les 4D.
3. Ne pas dupliquer : si « Mes outils » et « Les 4D » se recoupent, présenter les 4D comme le bloc outil.

### Validation
- Auto : `npx tsc --noEmit` + `npm run build` → OK.
- Humain : section outils du livret → « 4D » clair, plus de « parade » → `VALIDATION.md`.

### Message de commit
`feat(livret): rename "parades" section to the 4D tool`

### Statut
[ ] à faire · exécuté le : — · commit : —

---

## D4 — Situations à risque regroupées par composante de l'addiction

### Objectif
Les chips de « Mes situations à risque » sont regroupées par composante (physique / psychologique /
comportementale), avec sous-titres, au lieu d'une liste à plat.

### Décision clé
Le champ `pilier` existe déjà sur chaque situation (`situations.ts`) et correspond exactement aux 3
composantes du module Addiction. Simple *group-by* ; réutiliser les libellés de composante
d'`AddictionModule` (l.35-57). Les situations libres (sans pilier) vont dans un groupe « Autres ».

### Lire / Modifier
- Modifier : `src/features/tabac/plan-arret/livretSections.tsx` (`comprendreBody`)

### Étapes
1. Dans `comprendreBody` (≈ l.27-51), au lieu d'un `.tagRow` à plat : regrouper `state.situations` par
   `pilier` (via `SITUATIONS`), un sous-titre par composante (libellés Addiction), chips dessous.
2. `state.situationsLibres` (sans pilier) → groupe « Autres » en fin.
3. N'afficher que les groupes non vides ; garder le repli `.empty` si aucune situation.
4. Chips plus lisibles (hiérarchie 2 colonnes possible) — rester dans le style existant.

### Validation
- Auto : `npx tsc --noEmit` + `npm run build` + `npm test` → OK.
- Humain : sélectionner des situations de piliers différents → chips regroupées par composante avec
  sous-titres ; situations libres sous « Autres » → `VALIDATION.md`.

### Si bloqué
Une situation sélectionnable dans le livret n'a pas de `pilier` mappable → la router vers « Autres » et
signaler.

### Message de commit
`feat(livret): group risk situations by addiction component`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
Dérouler `/fin-de-tache` (mode parallèle : ne pas toucher STATUS/TASKS/index/VALIDATION, pas de push).
