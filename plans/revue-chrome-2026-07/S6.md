# revue-chrome-2026-07 · S6 — Patient : schéma agrandi + label reformulé   (rédigé par Opus)

> **Modèle : Sonnet · effort : low · Vague : 1 (parallèle : oui)**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-07-15 · Branche : —

## Lire (commun à la session)

- `src/patient/substituts/PatientSubstituts.module.css` (`.cardIllustration` 72px ≈ l.64-70 ;
  `.illustration` `object-fit: contain` ≈ l.72-77 ; `.illustrationWrap .illustration` forcé 160px ≈ l.111-115)
- `src/patient/situations/PatientSituations.tsx` (`PILIER_LABELS` ≈ l.22-26, `.pilierLabel` rendu ≈ l.164)

## Hors périmètre

- Ne pas remplacer les fichiers PNG des substituts (visuels produit = S14). Ici, **taille d'affichage**
  du schéma détail uniquement.
- Ne pas toucher au mapping outils ni au reste des situations. Fichiers partagés / commit : non (S17).

---

## E2 — Agrandir le schéma d'utilisation (détail substitut)

### Objectif
Le schéma paysage (≈ 900×393) s'affiche en exploitant la largeur du conteneur (~328 px), au lieu
d'être écrasé dans un carré 160 px.

### Décision clé
`.illustration` est partagée grille (72px) / détail (160px) ; le détail est forcé par la règle
**scopée** `.illustrationWrap .illustration { width:160px; height:160px }`. Ne modifier QUE cette
règle scopée — la vignette de grille reste à 72px.

### Lire / Modifier
- Modifier : `src/patient/substituts/PatientSubstituts.module.css`

### Étapes
1. Dans `.illustrationWrap .illustration` (et `.illustrationFallback`, ≈ l.111-115) : remplacer
   `width:160px; height:160px` par `width:100%; max-width:328px; height:auto` (respecte le ratio
   paysage ; `object-fit: contain` devient neutre).
2. `.illustrationWrap` (≈ l.105) : passer une éventuelle hauteur fixe en `height:auto`.

### Validation
- Auto : `npx tsc --noEmit` + `npm run build` (2 entrées) → OK.
- Humain : détail Patch/Gomme… sur mobile → schéma grand et lisible, ratio respecté ; vignette de
  grille toujours à 72px → `VALIDATION.md`.

### Message de commit
`fix(patient): show substitute how-to schema at full container width`

### Statut
[ ] à faire · exécuté le : — · commit : —

---

## E5 — Reformuler « Ce que la cigarette apporte »

### Objectif
Le libellé du groupe « psychologique » ne laisse plus croire que la cigarette *apporte* du calme.

### Décision clé
Nouvelle formulation retenue : **« Émotions propices au tabac »** (à revalider). Cohérent avec la
carte « Respirer pour redescendre » (la cigarette soulage le manque qu'elle a créé).

### Lire / Modifier
- Modifier : `src/patient/situations/PatientSituations.tsx` (`PILIER_LABELS.psychologique`, ≈ l.24)

### Étapes
1. Remplacer `psychologique: 'Ce que la cigarette apporte'` par `psychologique: 'Émotions propices au
   tabac'`. Ajouter `// à revalider (Thibault)`.
2. Vérifier s'il existe un libellé équivalent côté consultation (module « Agir face à une situation »
   / addiction) ; si oui, signaler pour cohérence (ne pas modifier hors périmètre patient sans accord).

### Validation
- Auto : `npx tsc --noEmit` + `npm run build` (2 entrées) → OK.
- Humain : app patient → groupe renommé, chips inchangées → `VALIDATION.md`.

### Si bloqué
Un libellé jumeau existe côté consultation → signaler (cohérence à trancher), ne pas le modifier ici.

### Message de commit
`fix(patient): reword "ce que la cigarette apporte" pillar label`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
Dérouler `/fin-de-tache` (mode parallèle : ne pas toucher STATUS/TASKS/index/VALIDATION, pas de push).
