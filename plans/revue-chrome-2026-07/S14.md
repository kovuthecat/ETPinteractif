# revue-chrome-2026-07 · S14 — Patient : visuels produit substituts (assets)   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 4 (assets fournis + normalisés — prêt)**
> Exécutant : UNIQUEMENT la tâche ci-dessous ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-07-15 · Branche : —

## État des assets — FOURNIS

Les 6 visuels produit sont déjà générés (Thibault, 2026-07-15), **normalisés et placés** dans
`public/illustrations/tabac/` : `produit-patch.png`, `produit-gomme.png`, `produit-pastille.png`,
`produit-sublingual.png`, `produit-spray.png`, `produit-vapoteuse.png`. Traitement appliqué :
recadrage sur l'objet + marge égale (objet ≈ 80 % du côté), fond crème conservé, 256×256, métadonnées
ChatGPT/C2PA purgées (Pillow). Cette session n'a donc plus que le **câblage code** à faire.

## Lire (commun à la session)

- `src/patient/substituts/PatientSubstituts.tsx` (`FormeIllustration` `src` ≈ l.33, grille ≈ l.104,
  détail ≈ l.62) + `.module.css` (`.cardIllustration` 72px, `.illustrationWrap` détail)

## Hors périmètre

- Ne pas retoucher la taille du schéma détail (fait en S6/E2).
- Ne pas casser la consultation (elle continue d'utiliser les schémas `substitut-*.png` existants).
- Fichiers partagés / commit / push : non (S17).

---

## E1 — Vignette de grille = visuel produit (schéma réservé au détail)

### Objectif
Dans la grille « Mes substituts » (app patient), chaque carte montre un **visuel produit** clair et
identifiable à 72 px ; le **schéma multi-étapes** reste réservé à l'écran de détail.

### Décision clé
Les 6 PNG actuels sont des **schémas paysage** — bons pour le détail (E2), illisibles en vignette 72px.
Séparer les deux usages : nouveaux assets produit carrés pour la grille, schémas conservés pour le
détail. Conserver les `alt` existants.

### Mapping source → cible (déjà appliqué)

| Fichier fourni (Downloads) | Cible `public/illustrations/tabac/` |
| --- | --- |
| `Patch.png` | `produit-patch.png` |
| `Gomme.png` | `produit-gomme.png` |
| `Pastille.png` | `produit-pastille.png` |
| `Cp sublingual.png` | `produit-sublingual.png` |
| `Spray.png` | `produit-spray.png` |
| `vapoteuse.png` | `produit-vapoteuse.png` |

Prompt de référence conservé (si un asset doit être refait plus tard) : trait fin minimaliste, style
éditorial médical doux, objet seul centré, fond crème (#FAF3EA), traits gris-bruns + touches vert
sauge, aucune ombre dure/texte/main/étapes, lisible à 72 px, cohérent avec la série.

### Lire / Modifier
- Modifier : `src/patient/substituts/PatientSubstituts.tsx` (`FormeIllustration` : `src` produit pour la
  grille, `src` schéma pour le détail — deux `src` distincts selon l'usage)
- Assets : `public/illustrations/tabac/produit-*.png` (fournis)

### Étapes
1. Introduire un `src` produit pour la grille (`produit-${forme}.png`) et garder le schéma
   (`substitut-${forme}.png`) pour le détail.
2. Vérifier le rendu à 72 px (grille) et le fallback si un asset manque.

### Validation
- Auto : `npx tsc --noEmit` + `npm run build` (2 entrées) → OK.
- Humain : grille patient → 6 visuels produit lisibles et homogènes ; détail → schéma inchangé → `VALIDATION.md`.

### Si bloqué
Un `produit-*.png` manque ou détonne visuellement → STOP, lister, rendre la main (asset à refaire).

### Message de commit
`feat(patient): product thumbnails in substitutes grid (schemas kept for detail)`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
Dérouler `/fin-de-tache` (mode parallèle : ne pas toucher STATUS/TASKS/index/VALIDATION, pas de push).
