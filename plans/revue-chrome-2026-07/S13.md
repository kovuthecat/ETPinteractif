# revue-chrome-2026-07 · S13 — Livret : substituts `contain` + carte patch dédiée (dose)   (rédigé par Opus)

> **Modèle : Sonnet · effort : high · Vague : 3 (après S10 et S11)**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-07-15 · Branche : —

## Lire (commun à la session)

- `src/features/tabac/plan-arret/livretSections.tsx` (`substitutsBody` ; `IllustrationSlot
  id={`substitut-${forme}`} size={120}` ≈ l.66)
- `src/components/PrintableLivret.tsx` + `.module.css` (`.substitutBlock` grid `132px 1fr` ≈ l.182 ;
  `.substitutIllus` ≈ l.190 ; `.image { object-fit: cover }` via `IllustrationSlot.module.css`)
- `src/state/SelectionContext.tsx` (champ `titrationPatch` ajouté en S11)

## Hors périmètre

- Ne pas refaire l'ordre des sections (S10) ni le CSS print global (S9).
- N'afficher que les substituts sélectionnés (piloté par la sélection) — ne pas remettre de défaut.
- Fichiers partagés / commit / push : non (S17).

---

## D5a — Illustrations substituts entièrement visibles (`contain`)

### Objectif
Les illustrations de substituts du livret ne sont plus tronquées : la technique complète est lisible.

### Décision clé
`object-fit: cover` sur un cadre carré 120×120 recadre le schéma paysage → passer en `contain` avec un
ratio adapté (pas forcément carré). Vérifier lisibilité N&B + A5.

### Lire / Modifier
- Modifier : `src/components/PrintableLivret.module.css` (`.substitutIllus` / règle image scopée),
  éventuellement `livretSections.tsx` (taille/ratio de l'`IllustrationSlot`)

### Étapes
1. Pour l'image du livret : `object-fit: cover` → `contain` (règle scopée au livret, ne pas impacter les
   autres usages d'`IllustrationSlot`). Adapter le cadre au ratio paysage.
2. Vérifier rendu N&B (S9) et A5 (S9) : schéma déchiffrable.

### Validation
- Auto : `npx tsc --noEmit` + `npm run build` + `npm test` → OK.
- Humain : livret → schémas substituts entiers (non tronqués), lisibles en A5 N&B → `VALIDATION.md`.

### Message de commit
`fix(livret): show substitute schemas with object-fit contain`

### Statut
[ ] à faire · exécuté le : — · commit : —

---

## D5b — Carte patch dédiée avec la dose choisie, sur sa propre page

### Objectif
Le patch apparaît dans le livret comme une carte substitut présentant la **dose choisie** (quarts
jour/nuit), et occupe **une page pour lui seul**.

### Décision clé
La dose vient du `SelectionContext.titrationPatch` (S11). N'afficher que les substituts sélectionnés
(vérifier le pilotage par la sélection ; aujourd'hui Patch + Gomme apparaissent par défaut — à corriger
si résiduel). Décision actée : le patch a sa page dédiée.

### Lire / Modifier
- Modifier : `src/features/tabac/plan-arret/livretSections.tsx` (`substitutsBody`),
  `src/components/PrintableLivret.tsx` + `.module.css` (page dédiée patch)

### Étapes
1. `substitutsBody` : n'inclure que les substituts réellement sélectionnés (retirer tout défaut
   résiduel Patch+Gomme si non piloté par la sélection).
2. Pour le patch, injecter la dose (`titrationPatch` : quarts jour/nuit, distinction jour/nuit) dans la
   carte substitut (même gabarit), présentée comme la titration par quarts.
3. Pagination : la carte patch occupe sa propre page — appliquer `break-before: page` + `break-after:
   page` (ou conteneur dédié) à la carte patch uniquement, cohérent avec la pagination compacte de S9.

### Validation
- Auto (bloque le commit) : `npx tsc --noEmit` + `npm run build` + `npm test` → OK.
- Humain : sélectionner Patch (+ dose) et d'autres substituts → livret affiche uniquement les
  sélectionnés ; carte patch avec dose jour/nuit sur une page seule → `VALIDATION.md`.

### Si bloqué
`titrationPatch` absent du contexte (S11 non fait) → STOP, signaler la dépendance.

### Message de commit
`feat(livret): dedicated patch page with the chosen titration dose`

### Statut
[ ] à faire · exécuté le : — · commit : —

## Fin de session
Dérouler `/fin-de-tache` (mode parallèle : ne pas toucher STATUS/TASKS/index/VALIDATION, pas de push).
