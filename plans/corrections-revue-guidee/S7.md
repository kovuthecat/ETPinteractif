# S7 — Consolidation : contexte + commits tâche par tâche + push   (rédigé par Opus)

> **Modèle : Haiku · effort : minimal · Vague : 3 (consolidation — après S1-S6 exécutées ET validées visuellement par Thibault)**
> Exécutant : UNIQUEMENT les étapes ci-dessous. Aucune reconception, aucun correctif de code. Doute → STOP.

- Date : 2026-07-13 · Branche : —

## Pré-requis (gate humaine bloquante)

- **Toutes** les sessions S1-S6 sont `[x]` dans leur `S<n>.md` **et** validées visuellement par Thibault
  (`npm run dev`, `VALIDATION.md`). Ne pas committer une tâche non validée.
- `npx tsc --noEmit` · `npm run build` · `npm test` verts sur l'arbre de travail consolidé.

## Lire (commun à la session)

- Chaque `S<n>.md` de ce plan (statuts + messages de commit prévus par tâche + checklists visuelles).
- `STATUS.md`, `TASKS.md`, `VALIDATION.md`, `DECISIONS.md`, `PROJECT_MAP.md`, `index.md` de ce plan.
- `../Templates/WORKFLOW.md` §4d (commits tâche par tâche, staging explicite, un seul push).

## Hors périmètre

- **Jamais** `git add -A` / `git commit -a` : staging **explicite** des fichiers de chaque tâche.
- Ne pas mélanger deux tâches dans un commit ; ne pas pusher avant que tous les commits soient faits.
- Ne pas modifier le code des modules (consolidation documentaire + git uniquement).

---

## T1-G — Commits tâche par tâche

### Objectif
Committer chaque tâche exécutée et validée, avec le message prévu dans son `S<n>.md`, en staging explicite.

### Étapes
1. Pour **chaque** tâche `[x]` validée (dans l'ordre S1→S6), `git add <fichiers de la tâche>` puis `git commit` avec
   le message exact prévu dans le `T<n>` correspondant. Un commit = une tâche (atomique).
2. Renseigner dans chaque `S<n>.md` la date d'exécution et le hash de commit (ligne Statut).
3. Ne rien pusher encore.

### Validation
- Auto : `git log --oneline` montre un commit par tâche validée, messages conformes ; `git status` propre hors
  fichiers de contexte (T2-G).

### Message de commit
(Les messages sont ceux des tâches S1-S6 ; cette étape ne crée pas de commit propre.)

### Statut
[ ] à faire · exécuté le : — · commit : —

---

## T2-G — Mise à jour du contexte + push unique

### Objectif
Mettre à jour les fichiers de contexte, puis **un seul** push pour tout le plan.

### Étapes
1. `STATUS.md` : ajouter l'entrée du chantier `corrections-revue-guidee` (points 1-13 traités, blocs A-E).
2. `TASKS.md` : marquer les tâches (ids `T*-A`…`T*-G`) comme faites.
3. `VALIDATION.md` : consolider les checklists visuelles des `S<n>.md`.
4. `PROJECT_MAP.md` : refléter les évolutions (benefices-arret nav par silhouette ; plan-arret stratégie ;
   insuline-rapide modèle « couvrir » corrigé).
5. `DECISIONS.md` : consigner les décisions structurantes (dose habituelle fixe temps ① ; silhouette anatomique
   hotspot réutilisée sans wrapper diabète ; stratégie d'arrêt en mémoire).
6. `index.md` de ce plan + les `S<n>.md` : statuts à `[x]`.
7. Committer le contexte (`docs(...)`), puis **un seul `git push`** pour l'ensemble du plan.

### Validation
- Auto : `git status` propre ; `git log @{u}..` = l'ensemble des commits du plan ; push réussi.

### Message de commit
`docs(corrections-revue-guidee): consolidation contexte (STATUS/TASKS/VALIDATION/DECISIONS/PROJECT_MAP)`

### Statut
[ ] à faire · exécuté le : — · commit : —

---

## Fin de session

Dérouler `/fin-de-tache` (mode **solo** / fin de plan). Après push : purge éventuelle du dossier `plans/
corrections-revue-guidee/` selon la convention (historique → `git log`), une fois le contexte à jour.
