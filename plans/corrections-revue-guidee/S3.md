# S3 — Boîte à outils : retrait toggle grille + lien plan redondant   (rédigé par Opus)

> **Modèle : Sonnet · effort : low · Vague : 1 (parallèle : oui — zone `tabac/boite-a-outils/` + `content/tabac/outils.ts`)**
> Exécutant : UNIQUEMENT les tâches ci-dessous ; fichiers sous « Lire » / « Modifier ».
> Retraits ciblés — ne recâble rien d'autre. Doute ou blocage → STOP, signale, rends la main.

- Date : 2026-07-13 · Branche : —

## Lire (commun à la session)

- `src/features/tabac/boite-a-outils/BoiteAOutilsModule.tsx` (vue **grille** lignes ~203-238 ; vue **détail**
  lignes ~126-145 ; handler `toggleFiche`, `ficheItems`)
- `src/features/tabac/boite-a-outils/BoiteAOutilsModule.module.css` (classes `.tile`, `.tileCheck`,
  `.tileCheckActive`, `.tileCheckInput`, `.tileCheckIcon`)
- `src/content/tabac/outils.ts` (type `Outil`, champ `renvoi?` ligne 27 ; occurrences `renvoi` lignes 58, 83,
  194, 207)
- `rapport-ameliorations-etp-interactif.md` §C (points 7, 8)

## Hors périmètre

- **Garder** le bouton « Ajouter à ma fiche » / « Dans ma fiche » de la **vue détail** (lignes ~132-144) et le
  handler `toggleFiche` (encore utilisé par le détail).
- Ne pas modifier la logique de la fiche « Ma boîte à outils » ni `SelectionContext`.
- Ne pas toucher aux **autres** `renvoi` que ceux décidés en T2-C.

---

## T1-C — Retirer le toggle « Dans ma fiche » des cartes de la grille

### Objectif
Chaque carte de la grille porte un toggle `label.tileCheck` (« Dans ma fiche », checkbox). L'ajout étant **déjà
possible depuis la vue détail** (« Ajouter à ma fiche »), retirer ce toggle de la **grille** (point 7).

### Décision clé
`BoiteAOutilsModule.tsx` lignes **219-235** : le `<label className={styles.tileCheck}>…</label>` (checkbox +
icône `Circle`/`CheckCircle2` + texte « Dans ma fiche ») dans chaque `<div className={styles.tile}>`. Le retirer
entièrement ; la carte ne garde que son `<button className={styles.tileBtn}>` (illustration + titre + accroche →
ouvre le détail). `toggleFiche` reste utilisé par la vue détail → **ne pas** le supprimer.

### Lire / Modifier
- Modifier : `BoiteAOutilsModule.tsx` (retirer le `<label className={styles.tileCheck}>` de la grille + imports
  `Circle`/`CheckCircle2` si devenus orphelins).
- Modifier : `BoiteAOutilsModule.module.css` (retirer/neutraliser `.tileCheck*` si plus référencées).

### Étapes
1. Supprimer le bloc `<label className={styles.tileCheck}>…</label>` (lignes ~219-235).
2. Vérifier que `toggleFiche` reste appelé (vue détail) et que `ficheItems`/`dansLaFiche` de la grille ne laissent
   pas de variable morte (nettoyer si oui).
3. Nettoyer les imports d'icônes orphelins et le CSS orphelin.

### Validation
- Auto (bloque le commit) : `npx tsc --noEmit` · `npm run build` · `npm test` → verts.
- Humain (visuel) : plus de toggle « Dans ma fiche » sur les cartes de la grille ; l'ajout fonctionne toujours
  depuis la vue détail. → `VALIDATION.md`.

### Si bloqué
Si retirer le toggle laisse `toggleFiche`/`ficheItems` inutilisés côté grille ET référencés ailleurs de façon
ambiguë → STOP, décrire le couplage.

### Message de commit (appliqué en fin de plan, cf. §4d)
`refactor(tabac/boite-a-outils): retire le toggle « Dans ma fiche » de la grille`

### Statut
[x] fait · exécuté le : 2026-07-14 · commit : —

---

## T2-C — Retirer les deux renvois redondants vers le plan d'arrêt

### Objectif
Dans la vue détail d'un outil, les liens `renvoi` vers le plan d'arrêt (au-dessus de « Ajouter à ma fiche ») sont
redondants : ce qui est ajouté à la fiche est **déjà inclus** dans le plan d'arrêt. Retirer **les deux** (point 8,
tranché 2026-07-14).

### Décision clé
Les liens sont **pilotés par les données** : `src/content/tabac/outils.ts`, rendus par le bouton `renvoiBtn`
(`BoiteAOutilsModule.tsx` lignes ~116-124). **Deux** renvois pointent vers `plan-arret` :
- ligne **58** : `renvoi: { id: 'plan-arret', label: "Les inscrire dans mon plan d'arrêt" }` ;
- ligne **194** : `renvoi: { id: 'plan-arret', label: "Le préparer dans mon plan d'arrêt" }`.
Retirer **ces deux** clés `renvoi`. **Ne pas** toucher les renvois vers d'autres modules (lignes 83 `soulagement`,
207 `substituts`) ni le type `renvoi?` ni le rendu `renvoiBtn` (encore utilisés). `outils.ts` est **partagé** avec
l'app patient (couche `src/content/tabac/`) → vérifier l'impact.

### Lire / Modifier
- Modifier : `src/content/tabac/outils.ts` (retirer la clé `renvoi` des **deux** outils pointant vers `plan-arret`,
  lignes ~58 et ~194 ; conserver le champ optionnel `renvoi?` et les renvois non-plan).

### Étapes
1. Retirer la propriété `renvoi` des deux outils qui pointent vers `plan-arret` (lignes ~58 et ~194).
2. Vérifier que les renvois vers `soulagement` (L83) et `substituts` (L207) sont **intacts**.
3. **Vérifier l'app patient** : `src/patient/situations/PatientSituations.tsx` (ou équivalent « Agir face à une
   situation ») — s'assurer qu'elle ne rend/ne dépend pas de ces `renvoi` (elle ne devrait afficher que le contenu
   patient). Si elle les affichait, confirmer que le retrait est cohérent côté patient.

### Validation
- Auto (bloque le commit) : `npx tsc --noEmit` · `npm run build` (les **deux** entrées : consultation + patient)
  · `npm test` → verts.
- Humain (visuel) : dans le détail des deux outils concernés, plus de lien « Les inscrire… » / « Le préparer dans
  mon plan d'arrêt » ; le bouton « Ajouter à ma fiche » demeure ; renvois vers soulagement/substituts intacts ;
  app patient inchangée. → `VALIDATION.md`.

### Si bloqué
Si l'app patient rend ces `renvoi` de façon qui casserait à leur retrait → STOP, décrire le couplage patient.

### Message de commit (appliqué en fin de plan, cf. §4d)
`refactor(tabac/outils): retire les deux renvois redondants vers le plan d'arrêt`

### Statut
[x] fait · exécuté le : 2026-07-14 · commit : —

---

## Fin de session

Dérouler `/fin-de-tache` (mode **parallèle** : pas de commit/push, pas de fichiers partagés ; statut +
checklist visuelle dans ce `S3.md`).

### Bilan de session (2026-07-14)

- **T1-C** et **T2-C** : faites, validées en auto (`npx tsc --noEmit`, `npm run build` — deux entrées
  `index.html`/`patient.html` OK —, `npm test` → 95 tests verts). Aucun blocage rencontré ; aucune des deux
  conditions « Si bloqué » ne s'est déclenchée (grille : `toggleFiche`/`ficheItems` restent proprement
  utilisés côté vue détail après retrait du toggle ; app patient : `PatientSituations.tsx` ne rend/n'importe
  aucun `renvoi` — confirmé par grep, seule une mention en commentaire de doc l'exclut explicitement du
  périmètre patient v1 — donc retrait des deux `renvoi` sans impact patient).
- Fichiers modifiés : `src/features/tabac/boite-a-outils/BoiteAOutilsModule.tsx`,
  `src/features/tabac/boite-a-outils/BoiteAOutilsModule.module.css`, `src/content/tabac/outils.ts`.
  Diff vérifié : strictement dans le périmètre « Modifier » des deux tâches, rien d'autre.
- **Checklist visuelle (humain, en attente de validation Thibault)** :
  - [ ] T1-C — plus de toggle « Dans ma fiche » sur les cartes de la grille ; l'ajout à la fiche fonctionne
    toujours depuis la vue détail (bouton « Ajouter à ma fiche » / « Dans ma fiche »).
  - [ ] T2-C — dans le détail de « Mes plans SI…ALORS… » et « Si j'ai un écart — le plan de secours », plus
    de lien vers le plan d'arrêt ; le bouton « Ajouter à ma fiche » demeure ; renvois vers
    soulagement (outil-respiration) et substituts (outil-substituts) intacts ; app patient inchangée.
- Aucun fichier partagé du plan touché (`STATUS.md`/`TASKS.md`/`index.md`/`VALIDATION.md` non modifiés).
  Pas de commit/push — laissé à S7.
- Note : le working tree contient aussi des modifications de `src/features/tabac/substituts/SubstitutsModule.tsx`
  et `src/state/SelectionContext.tsx` constatées lors du diff, non produites par cette session (vague
  parallèle — autres sessions S1/S2/S4-S7 sur le même dépôt) ; non touchées ici.
