# S4 — Plan d'arrêt : sélecteur de stratégie (arrêt complet / réduction)   (rédigé par Opus)

> **Modèle : Opus · effort : high · Vague : 1 (parallèle : oui — zone `tabac/plan-arret/` + `state/SelectionContext.tsx`)**
> Exécutant : UNIQUEMENT la tâche ci-dessous ; fichiers sous « Lire » / « Modifier ».
> Feature + contenu clinique : tout libellé de plan proposé est `// à revalider (Thibault)`. Doute clinique → STOP, signale plutôt qu'inventer (invariant 5).

- Date : 2026-07-13 · Branche : —

## Lire (commun à la session)

- `src/features/tabac/plan-arret/PlanArretModule.tsx` (section « 1. Ma date » lignes ~153-167 ; usage de
  `useSelection()` : `state.dateArret`, `setDate`)
- `src/state/SelectionContext.tsx` (forme du `state`, actions/reducer, valeurs par défaut — pour ajouter un champ
  `strategie` **en mémoire**)
- `src/features/tabac/plan-arret/livretSections.tsx` (`buildLivretSections(state)` — **lecture seule** : vérifier
  que l'ajout du champ `strategie` au state ne le casse pas ; livret **non modifié** en v1)
- `docs/contenu-modules-tabac.md` (registre/ton du module plan-arret) · `PROJECT_BRIEF.md`
- `rapport-ameliorations-etp-interactif.md` §D (point 9)

## Hors périmètre

- **Zéro persistance** : la stratégie vit dans `SelectionContext` (React, en mémoire), **jamais** localStorage.
- Ne **pas** inventer de protocole clinique (paliers chiffrés, calendrier de réduction) : le défaut est
  d'adapter des **libellés**, pas de générer un plan médical (cf. index §À trancher point 9).
- Ne pas modifier l'app patient ni les autres sections du plan.

## Décision clé (à confirmer par Thibault — cf. index §À trancher point 9)

« Arrêt complet » et « Réduction progressive » sont **deux stratégies également valables**. Portée v1 tranchée
2026-07-14 : **libellés seuls** (aucune génération de protocole/paliers, aucun changement de contenu du livret).
1. **State** : ajouter `strategie: 'complet' | 'progressive' | null` au `SelectionContext` (défaut `null`), avec
   son action (ex. `setStrategie`). Additif — ne casse ni le livret ni le reset.
2. **UI** : dans la section « 1. Ma date », **au-dessus** de l'`input date`, un sélecteur 2 options (même grammaire
   visuelle que les chips du module : `role="radiogroup"`, cible ≥ 44 px).
3. **Adaptation des libellés uniquement** (pas de calcul, pas de dates intermédiaires) : le libellé/aide de la date
   s'ajuste au choix — ex. « Ma date d'arrêt » (complet) vs « Ma date de début de réduction » (progressive) ; titre
   de section éventuellement « 1. Ma stratégie et ma date ». Tout texte clinique nouveau → `// à revalider (Thibault)`.
4. **Livret : inchangé en v1.** Ne pas toucher `buildLivretSections` ni `PrintableLivret` (la stratégie ne modifie
   pas le contenu du livret pour l'instant).

---

## T1-D — Sélecteur de stratégie dans « 1. Ma date »

### Objectif
Permettre de choisir entre **Arrêt complet** et **Réduction progressive** dans la section « 1. Ma date », et
adapter les **libellés** du plan selon le choix (point 9).

### Lire / Modifier
- Modifier : `src/state/SelectionContext.tsx` (champ `strategie` + action + défaut + reset).
- Modifier : `PlanArretModule.tsx` (sélecteur 2 options ; libellés conditionnels de la section date).
- **Ne pas** modifier `livretSections.tsx` / `PrintableLivret` (livret inchangé en v1).

### Étapes
1. Étendre le `state` du contexte avec `strategie` (défaut `null`) + l'action correspondante ; l'inclure dans le
   `reset` (retour à `null`).
2. Rendre le sélecteur 2 options en tête de la section « 1. Ma date » (`radiogroup`), câblé sur le contexte.
3. Conditionner libellé de section + aide de la date au choix (textes `// à revalider (Thibault)`) — **libellés
   uniquement**, aucune date/palier calculé.
4. Vérifier que sans choix (`null`) le rendu reste celui d'aujourd'hui (rétrocompatibilité) et que le livret est
   inchangé.

### Validation
- Auto (bloque le commit) : `npx tsc --noEmit` · `npm run build` · `npm test` → verts (ajuster les tests du
  contexte si sa forme est testée).
- Humain (visuel) : le sélecteur 2 options apparaît, le choix ajuste les libellés de la section date ; l'état
  survit à la navigation inter-modules et se réinitialise au rechargement (mémoire). → `VALIDATION.md`.

### Si bloqué
Si l'adaptation du **contenu** (au-delà des libellés) est attendue mais non spécifiée cliniquement → STOP, poser
la question du protocole « réduction progressive » à Thibault (ne pas inventer).

### Message de commit (appliqué en fin de plan, cf. §4d)
`feat(tabac/plan-arret): choix de stratégie (arrêt complet / réduction progressive)`

### Statut
[x] fait · exécuté le : 2026-07-14 · commit : —

Validation auto : `npx tsc --noEmit` ✅ · `npm run build` ✅ (relancé une fois — collision
transitoire avec une session parallèle sur `benefices-arret/`, hors zone) · `npm test` ✅ 95/95.
Validation humaine (visuel) : **en attente de Thibault** — vérifier que le sélecteur 2 options
(radiogroup) apparaît en tête de « 1. Ma date », que le choix ajuste le titre de section + le
libellé/aide de la date, que l'état survit à la navigation inter-modules et se réinitialise au
rechargement (mémoire). Libellés cliniques annotés `// à revalider (Thibault)`.

---

## Fin de session

Dérouler `/fin-de-tache` (mode **parallèle** : pas de commit/push, pas de fichiers partagés ; statut +
checklist visuelle dans ce `S4.md`).
