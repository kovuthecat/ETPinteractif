# extensions-tabac · X6 — Portes de fin de module + fil rouge + 2ᵉ niveau   (rédigé par Fable)

> **Modèle : Sonnet · effort : high · Vague : 3 (parallèle : non — touche presque tous les modules, après X2-X5)**
> Exécutant : UNIQUEMENT les tâches ci-dessous, dans l'ordre ; fichiers sous « Lire » / « Modifier ».
> Design fixé par `docs/BRIEF_TABAC.md §3.3, §3.4, §3.5` — les tables y sont normatives. Doute → STOP, signale.

- Date : 2026-07-09 · Branche : — (repo local, main) · Dépend de : **X1-X5 faits** (les portes pointent `plan-arret`, le fil rouge va aux pieds de fiches)

## Lire (commun à la session)
- `docs/BRIEF_TABAC.md` §3.3 (table des portes), §3.4 (fil rouge + emplacements), §3.5 (table 2ᵉ niveau) — **autorité**.
- `src/features/tabac/nicotine-toxique/NicotineToxiqueModule.tsx` + son CSS (le bloc nav existant à extraire).
- `src/features/tabac/nicotine/NicotineModule.tsx` (pattern tooltip de zones à extraire).
- `src/features/types.ts`, `src/components/Home.tsx`, `src/features/registry.ts` (définition du thème pour l'exergue).

## Hors périmètre
- Ne pas modifier la logique interne des modules (courbes, états, phases) — on ajoute en périphérie.
- Ne pas ajouter de porte à Addiction ni à Plan d'arrêt (décision brief §3.3).

---

## T1 — Composant `ModuleFooterNav` + migration Nicotine ≠ toxique

### Objectif
Extraire le bloc « Continuer l'exploration » en composant générique réutilisable.

### Décisions clés
- **Créer** `src/components/ModuleFooterNav.tsx` (+ `.module.css`) — générique :
  `{ titre?: string; items: { id: string; label: string }[]; onNavigate: (id) => void }`,
  `titre` par défaut « Continuer l'exploration », rendu = celui du bloc actuel de Nicotine ≠ toxique
  (reprendre ses styles ; `<nav aria-label="Aller plus loin">`).
- Migrer Nicotine ≠ toxique dessus **sans changement visuel** (mêmes libellés, mêmes cibles).

### Validation
- Auto : `npx tsc --noEmit` + `vite build` → verts.

### Commit
`refactor(moteur): ModuleFooterNav générique, extrait de nicotine-toxique`

### Statut
[x] fait · exécuté le : 2026-07-09 · commit : —

---

## T2 — Câbler les portes (5 modules)

### Objectif
Poser `ModuleFooterNav` en pied des modules listés au brief §3.3, avec les ids/libellés **de la table, verbatim**.

### Lire / Modifier
- Modifier : `nicotine/NicotineModule.tsx`, `soulagement/SoulagementModule.tsx`,
  `craving/CravingModule.tsx`, `substituts/SubstitutsModule.tsx`, `motivation/MotivationModule.tsx`.

### Étapes
1. Chaque module reçoit déjà `ModuleProps.onNavigate` — le brancher (Craving et Motivation
   l'ignorent actuellement via `_` : renommer le paramètre).
2. Placement : tout en bas du module, après le contenu existant (pour Craving : visible dans les
   3 phases, sous l'aparté 39 89).

### Validation
- Auto : `npx tsc --noEmit` + `vite build` + `npx vitest run` → verts.
- Humain : chaque porte navigue au bon module, retour OK → `VALIDATION.md §X6`.

### Commit
`feat(tabac): portes de fin de module (nicotine, soulagement, craving, substituts, motivation)`

### Statut
[x] fait · exécuté le : 2026-07-09 · commit : —

---

## T3 — Fil rouge

### Objectif
Installer le refrain du brief §3.4 : exergue d'accueil + clôture des 4 modules Comprendre + pieds de fiches.

### Décisions clés
- Libellé **verbatim brief §3.4** : « C'est la fumée qui rend malade. C'est le manque qui fait fumer. Et le manque, ça se traite. »
- `types.ts` : champ optionnel `exergue?: string` sur `ThemeDef` (générique — ne rien câbler de tabac dans le moteur).
- `Home.tsx` : si `theme.exergue`, le rendre sous le titre (une ligne Source Serif italique, `--color-text-soft`).
- `global.css` : classe `.filrouge` (serif italique, filet gauche fin `--color-line`, marge haute) —
  posée en dernière ligne des modules Addiction, Nicotine, Soulagement. Sur **Nicotine ≠ toxique**,
  remplacer le paragraphe « À retenir » actuel par la même ligne `.filrouge` (message harmonisé).
- Pieds de fiches : passer le refrain comme `footer` de chaque `FicheOverlay` existant
  (craving X2, substituts X3, motivation X4, plan-arret X5) — texte simple, petite taille.

### Lire / Modifier
- Modifier : `src/features/types.ts`, `src/components/Home.tsx`, `src/features/registry.ts`
  (définition du thème tabac : ajouter `exergue`), `src/styles/global.css`, les 4 modules Comprendre,
  les 4 appels `FicheOverlay`.

### Validation
- Auto : `npx tsc --noEmit` + `vite build` + `npx vitest run` → verts.
- Humain : le refrain se lit à l'accueil et en clôture sans lourdeur → `VALIDATION.md §X6`.

### Commit
`feat(tabac): fil rouge « fumée / manque / ça se traite » (accueil, clôtures, fiches)`

### Statut
[x] fait · exécuté le : 2026-07-09 · commit : —

---

## T4 — 2ᵉ niveau de lecture (`InfoHover`)

### Objectif
Généraliser le pattern tooltip du module Nicotine ; câbler **uniquement** les contenus du brief
§3.5 que Thibault aura validés au moment de l'exécution.

### Décisions clés
- **Créer** `src/components/InfoHover.tsx` (+ `.module.css`) — générique : un déclencheur
  (children) qui affiche un panneau au survol **et** au focus clavier (reprendre le comportement
  `onMouseEnter/onFocus` + `role`/`tabIndex` des tooltips de zones du NicotineModule ; positionnement
  simple au-dessus/au-dessous suffit).
- **Vérifier dans `docs/BRIEF_TABAC.md` §5** (ou demander) quelles entrées de la table §3.5 sont
  validées. **Non validée = non câblée.** Si aucune : livrer le composant seul et le signaler —
  ce n'est pas un échec de session.
- Ne pas migrer les tooltips de zones existants du NicotineModule (déjà fonctionnels — hors périmètre).

### Lire / Modifier
- Créer : `src/components/InfoHover.tsx`, `InfoHover.module.css`.
- Modifier (selon validation) : `substituts/SubstitutsModule.tsx`, `nicotine/NicotineModule.tsx`,
  `craving/CravingModule.tsx`.

### Validation
- Auto : `npx tsc --noEmit` + `vite build` + `npx vitest run` → verts.
- Humain : survol + Tab → panneau ; jamais bloquant pour le clic principal → `VALIDATION.md §X6`.

### Commit
`feat(tabac): composant InfoHover + 2ᵉ niveau de lecture (entrées validées)`

### Statut
[x] fait · exécuté le : 2026-07-09 · commit : — · **composant `InfoHover` livré seul, non câblé** :
§5 du brief ne marque aucune des 3 entrées §3.5 comme validée → aucun contenu câblé (comportement
attendu, cf. décision clé ci-dessus).

## Fin de session
Dérouler `/fin-de-tache` (mode **solo**).
