# extensions-tabac · X4 — Fiche « Mes raisons » (Motivation)   (rédigé par Fable)

> **Modèle : Sonnet · effort : medium · Vague : 2 (parallèle : oui — avec X2, X3, X5 ; fichiers disjoints)**
> Exécutant : UNIQUEMENT les tâches ci-dessous ; fichiers sous « Lire » / « Modifier ».
> Design fixé par `docs/BRIEF_TABAC.md §3.1` (ligne « Mes raisons »). Doute → STOP, signale.

- Date : 2026-07-09 · Branche : — (repo local, main) · Dépend de : **X1 fait**

## Lire
- `docs/BRIEF_TABAC.md` §3.1 — **autorité**.
- `src/components/FicheOverlay.tsx` + section « Fiches » de `src/styles/global.css`.
- `src/features/tabac/motivation/MotivationModule.tsx` (états `raisonsBoard`, `importance`,
  `confiance`, `importanceTouched`, `confianceTouched`, `etape`).

## Hors périmètre
- Ne pas modifier le cadran, le flux 2 questions ni le tableau ; ne toucher que `src/features/tabac/motivation/**`.

---

## T1 — Bouton + contenu de la fiche

### Objectif
Imprimer les raisons posées sur le tableau (+ Importance/Confiance si le patient a répondu).

### Décisions clés
- **Catégorie : photographie** — chaleureuse, personnelle. Ton : ce sont *ses* mots, on ne reformule rien.
- Contenu de la feuille (ordre) :
  1. Les cartes du tableau : libellé en gras + détail personnel en dessous (si renseigné), chacune
     avec sa pastille de couleur (reprendre `carte.color`) — liste verticale aérée, pas de
     reproduction spatiale du tableau ;
  2. si `importanceTouched` **ou** `confianceTouched` : bloc « Où j'en suis » — Importance N/10 ·
     Confiance N/10 (n'imprimer que les valeurs touchées) ;
  3. pas de bandeau 39 89 ici (fiche intime, pas fiche d'action) — le pied standard suffit.
- Titre : « Mes raisons d'arrêter » ; eyebrow : « PROGRAMME ETP · SEVRAGE TABAGIQUE ».
- Bouton « Imprimer mes raisons » (`.btn .btn--ghost`) : dans l'onglet « Mes raisons », visible
  seulement si `raisonsBoard.length > 0` ; également sur l'écran de synthèse (étape 2) s'il y a des cartes.

### Lire / Modifier
- Modifier : `src/features/tabac/motivation/MotivationModule.tsx`, `MotivationModule.module.css`.

### Étapes
1. État `ficheOpen` + les deux points d'entrée du bouton.
2. Composer la feuille depuis `raisonsBoard` (ordre d'ajout, stable).
3. Footer de fiche : vide (fil rouge ajouté en X6).

### Validation
- Auto : `npx tsc --noEmit` + `vite build` + `npx vitest run` → verts.
- Humain : Ctrl+P → 1 page, cartes + éventuels scores fidèles à l'écran → `VALIDATION.md §X4`.

### Si bloqué
- Plus de ~12 cartes : laisser couler sur 2 pages (ne pas tronquer), signaler.

### Commit
`feat(motivation): fiche imprimable « Mes raisons » (cartes + échelles si renseignées)`

### Statut
[x] fait · exécuté le : 2026-07-09 · commit : —

---

## Session — validations & bilan (consigné ici, mode vague parallèle)

### Validations auto

- `npx tsc --noEmit` → OK, aucune erreur.
- `npx vite build` → OK, build réussi (1804 modules, `dist/` généré).
- `npx vitest run` → OK, 20/20 tests passés (1 fichier).

### Validation humaine — à faire (non vérifiable par l'agent, pas de navigateur)

- [ ] Onglet « Mes raisons » : bouton « Imprimer mes raisons » visible uniquement si au moins une
  carte est sur le tableau ; invisible tableau vide.
- [ ] Écran de synthèse (étape 2, onglet « Où en êtes-vous ? ») : bouton « Imprimer mes raisons »
  visible seulement si des cartes existent sur le tableau (indépendant de l'onglet actif).
- [ ] Ctrl+P sur la fiche → tient sur 1 page pour un nombre usuel de cartes (jusqu'à ~8-10) ;
  pastille de couleur de chaque carte fidèle à celle du tableau ; libellé en gras + détail
  personnel en dessous si renseigné ; ordre = ordre d'ajout (stable, pas de reproduction spatiale).
- [ ] Bloc « Où j'en suis » : n'apparaît que si Importance et/ou Confiance ont été manipulées
  (`importanceTouched` / `confianceTouched`) ; n'affiche que la ou les valeurs touchées.
- [ ] Pied de fiche : uniquement la mention standard « Fiche générée en consultation — rien n'est
  enregistré. » (pas de bandeau 39 89, footer vide comme prévu).
- [ ] Eyebrow « PROGRAMME ETP · SEVRAGE TABAGIQUE » et titre « Mes raisons d'arrêter » corrects.
- [ ] Cas > 12 cartes (signalé dans « Si bloqué ») : laisser couler sur 2 pages, vérifier qu'aucun
  contenu n'est tronqué à l'impression.

### Bilan

- Fichiers modifiés (périmètre `src/features/tabac/motivation/**` uniquement, comme prévu) :
  - `src/features/tabac/motivation/MotivationModule.tsx`
  - `src/features/tabac/motivation/MotivationModule.module.css`
- Aucun fichier hors périmètre touché (le diff préexistant M1 sur d'autres fichiers du dépôt n'a pas
  été modifié par cette session).
- Aucun blocage rencontré ; les décisions clés du brief ont été suivies à la lettre (catégorie
  photographie, aucune reformulation des mots du patient, pas de bandeau 39 89, footer vide).
- Aucun commit ni push effectué (laissé pour la fin du plan complet).

## Fin de session
Dérouler `/fin-de-tache` (mode **vague parallèle**).
