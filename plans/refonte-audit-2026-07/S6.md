# refonte-audit-2026-07 · S6 — Cardio M6 « Le tabac » : ré-enrichir le mécanisme CV (contenu d'abord)   (rédigé par Opus)

> **Modèle : Opus · effort : high · Vague : 1 (parallèle : oui)**
> Exécutant : UNIQUEMENT la tâche ci-dessous ; fichiers sous « Lire » / « Modifier ».
> **Gate contenu G-A8 : valider le mécanisme clinique AVANT tout code.** Ne pas inventer de mécanisme.
> Doute clinique ou blocage → STOP, signale, rends la main.

- Date : — · Branche : —

## Contexte (constat audit + décision Thibault, 2026-07-23)

**Cardio M6 « Le tabac »** a été réduit à une **bascule 2 états** (Fumeur / Arrêté) après le retrait de
la frise de réversibilité (décision 2026-07-23). Il ne reste **presque plus rien à manipuler** :
cliquable, pas interactif — l'audit le classe « faible / candidat à refonte ». **Décision Thibault
(2026-07-23)** : **ré-enrichir** le module avec un objet qui **explique et illustre comment le tabac
abîme les artères** — pas restaurer la frise retirée, mais **montrer le mécanisme CV**.

## Objectif

Un module M6 **démonstratif et manipulable** du **mécanisme** par lequel le tabac agresse l'artère :
agression de la paroi (dysfonction endothéliale), **vasoconstriction / spasme**, accélération de la
**plaque**, sur-risque de **thrombose** — et la **réversibilité** à l'arrêt. Le patient doit comprendre
*pourquoi* le tabac est un facteur CV majeur, pas seulement *qu'*il l'est. Réutiliser l'**artère héros**
partagée du thème.

## Gate contenu G-A8 (mini-G1) — À FAIRE AVANT LE CODE

1. Extraire de `docs/cardio/CONTENU_cardio.md` §M6 (+ evidence `docs/cardio/evidence-cardio/` si présent)
   le mécanisme CV du tabac : **quels effets, dans quel ordre, avec quelle réversibilité**, en registre
   patient (pas de jargon, pas de chiffres imposés — cohérent avec les invariants du thème).
2. Si le doc **ne couvre pas** le mécanisme au niveau requis : **écrire une proposition de contenu** dans
   `docs/cardio/CONTENU_cardio.md` §M6, marquée `// à revalider (Thibault)`, et **s'arrêter pour
   validation** avant de coder l'objet. Ne pas inventer de mécanisme non sourcé.
3. Confirmer qu'aucune aspirine et aucun chiffre de seuil ne sont introduits (invariants G1 du thème).

## Lire

- `src/features/cardio/tabac/` (module M6 actuel — bascule 2 états + lien vers thème Tabac)
- `docs/cardio/CONTENU_cardio.md` §M6 (autorité contenu)
- `docs/cardio/` evidence (rapports OpenEvidence) si disponibles
- `src/features/cardio/artere/` + `cardio/components/` (artère héros partagée — réutiliser)
- `src/features/tabac/` (le thème Tabac complet existe — le **lien** vers lui reste, ne pas le dupliquer)

## Hors périmètre

- Ne PAS restaurer la frise de réversibilité retirée (décision 2026-07-23) — nouvel objet mécanisme.
- Ne PAS dupliquer le thème Tabac ; conserver le **pont** (« → Thème Tabac ») en complément.
- Ne PAS introduire aspirine, seuils chiffrés, ni dépendance runtime. Commit/push : non (S9).
- Ne pas coder l'objet tant que **G-A8** n'est pas résolue (contenu validé ou proposé + STOP).

## Spécification indicative (après gate contenu)

- Objet central : l'**artère héros** partagée, sur laquelle on **montre le mécanisme** — p. ex. un
  toggle/curseur **Fumeur → Arrêté** qui, au lieu d'une simple barre 2 états, fait apparaître
  successivement paroi agressée → spasme/rétrécissement → plaque accélérée → risque de caillot, puis la
  **réversibilité** à l'arrêt (l'artère se détend / le sur-risque reflue).
- Registre non anxiogène, message positif à l'arrêt (« dès l'arrêt, le cœur commence à se réparer »
  conservé).
- Toute formulation nouvelle `// à revalider (Thibault)`.

### Validation
- Auto : `npx tsc --noEmit` + `npm run build` + `npx vitest run` → OK.
- Humain (→ `VALIDATION.md`) : à `npm run dev`, cardio → M6 ; manipuler l'objet, vérifier que le
  **mécanisme** est lisible et compris (pas une simple bascule), la réversibilité présente, le pont vers
  le thème Tabac conservé, le ton non anxiogène, la lisibilité 1 m. **Validation clinique du mécanisme
  par Thibault indispensable** (G-A8).

### Si bloqué
Le doc/evidence ne permet pas de fonder le mécanisme, ou l'artère partagée ne s'y prête pas → STOP,
livrer la **proposition de contenu** (doc) sans coder l'objet, rendre la main pour validation.

### Message de commit
`feat(cardio): M6 « le tabac » — objet demonstratif du mecanisme CV (re-enrichissement)`
(+ commit contenu séparé si `docs/cardio/CONTENU_cardio.md` modifié)

### Statut
[!] **bloqué gate contenu G-A8 — proposition de contenu livrée, code NON écrit** (2026-07-24).
Le mécanisme CV du tabac est **sourcé** (Socle §E.1/E.2) mais **absent du §M6** de
`CONTENU_cardio.md` au niveau requis (paroi + spasme non couverts en registre patient). Proposition
de ré-enrichissement écrite dans `docs/cardio/CONTENU_cardio.md` §M6, marquée `// à revalider
(Thibault)`. STOP câblage tant que les 5 formulations ne sont pas validées cliniquement (G-A8).

## Bilan de session

**Résultat : gate G-A8 bloquante → proposition de contenu livrée, objet interactif NON codé.**

### Mécanisme extrait / proposé
Le §M6 de `docs/cardio/CONTENU_cardio.md` (autorité de contenu, écran) ne portait que **2 des 5
étapes** du mécanisme demandé : « accélère le bouchage » + « favorise le caillot ». Manquaient au
niveau patient : **agression de la paroi (dysfonction endothéliale)** et **vasoconstriction /
spasme**. La refonte S6 vise précisément un objet qui *montre le mécanisme* — coder les 5 étapes
aurait affiché au patient des formulations cliniques non validées → gate bloquante.

Les 5 étapes SONT **sourcées** (donc pas d'invention), mais dans le rapport evidence, en jargon :
- **Socle §E.1** (`evidence-cardio/2026-07-21-rapport-openevidence-cardio-socle.md`, ligne 108) :
  « Dysfonction endothéliale, activation plaquettaire, ↑ fibrinogène, stress oxydatif, inflammation,
  spasme coronaire → accélère l'athérosclérose + favorise la thrombose aiguë. » [réf. 22]
- **Socle §E.2** (lignes 112-114) : réversibilité — surrisque coronaire −½ dès la 1ʳᵉ année, rejoint
  le non-fumeur en ~15 ans (coronaire), 5-15 ans (AVC). [réf. 23-24]

### Proposition livrée
Séquence patient (registre non anxiogène, zéro chiffre à l'écran) écrite dans §M6, bloc
`// PROPOSITION DE RÉ-ENRICHISSEMENT — mécanisme CV, à revalider (Thibault)` :
1. Agression de la paroi (E.1) — « la paroi intérieure de l'artère s'irrite et s'abîme ».
2. Vasoconstriction / spasme (E.1) — « l'artère se serre » *(étape neuve, absente du §M6)*.
3. Accélération de la plaque (E.1) — « la plaque se dépose et grossit plus vite ».
4. Thrombose (E.1) — « le sang devient plus épais, plus collant : un caillot peut boucher ».
5. Réversibilité (E.2) — « dès l'arrêt, l'artère se détend et le cœur commence à se réparer ».

### Choix pris
- **Traduction jargon → patient** : dysfonction endothéliale → « paroi qui s'irrite/s'abîme » ;
  spasme coronaire → « l'artère se serre » ; activation plaquettaire/fibrinogène → « sang plus épais,
  plus collant ». Ce sont des choix de formulation clinique → chacun `// à revalider (Thibault)`.
- **Ordre** : paroi → spasme → plaque → caillot → réversibilité (physiopathologie E.1, puis espoir).
- **Invariants respectés dans la proposition** : aucun chiffre à l'écran (E.2 reste hors écran) ;
  aucune aspirine ; tabac binaire (pas de palier) ; pont « → Thème Tabac » conservé, non dupliqué ;
  pas de restauration de la frise retirée (nouvel objet, conforme à la décision 2026-07-23).
- **Pas de code écrit** : `src/features/cardio/tabac/` non modifié. Modules partagés
  (`cardio/artere/`, `cardio/components/`) lus uniquement (moteur/artère héros réutilisables au
  câblage futur), non modifiés. `src/features/tabac/` non touché (pont informatif).

### Validations auto (sur l'unique modif — doc `.md`)
- `npx tsc --noEmit` → OK (aucune erreur).
- `npm run build` → OK (built in ~2.3s).
- `npx vitest run` → OK (127 tests passés, 4 fichiers).

### Point bloquant à trancher par Thibault (validation CLINIQUE indispensable — G-A8)
Valider (ou corriger) les **5 formulations patient** du bloc proposé au §M6. Une fois validées
(passage `// à revalider` → validé), le câblage de l'objet interactif (artère héros + séquence
Fumeur→Arrêté) pourra être fait dans une session ultérieure. La **checklist de validation visuelle
humaine n'est PAS ajoutée** ici : aucun code n'a été livré.

## Fin de session
Dérouler `/fin-de-tache` (mode parallèle : oui — ne pas toucher STATUS/TASKS/index/VALIDATION, pas de push).
