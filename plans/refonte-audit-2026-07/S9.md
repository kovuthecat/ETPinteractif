# refonte-audit-2026-07 · S9 — Consolidation   (rédigé par Opus)

> **Modèle : Haiku · effort : minimal · Vague : finale (après toutes les sessions)**
> Exécutant : consolidation uniquement — pas de nouveau code fonctionnel.

- Date : — · Branche : —

## Objectif

Clore le chantier : commits atomiques, statuts, fichiers de contexte, un seul push, vérif redeploy.

## Étapes (WORKFLOW §4d du template)

1. **Vérif finale sur l'arbre cumulé** : `npx tsc --noEmit` + `npm run build` + `npx vitest run` → tout
   vert. Consigner le compte de tests.
2. **Commits atomiques** — un par tâche/session réellement exécutée (S1, S2, S3, S4, S5, S6, S7 ; S8 si
   débloquée), messages fournis dans chaque fiche. Staging explicite (pas de `git add -A` aveugle).
   Regrouper si deux sessions ont touché le même fichier partagé (documenter le message composé).
3. **Statuts** : cocher les sessions faites dans `index.md` (ce dossier) et `TASKS.md`.
4. **`STATUS.md`** : bloc de synthèse du chantier (ce qui marche, points `// à revalider (Thibault)`,
   gates tranchées vs en attente). Rappeler que **S8 reste BLOQUÉE** si les PNG ne sont pas livrés, et
   que **G-Suivi / G-M10-nausées / G-M7-taille** restent des décisions produit/clinique ouvertes.
5. **`VALIDATION.md`** : consolider toutes les checklists de validation visuelle humaine des sessions
   (aucune vérif navigateur n'est faite côté Claude — règle projet).
6. **`DECISIONS.md`** : consigner les gates tranchées (A1 layout, A4 feedback, A5 payoff M9, A6a tooltip,
   A7 refonte M3, A8 ré-enrichissement M6 + mécanisme validé, A10 rétro-port) et les décisions en attente.
7. **`PROJECT_MAP.md`** : mettre à jour si un composant a été factorisé (ex. barre de risque partagée en
   `src/components/`, patron de layout partagé).
8. **Push unique** vers `origin/main` puis **vérifier le redeploy Vercel** (les deux entrées
   `consultation.html` / `patient.html`).
9. **Purge** : ce dossier `plans/refonte-audit-2026-07/` pourra être purgé lors d'un futur ménage une
   fois la synthèse dans `STATUS.md` (comme les chantiers clos précédents) — **ne pas purger ici**.

## Rappels (roadmap capturée, non exécutée)

- **A9** — micro-interaction Cardio M1 (optionnel).
- **G-Suivi** — pré-cochage des mois passés (`statusForMonth` → `'fait'`) : décision produit à trancher.
- **G-M10-nausées** — « nausées isolées » comme signe d'infarctus : décision clinique Thibault.
- **G-M7-taille** — « tour de taille » comme bénéfice d'activité vs son retrait de M2 : cohérence à confirmer.

## Statut
[ ] à faire

## Bilan de session
*(à remplir)*
