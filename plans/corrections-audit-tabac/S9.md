# S9 — Accueil tabac : ordre des familles « Se motiver → Comprendre → Agir »   (rédigé par Opus)

> **Modèle : Sonnet · effort : minimal · Vague : 1 (parallèle : oui — fichier `features/registry.ts` uniquement)**
> Exécutant : UNIQUEMENT la tâche ci-dessous. ⚠️ Fichier **moteur partagé** — ne toucher QUE l'ordre des
> familles du thème tabac. Doute → STOP, rends la main.

- Date : 2026-07-13 · Branche : —

## Lire (commun à la session)

- `src/features/registry.ts` (`THEMES`, tableau `familles` de chaque `ThemeDef`)
- `src/components/Home.tsx` (rend les familles dans l'ordre du tableau `ThemeDef.familles`)
- `rapport-bugs-etp-tabac.md` §T15

## Hors périmètre

- **Ne pas toucher au thème Diabète** (familles Comprendre / Agir au quotidien / Se soigner — ordre différent, à préserver).
- Ne pas modifier les `famille` des modules dans `tabac/registry.ts` (juste l'ordre d'affichage des sections).

---

## T15 — Réordonner les familles du thème tabac

### Objectif

Sur l'accueil du module Sevrage tabagique, afficher **Se motiver → Comprendre → Agir** (au lieu de Comprendre →
Agir → Se motiver).

### Décision clé (confirmée Thibault)

L'ordre vient de `THEMES[0].familles` (thème `tabac`) dans `registry.ts` (l.13-17). `Home.tsx` rend les sections
dans l'ordre de ce tableau. Réordonner uniquement ce tableau.

### Lire / Modifier

- Modifier : `src/features/registry.ts` — tableau `familles` du thème `tabac` :

```ts
familles: [
  { id: 'motivation', label: 'Se motiver' },
  { id: 'comprendre', label: 'Comprendre' },
  { id: 'agir', label: 'Agir' },
],
```

### Étapes

1. Déplacer l'entrée `{ id: 'motivation', label: 'Se motiver' }` en **tête** du tableau `familles` du thème tabac.
2. Ne rien changer d'autre (thème diabète intact).

### Validation

- Auto : `tsc` · `build` · `test` verts.
- auto OK (tsc/build/test verts) - en attente de validation visuelle par Thibault (npm run dev)

### Message de commit

`feat(tabac/accueil): ordre des familles « Se motiver → Comprendre → Agir »`

### Statut

[x] fait

---

## Fin de session

Dérouler `/fin-de-tache` (mode **parallèle** ; statut + checklist visuelle dans ce `S9.md`).
