# S4 — Composantes : cercle « Comportementale » + légende redondante   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 1 (parallèle : oui — zone `tabac/addiction/` uniquement)**
> Exécutant : UNIQUEMENT la tâche ci-dessous. Design fixé — ne reconçois pas. Doute → STOP, rends la main.

- Date : 2026-07-13 · Branche : —

## Lire (commun à la session)

- `src/features/tabac/addiction/AddictionModule.tsx` (SVG inline du diagramme de Venn + puces de situations)
- `src/features/tabac/addiction/AddictionModule.module.css` (`.circleShape`, `.situationChip`, `.legend`, `.legendItem`)
- `rapport-bugs-etp-tabac.md` §T1

## Hors périmètre

- Ne pas toucher à la logique de sélection radiale des situations (refonte BO3) ni au CTA « Stratégies & outils ».

---

## T1 — Anti-débordement du cercle « Comportementale » + suppression de la légende

### Objectif

Le cercle « Comportementale » (bas) déborde et **chevauche** la légende et des puces de situations. La légende du
bas est par ailleurs **redondante** (chaque cercle porte déjà son nom écrit dedans). Corriger le chevauchement et
supprimer la légende.

### Décision clé

Géométrie actuelle (`viewBox="0 0 600 460"`, cercles `r=130`) : Physique (cx210,cy160), Psychologique
(cx390,cy160), **Comportementale (cx300, cy300)** → bord bas `y=430`, à 30 px du bas (460). Deux leviers :
remonter le cercle et/ou agrandir le viewBox. La légende à retirer est le bloc `.legend` (3 `.legendItem`).

### Lire / Modifier

- Modifier : `AddictionModule.tsx` (coordonnées SVG + suppression du bloc `.legend`), `.module.css` si nécessaire.

### Étapes

1. Stopper le chevauchement : remonter « Comportementale » (`cy≈250–270`) et/ou réduire légèrement les rayons,
   **ou** augmenter la hauteur du viewBox (ex. `0 0 600 520`) + réserver un padding bas pour les puces.
2. Ajuster le label interne `<text>` « Comportementale » (actuellement 300,325) en cohérence.
3. **Supprimer** le bloc `.legend` (les 3 `.legendItem`) — l'info est déjà dans les cercles ; cela dégage aussi de l'espace.
4. Vérifier le responsive (le débordement peut varier selon la largeur).

### Validation

- Auto : `tsc --noEmit` OK, `npm test` OK (95 tests). `npm run build` échoue mais uniquement à cause d'une erreur
  TS dans `src/features/tabac/motivation/MotivationModule.tsx` (fichier hors périmètre, en cours de modification
  par un autre agent en parallèle — `git status` le montre modifié, non commité). Aucune erreur dans les fichiers
  de cette tâche.
- auto OK (tsc/build/test verts) - en attente de validation visuelle par Thibault (npm run dev)

### Si bloqué

Choix retenu : agrandissement du viewBox (`0 0 600 460` → `0 0 600 560`), cercle « Comportementale » **non
déplacé** (cx300,cy300 inchangé) pour ne prendre aucun risque sur l'alignement du menu radial de situations. Le
débordement venait surtout des puces de situations positionnées jusqu'à `cy+ITEM_RADIUS` (300+230=530), qui
dépassaient déjà l'ancien viewBox (460) — le nouveau viewBox (560) leur laisse la marge nécessaire.

### Message de commit

`fix(tabac/addiction): cercle Comportementale sans débordement + retrait de la légende redondante`

### Statut

[x] fait

---

## Fin de session

Dérouler `/fin-de-tache` (mode **parallèle** ; statut + checklist visuelle dans ce `S4.md`).
