# S3 — M4 Risque CV : plaque en overlay + onglet « L'artère » + feux lucide

> **Mode** : solo · **Exécutant** : Sonnet · **Effort** : xhigh · **Dépend de** : S1 (silhouette `bodyImage`).
> **À lire avant** : index · `src/features/diabete/risque-cardio/RisqueCardioModule.tsx` (+ `.module.css`) · le prototype `scratchpad/proto-m4-risque.html` · le composant codé `components/PlaqueArtere.tsx`.

## Résultat attendu

Onglet **L'anatomie** : cliquer un territoire (cou/cœur/jambes) **pose la plaque sur l'artère** correspondante de la silhouette + affiche le texte (AVC / infarctus / artérite). Onglet **L'artère** : les 3 états (saine/encrassée/bouchée) illustrés. Les **5 feux** (leviers) passent en **lucide**.

## Travail

1. **Plaque en overlay** : sur la silhouette (`bodyImage`), poser `plaque.png` (transparente, ~26 px) via `children` positionné à l'ancre du territoire sélectionné. Positions (index §7) : cou 50/17 rot 90°, cœur 49/26 rot 0°, jambes 46/63 rot 90°. Halo doux optionnel sur la zone active. Le hotspot du territoire = zone cliquable (réutiliser les 3 zones `cou/coeur/jambes` de la silhouette).
2. **Onglet « L'artère » (réversible) — décision Thibault 2026-07-11** : base = **`artere-saine.png`** (l'illustration de vaisseau à lumière ouverte, transparente) ; par-dessus, **la plaque codée** (`PlaqueArtere` ou une plaque SVG/CSS ambre dédiée) posée dans la lumière et dont la **taille croît progressivement** selon l'état (saine → encrassée → bouchée = plaque 0 % → ~50 % → ~90 % d'occlusion). Croissance fluide/animable, paramétrable. Caler la plaque sur la lumière de l'illustration. **Les images `artere-encrassee/bouchee` ne sont PAS utilisées** (réserve) — un seul asset d'artère + une plaque codée croissante.
3. **Feux → lucide** : remplacer les `IllustrationSlot id={f.illustration}` (les 5 feux) par des icônes lucide (index §4 : Droplet/Gauge/Droplets/Cigarette/Armchair). Retirer les ids `risque-cardio-feu-*` du data si plus utilisés. Garder la sémantique couleur vert/ambre/rouge du feu (état), portée par le composant feu, pas par l'icône.
4. Le **feu tricolore objet** (sélecteur d'état) reste codé.

## Vérifs & clôture

- Build + typecheck + tests verts. Plaque bien calée sur les 3 artères (pas de débordement) ; onglet artère lisible ; feux lucide nets à 56-74 px.
- `VALIDATION.md` · `DECISIONS.md`/`STATUS.md`/`TASKS.md` · `/fin-de-tache`.

## Garde-fous

- La silhouette étant statique, l'état « bouché » passe par l'**overlay plaque** (l'image ne change pas). Ne pas tenter d'animer l'intérieur du PNG.
- Ne pas régénérer d'images ; si `PlaqueArtere` codé suffit pour l'onglet artère, préférer le code.

## Commit prévu (exécuté en fin de plan, pas maintenant)

`feat(diabete/risque-cardio): artère illustrée + plaque codée en overlay, feux → lucide (S3)`

---

## Statut (rempli par l'exécutant)

- [x] Fait · gates verts · exécuté le 2026-07-10

### Gates

- `npx tsc --noEmit` : vert.
- `npm run build` : vert.
- `npm test` : vert, 78/78 (aucun test touché).

### Fichiers touchés

- `src/features/diabete/components/PlaqueArtere.tsx` + `.module.css` (réécrit : ne dessine plus que le dépôt, plus le vaisseau ; export `plaquePassagePct`).
- `src/features/diabete/risque-cardio/RisqueCardioModule.tsx` + `.module.css` (vue ② artère-image + overlay plaque ; vue ③ plaque.png par territoire ; feux → lucide ; vue ④ icônes réduites).
- Contexte : `STATUS.md`, `TASKS.md`, `VALIDATION.md`, `DECISIONS.md`, `index.md`.

### Écart par rapport au plan initial

- Le plan mentionnait « `PlaqueArtere` ou une plaque SVG/CSS ambre dédiée » pour l'onglet artère :
  choix fait de **réécrire `PlaqueArtere` lui-même** (plutôt que d'ajouter un composant séparé) pour
  n'avoir qu'une seule source de vérité pour la courbe de croissance du dépôt — l'ancienne variante
  `artere` (vaisseau codé complet) et `pastille` (plaque codée sur silhouette) sont **supprimées**,
  n'ayant plus aucun consommateur après ce recâblage (cf. `DECISIONS.md` pour le détail et les
  alternatives envisagées).
- L'angle de rotation de la plaque sur `artere-saine.png` (~-25°) a été mesuré par analyse d'image
  (PCA), pas fourni par le plan — documenté dans `DECISIONS.md`.

### À valider visuellement (Thibault, `npm run dev`) — point sensible

L'alignement de la plaque overlay (vue ② et vue ③) n'a **jamais été vérifié à l'écran** (pas de
navigateur côté Claude). Checklist détaillée dans `VALIDATION.md` § S3 ; un décalage éventuel se
corrige via les constantes `.artereOverlay` (CSS) ou `PLAQUE_OVERLAYS` (module), sans reconception.

### Prochaine action

S4 (M1 Mécanisme, indépendant) ou S5 (M7 Traitements, dépend de S1 — fait).
