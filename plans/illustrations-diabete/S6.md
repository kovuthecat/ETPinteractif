# S6 — M6 Suivi : stations & organes du cadran → lucide

> **Mode** : solo · **Exécutant** : Sonnet · **Effort** : medium · **Dépend de** : — (indépendant).
> **À lire avant** : index (§4) · `src/features/diabete/suivi/SuiviModule.tsx` (+ `.module.css`) · `suivi/logic.ts`.

## Résultat attendu

Les icônes de stations/organes du cadran (34-44 px) passent des `IllustrationSlot` à des **icônes lucide-react** — plus nettes à cette taille et thématisables. Le cadran (géométrie), l'aiguille et le centre restent codés.

## Travail

1. Remplacer les `IllustrationSlot` du module (`suivi-stethoscope`, `suivi-prise-de-sang`, `suivi-organe-*`) par des icônes lucide (index §4) :
   - stéthoscope → `Stethoscope` · prise de sang → `TestTube` · yeux → `Eye` · défenses → `ShieldPlus` · vaccins → `Syringe` · HbA1c/vaisseaux → `Droplet` (ou `Activity`) · cœur → `Heart`.
2. **Cas sans icône lucide — décidé (Thibault, 2026-07-11)** :
   - **Rein** : **réutiliser l'illustration `organe-reins.png`** en petit format (exception assumée au « tout lucide » — lucide n'a pas de rein).
   - **Dentiste** : substitut lucide simple (ex. `Smile`) ou un mini-SVG « dent » codé, au plus propre. Non bloquant.
3. Nettoyer les ids `suivi-*` devenus inutiles (data/logic) et vérifier qu'aucun autre module ne les référence.
4. Le **centre du cadran** (motif « fil rouge », `dialCenter*`) reste **codé** (cercles SVG). Ne pas y mettre d'image.

## Vérifs & clôture

- Build + typecheck + tests verts. Cadran lisible, icônes nettes à 34-44 px, cohérence visuelle du set.
- `VALIDATION.md` (Thibault valide le rendu + tranche rein/dent si besoin) · `DECISIONS.md`/`STATUS.md`/`TASKS.md` · `/fin-de-tache`.

## Garde-fous

- Cohérence avec la décision tabac (« petits pictos = lucide »).
- Ne pas régénérer d'images pour ce module (les stations générées partent en réserve).

## Commit prévu (exécuté en fin de plan, pas maintenant)

`feat(diabete/suivi): stations et organes du cadran passent à lucide (S6)`

---

## Statut (rempli par l'exécutant)

- [x] Fait · gates verts · exécuté le 2026-07-10

### Gates

- `npx tsc --noEmit` : vert.
- `npm run build` : vert.
- `npm test` : vert, 78/78 (aucun test touché).

### Fichiers touchés

- `src/features/diabete/suivi/SuiviModule.tsx` (composant local `StationIcon`, remplace 9 usages d'`IllustrationSlot`).
- `src/features/diabete/suivi/SuiviModule.module.css` (`.iconFrame`, `.iconImg`, `.iconShapeCircle`, `.iconShapeRounded`).
- Contexte : `STATUS.md`, `TASKS.md`, `VALIDATION.md`, `DECISIONS.md`, `index.md`.

### Écarts par rapport au plan initial

- `pied → Footprints` (non listé dans le plan, ajout évident) ; `defenses → Syringe` plutôt que
  `ShieldPlus` (les deux étaient suggérés pour deux entrées distinctes vaccins/défenses qui
  n'existent pas séparément dans le modèle de données actuel — une seule icône). Détail dans
  `DECISIONS.md`.
- Rein/dentiste : tranchés comme prévu (image `organe-reins.png` / `Smile`).

### Prochaine action

S7 (vignettes M2/M3/M8) — lot de sources quasi complet désormais disponible dans Downloads.
