# S2 — M5 Complications : hotspots organes + panneaux illustrés

> **Mode** : solo · **Exécutant** : Sonnet · **Effort** : xhigh · **Dépend de** : S1 (silhouette `bodyImage` + hotspots).
> **À lire avant** : index du chantier · `src/features/diabete/complications/ComplicationsModule.tsx` (+ `.module.css`) · `complications/data.ts` · le prototype de référence `scratchpad/proto-m5-complications.html`.

## Résultat attendu

Au clic (ou focus clavier) sur un organe de la silhouette, le panneau de détail affiche **l'illustration de l'organe** en plus des 3 temps (menace → évitable + chiffre → geste). Cœur et cerveau restent verrouillés (« déjà vus au module 4 »).

## Travail

1. **Silhouette** : le module utilise déjà `Silhouette` ; grâce à S1 elle affiche l'image + hotspots. Vérifier les états passés : `yeux/reins/nerfs/pied` = actif/ouvert selon la sélection ; `cerveau/coeur` = `verrouille`.
2. **Panneau organe** : ajouter en tête du bloc détail un `IllustrationSlot` (ou un simple `<img>`) `id="organe-${organeOuvert.id}"` → `organe-yeux|reins|nerfs.png`, et **réutiliser `pied-auto-examen.png`** pour le pied (déjà affiché aujourd'hui). Taille ~104-120 px, cadre doux (fond `--color-bg`, `border` fin, `border-radius`) — cf. proto.
3. **Données** : `ORGANES` (yeux/reins/nerfs/pied) ont déjà menace/evitable/geste/stat. Rien à changer côté texte. `SignatureEvitable` (composant codé) reste tel quel pour envelopper le `stat`.
4. **Assets** : produits en S1 (`organe-yeux/reins/nerfs`, `pied-auto-examen`). Si un manque, l'ajouter à `build_assets.py`.
5. **nerfs** = ancre sur la **main** (validé Thibault, index §7).

## Vérifs & clôture

- Build + typecheck + tests verts. Chaque organe cliquable ouvre son panneau illustré ; cœur/cerveau non cliquables ; halo au survol correct ; rien de coupé au recadrage.
- `VALIDATION.md` (checklist visuelle Thibault) · `DECISIONS.md`/`STATUS.md`/`TASKS.md` · `/fin-de-tache`.

## Garde-fous

- Ne pas transformer `SignatureEvitable` en image (il enveloppe un chiffre dynamique).
- Le panneau reste lisible à ~1 m (gros titres, illustration nette).

## Commit prévu (exécuté en fin de plan, pas maintenant)

`feat(diabete/complications): illustration d'organe en tête du panneau détail (S2)`

---

## Statut (rempli par l'exécutant)

- [x] Fait · gates verts · exécuté le 2026-07-10

### Gates

- `npx tsc --noEmit` : vert.
- `npm run build` : vert.
- `npm test` : vert, 78/78 (aucun test touché).

### Fichiers touchés

- `src/features/diabete/complications/ComplicationsModule.tsx` (`.organeHead` : illustration + titre).
- `src/features/diabete/complications/ComplicationsModule.module.css` (classe `.organeHead`).
- Contexte : `STATUS.md`, `TASKS.md`, `VALIDATION.md`, `index.md`.

### Écart par rapport au plan initial

- Le plan envisageait de réutiliser `pied-auto-examen.png` « déjà affiché aujourd'hui » sans forcément
  l'ajouter en tête pour le pied. Choix fait : l'afficher aussi en tête (comme les 3 autres organes),
  par cohérence avec le prototype de référence `proto-m5-complications.html` (qui montre l'illustration
  en tête pour les 4 organes, y compris le pied) et avec la ligne de clôture du plan lui-même
  (« chaque organe cliquable ouvre son panneau illustré »). Le pied affiche donc la même image à deux
  endroits (tête + bloc « points de contrôle » plus bas) — même asset, pas de nouveau fichier.

### Prochaine action

S3 (M4 Risque CV) ou S5 (M7 Traitements) — les deux ne dépendent que de S1 (fait).
