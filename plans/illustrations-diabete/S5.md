# S5 — M7 Traitements : silhouette `bodyImage` + halo zones protégées

> **Mode** : solo · **Exécutant** : Sonnet · **Effort** : high · **Dépend de** : S1 (silhouette partagée).
> **À lire avant** : index · `src/features/diabete/traitements/TraitementsModule.tsx` (+ `.module.css`) · `traitements/data.ts`.

## Résultat attendu

Le module 7 utilise la **même silhouette image** (via S1). Au clic « Voir l'effet » sur une ligne d'ordonnance, la/les zone(s) protégée(s) par la classe s'**allument d'un halo doux positif** sur la silhouette (sucre = systémique, cœur/reins = ancres réelles).

## Travail

1. **Silhouette** : le module passe déjà des `SilhouetteZoneState` ; grâce à S1 elle s'affiche en image. Les zones `coeur`/`reins` = état `allume` (halo confort persistant) quand la classe les protège. Le halo « sucre » systémique : conserver l'overlay CSS `.halo` existant, ou le porter en halo de zone — **au plus simple**, garder le `.halo` CSS actuel (il fonctionne) posé sur la silhouette image.
2. **Rien d'autre à illustrer** : l'ordonnance est un **formulaire live** (champs/selects) → inchangée. Les pictos de fréquence et les pastilles de surveillance restent en **lucide** (Clock/LifeBuoy/Info/Syringe) — ne pas les remplacer par des images (ce serait un recul : ils sont cliquables/navigants).
3. Vérifier que le halo positif reste **uniquement positif** (aucune alerte rouge), invariant du module.

## Vérifs & clôture

- Build + typecheck + tests verts. Sélection d'une ligne → bonnes zones allumées ; « Ordonnance » (tout) → toutes les zones ; halo doux, jamais d'alerte.
- `VALIDATION.md` · `DECISIONS.md`/`STATUS.md`/`TASKS.md` · `/fin-de-tache`.

## Garde-fous

- « Corps pur » : la silhouette ne montre jamais que le bien (halo positif), invariant M7.
- Ne pas intégrer les images ordonnance/pictos/pastilles/halo générées (réserve) — l'archi du module les rendrait pires que le code en place.

---

## Statut (rempli par l'exécutant)

- [x] Vérifié · **aucun changement de code nécessaire** · gates verts · exécuté le 2026-07-10

### Constat

`TraitementsModule.tsx` consomme déjà `<Silhouette zones={silhouetteZones} />` sans aucune
coordonnée en dur : depuis S1, le wrapper diabète (`Silhouette.tsx`) passe systématiquement
`bodyImage` à `SilhouetteCorps` pour **tous** les modules consommateurs, sans changement d'API.
Ce module affichait donc déjà la silhouette-image + hotspots dès S1, avec le comportement attendu :
- Cœur/reins passent en état `allume` (halo confort persistant, jamais de rouge) quand la classe
  sélectionnée les protège ; les 6 autres zones restent `masque` (jamais affichées, invariant
  « pas d'autres organes à ce module »).
- Le halo « sucre » systémique reste le `.halo` CSS existant (position absolue, `border-radius:50%`
  autour de `.silhouetteHost`) — déjà compatible avec le conteneur carré de la silhouette-image
  (`max-width` seul, pas de hauteur fixée).
- Les pictos (Clock/Info/LifeBuoy/Syringe/Plus/X) étaient déjà en lucide, aucune régression à
  crAindre.

Aucun fichier de `src/` modifié pour cette session — la vérification (lecture du code +
confirmation que le garde-fou « corps pur » et les 6 zones masquées sont intacts) constitue le
livrable.

### Gates

- `npx tsc --noEmit` : vert (inchangé).
- `npm run build` : vert (inchangé).
- `npm test` : vert, 78/78 (inchangé).

### À valider visuellement (Thibault, `npm run dev`)

Point bas risque (déjà couvert par la checklist S1 « silhouette-image dans M4/M5/M7 ») : vérifier
que le halo cœur/reins s'allume au bon endroit selon la ligne sélectionnée, et que le halo sucre
systémique reste doux/positif. Checklist dans `VALIDATION.md` § S5.

### Prochaine action

S6 (M6 Suivi, indépendant).
