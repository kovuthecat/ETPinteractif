# outils-interactifs-2026-07 · S8 — Consolidation   (rédigé par Opus)

> **Modèle : Haiku · effort : minimal · Vague : 3 (parallèle : non — après toutes les sessions)**
> Exécutant : consolidation mécanique. Ne rien reconcevoir, ne pas modifier de code métier.
> Doute → STOP, signale.

- Date : — · Branche : —

## Préalable

Toutes les sessions S1-S7 sont exécutées et validées (build 2 entrées + typecheck + tests OK,
lignes « Humain » reportées dans `VALIDATION.md`). Sinon → STOP.

## Lire (commun à la session)

- `plans/outils-interactifs-2026-07/index.md` (statuts, ids OI, messages de commit par tâche)
- `plans/outils-interactifs-2026-07/S1.md`..`S7.md` (blocs « Message de commit » + « Statut »)
- `STATUS.md`, `TASKS.md`, `DECISIONS.md`, `PROJECT_MAP.md` (racine du projet)
- `VALIDATION.md`

## Étapes (WORKFLOW §4d)

1. **Commits tâche par tâche**, staging explicite (jamais `git add -A`) : un commit par OI avec le
   message figé dans son bloc « Message de commit », dans l'ordre S1 → S7. Fichiers stagés = uniquement
   ceux touchés par l'OI.
2. **Statuts** : cocher `[x]` dans `index.md` (table Sessions) et dans chaque bloc « Statut » des S<k>
   (date + hash court de commit). Mettre à jour `TASKS.md`.
3. **`STATUS.md`** : nouvelle entrée — chantier « outils interactifs » livré (14 outils : 1 existant +
   respiration recâblée en consultation + 11 rendus interactifs dans les deux bundles).
4. **`VALIDATION.md`** : regrouper les points « Humain » des S1-S7 + les gates G1-G5 restées ouvertes.
5. **`DECISIONS.md`** : consigner les décisions structurantes de l'index (registre, persistance
   injectée, `outilsData`, extension cadrage patient G1, prix paquet G3…). Marquer les gates non
   tranchées comme « à revalider (Thibault) ».
6. **`PROJECT_MAP.md`** : ajouter le répertoire `src/features/tabac/boite-a-outils/outils-interactifs/`
   (registre + composants) et les hooks `useConsultationStore` / `usePatientStore`.
7. **Un seul `git push`** à la fin, après relecture `git log`/`git status`.

## Validation

- Auto : `npx tsc --noEmit` + `npm run build` (2 entrées) + `npm test` → OK avant push.
- Humain : `git log --oneline` cohérent (un commit par OI, messages conformes), `git status` propre.

## Si bloqué

Un statut S<k> encore `[ ]` ou une validation « Humain » manquante dans `VALIDATION.md` → STOP, ne pas
pusher, signaler la session incomplète.

### Statut
[ ] à faire · exécuté le : — · commit : —
