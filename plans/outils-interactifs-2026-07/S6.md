# outils-interactifs-2026-07 · S6 — Plan de secours + Ma phrase de refus   (rédigé par Opus)

> **Modèle : Sonnet · effort : medium · Vague : 2 (parallèle : oui)**
> Exécutant : UNIQUEMENT les tâches ci-dessous ; fichiers sous « Lire » / « Modifier ».
> Design fixé — ne reconçois pas. Doute ou blocage → STOP, signale, rends la main.

- Date : — · Branche : —

## Lire (commun à la session)

- `src/content/tabac/outils.ts` : `outil-faux-pas` (l.197-209), `outil-refus` (l.159-171) — sources
  verbatim, NE PAS reformuler
- `src/state/SelectionContext.tsx` (champ `raisons` — pour info ; le composant le reçoit déjà via
  `props.contexte.raisons`, injecté en S1, RÉFÉRENCE seulement)
- `src/features/tabac/boite-a-outils/outils-interactifs/types.ts` (`OutilInteractifProps` — S1)
- `src/features/tabac/boite-a-outils/VagueCraving.tsx` (RÉFÉRENCE de style)
- Les stubs `outils-interactifs/PlanSecours.tsx` et `PhraseRefus.tsx` créés en S1

## Hors périmètre

- Ne toucher qu'à `PlanSecours.*` et `PhraseRefus.*`. Ne PAS modifier `SelectionContext` (lecture seule
  de `raisons`). Aucune dépendance runtime. Commit/push : non (S8).

---

## OI9 — Plan de secours « en cas d'écart »

### Objectif
Une carte d'action immédiate (pas une fiche à lire) : les 3 gestes du plan de secours, le contact
39 89, et le rappel des raisons d'arrêter.

### Décision clé
Contenu = les 3 gestes du `proposition` (1. jeter paquet + briquet · 2. appeler un proche / le 39 89 ·
3. relire mes raisons). Afficher les `raisons` déjà saisies (`SelectionContext.raisons`) si présentes ;
sinon inviter à les noter. Ton déculpabilisant (« un écart n'est pas une rechute »), verbatim du `principe`.

### Lire / Modifier
- Modifier (remplir le stub) : `src/features/tabac/boite-a-outils/outils-interactifs/PlanSecours.tsx`
  (+ `.module.css`)

### Étapes
1. Bandeau rassurant (verbatim `principe` : « Un faux pas n'est pas une rechute »).
2. Les 3 gestes en grand, cochables (case = « fait »), état non persistant (geste d'urgence, éphémère).
3. Bloc contact : « Tabac Info Service 39 89 » mis en avant (cohérent avec `aparte` du module).
4. Rappel « Mes raisons d'arrêter » : lire `props.contexte?.raisons` (injecté par le bundle en S1 —
   consultation depuis `SelectionState.raisons`). Si vide, afficher l'invite « Notez vos raisons dans
   Mon plan d'arrêt ». (Le canal `contexte` est déjà prévu au socle — ne pas ajouter de prop ad hoc.)
5. Bouton Retour → `onClose`.

### Validation
- Auto : `npx tsc --noEmit` + `npm run build` (2 entrées) → OK.
- Humain : les 3 gestes s'affichent en grand ; en consultation, si des raisons ont été cochées ailleurs,
  elles apparaissent ; 39 89 visible. → `VALIDATION.md`.

### Si bloqué
`props.contexte?.raisons` arrive toujours vide (câblage S1 incomplet) → se limiter au rappel textuel
« relisez vos raisons d'arrêter » et signaler (le composant reste fonctionnel sans le contexte).

### Message de commit
`feat(outils): plan de secours en cas d'ecart`

### Statut
[x] fait · exécuté le : 2026-07-21 · commit : aa9e20b

---

## OI10 — Ma phrase de refus

### Objectif
Le patient choisit/compose sa phrase de refus et la garde dans sa fiche.

### Décision clé
Variantes proposées issues du `proposition` (« Non merci, j'ai arrêté. » + variantes courtes) ; saisie
libre possible. Persistance via `store.setList('outil-refus', [phraseChoisie])`. Rappel de la vigilance
alcool (verbatim `principe`).

### Lire / Modifier
- Modifier (remplir le stub) : `src/features/tabac/boite-a-outils/outils-interactifs/PhraseRefus.tsx`
  (+ `.module.css` si besoin)

### Étapes
1. 3-4 variantes proposées (boutons radio / puces sélectionnables) + champ libre « Ma phrase ».
2. Astuce comportementale du `proposition` : « gardez un verre non alcoolisé en main », « éloignez-vous
   quelques minutes » — en note sobre.
3. Persister la phrase retenue via `store` (clé `outil-refus`) → remonte dans la fiche (socle OI4).
4. Bouton Retour → `onClose`.

### Validation
- Auto : `npx tsc --noEmit` + `npm run build` (2 entrées) → OK.
- Humain : choisir une variante OU saisir la sienne → la fiche affiche la phrase retenue. → `VALIDATION.md`.

### Si bloqué
— (outil simple ; en cas de doute d'UX, calquer la sélection sur les `dPill` de `VagueCraving`).

### Message de commit
`feat(outils): ma phrase de refus`

### Statut
[x] fait · exécuté le : 2026-07-21 · commit : 57e8f23

## Fin de session
Dérouler `/fin-de-tache` (mode parallèle : oui — ne pas toucher STATUS/TASKS/index/VALIDATION, pas de push).

## Bilan de session

### Fichiers modifiés / créés

- `src/features/tabac/boite-a-outils/outils-interactifs/PlanSecours.tsx` (remplacement du stub S1)
- `src/features/tabac/boite-a-outils/outils-interactifs/PlanSecours.module.css` (créé)
- `src/features/tabac/boite-a-outils/outils-interactifs/PhraseRefus.tsx` (remplacement du stub S1)
- `src/features/tabac/boite-a-outils/outils-interactifs/PhraseRefus.module.css` (créé)

Aucun autre fichier touché (`registry.ts`, `types.ts`, `SelectionContext.tsx`, `useConsultationStore.ts`,
`usePatientStore.ts`, `BoiteAOutilsModule.tsx`, `PatientSituations.tsx` : lus pour référence
uniquement, non modifiés). `OutilStub.module.css` conservé intact (toujours utilisé par les stubs
hors périmètre S6 : Tirelire, OutilChecklist, MinuteurGuide, GabaritJournal).

### OI9 — Plan de secours

Carte d'action (pas une fiche) : bandeau rassurant (`outil.accroche` en titre — verbatim, contient
« Un faux pas n'est pas une rechute » — + `outil.principe` verbatim en corps), les 3 gestes du
`proposition` isolés verbatim en items cochables « fait » (case à cocher accessible, état 100%
local/éphémère — aucune écriture `store`, conformément à la décision « geste d'urgence »), bloc
contact 39 89 (classes globales `fiche-contact`), et rappel des raisons d'arrêter lu via
`props.contexte?.raisons` (injecté par S1 côté consultation depuis `state.raisons` — vérifié dans
`BoiteAOutilsModule.tsx`, déjà câblé) avec repli invite « Notez vos raisons dans « Mon plan
d'arrêt » » quand la liste est vide (cas normal côté patient, qui n'injecte pas `raisons`, et cas
consultation avant toute raison cochée). Bouton retour → `onClose`. Pas de scénario de blocage
rencontré (le canal `contexte.raisons` fonctionne comme prévu côté consultation).

### OI10 — Ma phrase de refus

4 variantes courtes sélectionnables (chips `role="radio"` + `activeDoubled`, même convention que
`PlansSiAlors` livré en parallèle par S2) — la 1ʳᵉ verbatim du `proposition` (« Non merci, j'ai
arrêté. »), les 3 autres sont des formulations courtes/fermes/sans justification supplémentaires
(pas de source clinique dédiée au-delà de la phrase-modèle) — plus un champ libre « Ma phrase »
partageant le même état (clic sur une puce = la remplit, saisie libre = la redéfinit). Astuce
comportementale et rappel de vigilance alcool affichés verbatim (extraits non reformulés du
`proposition`/`principe`). Persistance : `store.setList(outil.id, [phrase])` (clé dynamique =
`outil.id`, qui vaut `outil-refus` pour cet outil) dès qu'une phrase est retenue (puce ou saisie
libre), lu au montage pour préremplir si une phrase existait déjà. Aucun blocage.

### Validation Auto

- `npx tsc --noEmit` : OK (aucune erreur), exécuté après les deux tâches.
- `npm run build` : OK (`tsc -b && vite build` complet, les deux entrées `consultation.html` /
  `patient.html` construites sans erreur).

### Checklist Humain (reportée telle quelle pour VALIDATION.md, à charge de S8)

- OI9 : les 3 gestes s'affichent en grand ; en consultation, si des raisons ont été cochées
  ailleurs, elles apparaissent ; 39 89 visible.
- OI10 : choisir une variante OU saisir la sienne → la fiche affiche la phrase retenue.

### Blocages

Aucun.
