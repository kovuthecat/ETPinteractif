# S6 — Alimentation : débordements Qualité/Ordre corrigés + LA COURBE agrandie

> **Mode** : solo · **Exécutant** : Sonnet · **Effort** : xhigh · **Dépend de** : —.
> **À lire avant** : index v2 · `plans/corrections-visuelles-diabete/S2.md` + `S3.md` (tour 1 : bande-cible, layout agrandi, désaturation de la courbe) · `src/features/diabete/alimentation/AlimentationModule.tsx` + `.module.css` + `data.ts` · `src/features/diabete/lib/glycemieCurve.ts` · `src/features/diabete/components/CourbeGlycemie.tsx` + `.module.css`.

## Problème (captures #14, #15, #16)

- **② Qualité** : la mise en page **déborde de l'écran** (garde-manger + panneau de comparaison).
- **③ Ordre** : **déborde** aussi, et la **courbe de glycémie du bas est trop petite** pour visualiser l'effet de l'ordre des bouchées.
- **④ Proportion** : idem, la courbe « Glycémie après le repas » est **trop petite**.

Le tour 1 (S2/S3) a agrandi la scène et ajouté la bande-cible, mais des débordements subsistent et LA COURBE reste sous-dimensionnée dans ces deux onglets.

## Résultat attendu

1. Aucun débordement horizontal dans les onglets Qualité et Ordre (à 1024×768 comme à 1440×900).
2. LA COURBE (Ordre et Proportion) est **assez grande pour lire la différence de pic** — cohérente avec l'identité graphique unique de la courbe (brief : « une seule identité, déclinée »).

## Travail

### 1. Débordement Qualité (`AlimentationModule.module.css`)
- Repérer la cause du débordement de l'onglet ② (largeurs fixes, grille garde-manger + duels, min-width). Contraindre à la largeur disponible (`minmax`, `overflow` maîtrisé, wrap).

### 2. Débordement Ordre + courbe (`AlimentationModule.tsx` / `.module.css`)
- Corriger le débordement de l'onglet ③ (les cartes « 1ʳᵉ / 2ᵉ / 3ᵉ bouchée » + garde-manger).
- Agrandir la `CourbeGlycemie` affichée en bas (hauteur et largeur), pour que l'écrêtage lié à l'ordre soit lisible à ~1 m.

### 3. Courbe Proportion (`AlimentationModule.tsx` / `.module.css`)
- Agrandir la courbe « Glycémie après le repas » de l'onglet ④ (même traitement que ③).

### 4. Règle partagée de taille minimale de LA COURBE
- Si `CourbeGlycemie` est instanciée avec des tailles trop petites par ses conteneurs, poser une **taille minimale confortable** commune (dans le composant ou via une classe partagée) plutôt que patcher onglet par onglet — LA COURBE est réutilisée aux modules 2/3/8/9. Vérifier qu'agrandir ici ne dégrade pas les autres usages (module 3 timing, 8 récupération, 9 capteur).

## Vérifs & clôture
- `tsc --noEmit` + `npm run build` + `npm test` verts (les tests portent sur `glycemieCurve.ts`, logique pure — le redimensionnement est CSS/rendu, ne doit pas les toucher).
- Checklist `VALIDATION.md` § S6-v2 : pas de débordement Qualité/Ordre à 1024×768 et 1440×900 ; courbe Ordre et Proportion nettement plus grande, différence de pic lisible ; pas de régression de la courbe dans les modules 3/8/9.
- `DECISIONS.md`/`STATUS.md`/`TASKS.md` · `/fin-de-tache`.

## Garde-fous
- **Pas de chiffre imposé sur la courbe** (mg/dL au survol seulement — brief) : on change la taille, pas le modèle pédagogique.
- Ne pas casser les invariants qualitatifs de `glycemieCurve.test.ts`.
- LA COURBE = objet partagé : toute taille minimale commune doit être vérifiée sur les 4 modules qui l'utilisent.

## Commit prévu
`fix(diabete/alimentation): débordements Qualité/Ordre + courbe agrandie (S6-v2)`

---

## Statut (rempli par l'exécutant)
- [x] Fait · gates : tsc + build + test verts (80/80) · le 2026-07-11.

### Gates
`tsc --noEmit` ✓ · `npm run build` ✓ · `npm test` ✓ (80/80 — `glycemieCurve.test.ts` inchangé, aucune logique touchée, uniquement CSS/layout).

### Fichiers touchés
- `src/features/diabete/components/CourbeGlycemie.module.css` (**T4, composant partagé**) : `.wrap` gagne un plancher `min-width: 440px` au-dessus de 480px de viewport (aucun plancher forcé en dessous, pour ne jamais provoquer de débordement réel sur mobile) — si un conteneur flex trop étroit (ex. Alimentation défi ③ avec le garde-manger à côté) ne peut pas offrir 440px, l'ancêtre `flex-wrap` reflue au lieu d'écraser la courbe. Bénéficie automatiquement aux 4 modules qui réutilisent `CourbeGlycemie` (Alimentation, Activité, Hypoglycémie, Insuline) sans code dupliqué.
- `src/features/diabete/alimentation/AlimentationModule.module.css` : `.courbeCard` plafond 760→900px (défis ③/④ en pleine largeur, sans garde-manger, restaient capés bien en-dessous de l'espace réel) ; `.d2Card` (défi ②) et `.d3Slot` (défi ③) : `width` fixe (redondant avec `flex-basis`, source de confusion selon les moteurs de rendu) remplacé par `min-width: 0`, qui laisse réellement la carte se réduire à son contenu au lieu d'imposer un plancher implicite.

### Écart par rapport au plan
Le mécanisme exact du débordement (#14/#15) n'a pas pu être identifié avec certitude par analyse statique seule (pas de navigateur côté Claude, cf. garde-fou projet) : le calcul manuel des largeurs flex (`.layout`/`.stage`/`.shelf`, contenu ~900px sous `ModuleShell`) ne fait pas ressortir de dépassement garanti aux résolutions cibles — `flex-wrap` devrait normalement replier plutôt que déborder. Le correctif appliqué est donc une **passe défensive** (largeurs fixes redondantes retirées, `min-width: 0`, plancher/plafond de LA COURBE) plutôt qu'un fix ciblé sur une cause unique identifiée — cohérent avec les bonnes pratiques CSS flex/grid, mais **à re-vérifier en priorité par Thibault** (`npm run dev`, 1024×768 et 1440×900) : le point est explicitement bloquant au sens du plan (« critère bloquant »).

### À valider visuellement (Thibault)
- **Prioritaire (bloquant du plan)** : plus de débordement horizontal Qualité/Ordre à 1024×768 et 1440×900.
- LA COURBE défis ③/④ : nettement plus grande, différence de pic lisible ?
- Défi ③ : si le plancher de 440px force le garde-manger à passer sous la courbe plutôt qu'à côté, ce repli est-il acceptable visuellement ?
- Aucune régression de la courbe dans les modules 3 (Activité, onglet Timing), 8 (Hypoglycémie), 9 (Insuline) — tous en layout pleine largeur, le nouveau plancher ne devrait rien changer chez eux (déjà ≥440px).

### Prochaine action
Toutes les sessions du plan (S1-S6) sont faites — passer à la consolidation fin de plan (commits, statuts globaux, push).
