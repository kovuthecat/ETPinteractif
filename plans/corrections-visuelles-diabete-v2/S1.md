# S1 — Silhouettes tour 2 : vraiment dominantes + plaques localisées + texte Traitements retiré

> **Mode** : solo · **Exécutant** : Sonnet · **Effort** : xhigh · **Dépend de** : —.
> **À lire avant** : index v2 · `plans/corrections-visuelles-diabete/S1.md` (tour 1 : ce qui a déjà été agrandi, 420/400/400 px) · `src/components/SilhouetteCorps.tsx` + `.module.css` · `src/features/diabete/traitements/TraitementsModule.tsx` + `.module.css` · `src/features/diabete/complications/ComplicationsModule.tsx` + `.module.css` · `src/features/diabete/risque-cardio/RisqueCardioModule.tsx` + `.module.css`.

## Problème (captures #1, #2, #3, #4)

Le tour 1 a agrandi les silhouettes (`.wrapImage` max-width 460 ; hosts 420/400/400 px) mais Thibault les trouve **encore trop petites par rapport à l'écran** ; l'effet « organe allumé » reste peu perceptible faute de surface. En plus :
- **Traitements** : un bloc de texte narratif « Cliquez "Voir l'effet"… pour tout allumer d'un coup » subsiste → à retirer (le soignant narre).
- **Risque cardio ③ Anatomie** : les dépôts de plaque semblent apparaître ailleurs que sur la zone cliquée → ils ne doivent apparaître **que sur la zone sélectionnée**.

## Résultat attendu

1. Dans les 3 modules, la silhouette **domine l'écran** (élément principal, pas une vignette dans une colonne étroite) — nettement plus grande qu'au tour 1.
2. L'organe « allumé »/actif est **franchement visible** (surface + halo suffisants).
3. Traitements : plus de bloc de texte explicatif dans le panneau latéral.
4. Risque cardio ③ : le dépôt de plaque n'est visible **que** sur le territoire actif.

## Travail

### 1. Règle de taille partagée — `SilhouetteCorps.module.css`
- Relever encore le plafond du mode image : `.wrapImage` de `max-width: 460px` vers **~600–680 px** (à caler visuellement) — l'objectif est qu'elle remplisse l'espace, pas qu'elle reste une carte.
- La taille rendue vient surtout des **conteneurs modules** (points 3–5) : y donner à la silhouette la part dominante de la largeur.
- Vérifier que le halo « allumé » (tour 1 : anneau + `box-shadow` + `scale`) reste proportionné à la nouvelle taille (pas un halo ridicule sur une grande silhouette).
- **Rétro-compat** : mode pastille (tabac) strictement inchangé.

### 2. Traitements — retrait du texte (capture #2)
- Dans `TraitementsModule.tsx`, supprimer le paragraphe narratif du panneau « Ce que ce traitement protège » (« Cliquez "Voir l'effet" sur une ligne… pour tout allumer d'un coup »). Garder l'eyebrow court et l'affordance des lignes/boutons.
- Nettoyer le CSS orphelin correspondant dans `.module.css`.

### 3. Traitements — silhouette dominante (capture #1)
- Rééquilibrer la grille ordonnance / silhouette pour donner à la silhouette une largeur confortable (viser ~480–560 px de rendu). L'ordonnance peut rester à gauche, mais la silhouette ne doit plus être une bande étroite à droite.
- Vérifier que le `.halo`/allumage cœur-reins est bien visible à la nouvelle taille (c'est le cœur de la remarque).

### 4. Complications — silhouette dominante (capture #4)
- Agrandir fortement la `.silhouetteCol` (viser ~480–560 px). Le module est « contemplatif » (brief) : la silhouette doit occuper l'écran, le panneau de texte s'adapte autour.

### 5. Risque cardio ③ Anatomie — silhouette + plaque localisée (capture #3)
- Agrandir la `.silhouetteWrap` (viser ~480–560 px).
- **Plaque localisée** : s'assurer que l'overlay `plaquePin` (ou équivalent) n'est rendu **que** pour le territoire actif (`cou`/`coeur`/`jambes`). Si le code dépose actuellement des marqueurs sur plusieurs territoires simultanément, conditionner le rendu à `zoneActive`. À confirmer dans le code avant de corriger.

## Vérifs & clôture
- `tsc --noEmit` + `npm run build` + `npm test` verts (les tests ne touchent pas la silhouette ; vérifier l'absence de régression tabac).
- Checklist `VALIDATION.md` § S1-v2 : silhouette dominante dans les 3 modules ; organe allumé nettement visible ; plus de bloc texte Traitements ; plaque Risque cardio ③ visible uniquement sur la zone active ; **tabac `benefices-arret` inchangé**.
- `DECISIONS.md`/`STATUS.md`/`TASKS.md` · `/fin-de-tache`.

## Garde-fous
- Ne pas coder de nom de thème dans `SilhouetteCorps`.
- Ne pas régénérer les PNG (on agrandit l'affichage).
- Surveiller le recouvrement à grande taille (écrans larges ≥1800 px et mobile) — consigner dans `VALIDATION.md`.

## Commit prévu
`fix(diabete): silhouettes dominantes + plaque localisée + texte Traitements retiré (S1-v2)`

---

## Statut (rempli par l'exécutant)
- [x] Fait · gates : tsc + build + test verts (80/80) · le 2026-07-11.

### Gates
`tsc --noEmit` ✓ · `npm run build` ✓ (1859 modules) · `npm test` ✓ (80/80, aucune régression tabac/diabète).

### Fichiers touchés
- `src/components/SilhouetteCorps.module.css` (`.wrapImage` 460→640px — règle partagée mode `bodyImage`, mode pastille/SVG tabac inchangé).
- `src/features/diabete/traitements/TraitementsModule.tsx` (retrait du texte de guidage ambiant — le panneau « Ce que ce traitement protège » ne s'affiche plus que comme sortie d'une interaction, sinon masqué) + `.module.css` (grille `460px / minmax(480,1fr)`, host silhouette 420→560px).
- `src/features/diabete/complications/ComplicationsModule.module.css` (`.silhouetteCol` 400→560px).
- `src/features/diabete/risque-cardio/RisqueCardioModule.tsx` (vue ③ : la plaque `plaque.png` ne se dépose plus que sur `zoneActive`, avant elle apparaissait sur les 3 territoires dès qu'un feu était rouge) + `.module.css` (`.silhouetteWrap` 400→560px).

### Écart par rapport au plan
Aucun. Traitements : au lieu de « garder l'eyebrow, retirer juste le paragraphe », le panneau entier (eyebrow + carte) est masqué tant qu'aucune ligne n'est sélectionnée / aucune vue d'ensemble affichée — un eyebrow seul sans contenu n'avait pas de sens visuel ; les cas avec contenu réel (ligne sélectionnée, vue d'ensemble, état vide « Écrivez une molécule… ») gardent eyebrow + texte inchangés.

### À valider visuellement (Thibault)
- Taille réelle des 3 silhouettes à 560px vs écran (« domine » ou encore petit ?).
- Recouvrement à grande largeur (≥1800px) et sur mobile — non vérifié par Claude.
- Risque cardio ③ : la plaque doit maintenant apparaître uniquement sur la zone cliquée (cou/cœur/jambes), plus sur les 3 à la fois.
- Traitements : absence de panneau texte quand rien n'est sélectionné — vérifier que ça ne fait pas un vide gênant à côté de la silhouette.

### Prochaine action
S2 (feux cardio + artère 70 %), dépend de S1 (même fichier `RisqueCardioModule`).
