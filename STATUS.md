# STATUS.md

État à l'instant T : ce qui marche, ce qui casse. Historique détaillé : `git log`.

> **Frontières** — STATUS : état actuel · `TASKS.md` : backlog + tâches · `plans/` : plan d'une tâche active · `VALIDATION.md` : checklist visuelle.
>
> **Dernière mise à jour :** 2026-07-08 (S1 refonte-ui exécutée)

## Phase actuelle

Phase 1 (squelette + 6 modules tabac) terminée. Phase 2 (corrections UX, `plans/PLAN_corrections-ux.md`) : C1-C9 faits, reste **C10** (bloqué sur contenu à fournir par Thibault). Phase 3 (`plans/PLAN_corrections-v2.md`, captures Thibault 2026-07-01) : **R1** fait (débordement systémique : `.content` borné à 960px, graphiques SVG bornés en hauteur/largeur dans Nicotine/Soulagement/Craving/Addiction). **R2** fait (Nicotine-toxique : hotspots/étiquettes/pop-up dérivés d'un point unique en coordonnées viewBox ; `.scene` suit le ratio du viewBox `1000/620` — supprime le décalage lié au letterboxing du SVG sur les breakpoints mobiles ; pop-up ancré près du point cliqué au lieu d'un coin fixe). **R3** fait (Craving : `.overlayZone` bornée par `inset`/`max-height`/`overflow` tant qu'≤1 outil actif ; dès 2+ outils actifs les cartes basculent sous le graphe en grille responsive — plus aucun débordement quand les 4 outils sont actifs). **R4** fait (Nicotine : le balayage temporel démarre automatiquement à l'ouverture et boucle en continu façon oscilloscope — supprime la dépendance au bouton « Lecture » ; un clic sur un geste insère la prise au temps courant et dessine sa cinétique immédiatement ; Pause/Vitesse relégués en contrôles secondaires, Réinitialiser en tertiaire/alerte ; ajout d'un dégradé sous la courbe déjà parcourue et d'un chip « État actuel » avec icône, mieux intégré ; `nicotineCurve.ts` non touché, partagé avec R5). **R5** fait (Soulagement : bascule d'onglets Non-fumeur/Fumeur supprimée, remplacée par un bac à sable temps réel calqué sur R4 — le balayage tourne seul, « Fumer une cigarette » insère la prise au temps courant et le stress chute exactement au pic de nicotine puis remonte ; bouton unique « Comparer au non-fumeur » superpose une ligne de repère toujours sous le creux le plus bas du fumeur, invariant garanti par construction (`STRESS_BASAL_FUMEUR` > `STRESS_BASAL_NON_FUMEUR`) et désormais couvert par 2 tests Vitest dédiés ; `nicotineCurve.ts` non modifié en formule). **R6** fait (Addiction : le panneau « De quoi parle-t-on ? » n'est plus superposé en absolu sur le diagramme de Venn — il s'affiche désormais en flux, sous le diagramme, empilé avec le panneau « Outils & stratégies » existant ; contenu des outils inchangé, la source §Module 1 ne fournissant pas d'items supplémentaires — enrichissement non fait, signalé dans `VALIDATION.md` plutôt qu'inventé). **R7** fait (Craving : courbe « vague » enrichie d'une aire dégradée sous la courbe, trait épaissi/arrondi, marqueur « maintenant » avec halo, repère pointillé du pic, axe temps discret — logique de la vague inchangée). **R8** fait (Substituts : les 3 cases à cocher deviennent des cartes cliquables avec icône et cible ≥ 44 px, colorées par token sémantique à l'état actif ; le bouton grisé « surdosage → revenir en arrière » remplacé par une bannière d'alerte rouge explicite ; commandes de dose regroupées en blocs Jour/Nuit ; libellé des patchs sur deux lignes pour la lisibilité — mécanique inchangée, aucun dosage chiffré). **R9** fait (nouveau module 7 « Explorer ma motivation » : `motivation/MotivationModule.tsx` + `.module.css`, branché via `types.ts`/`registry.ts` uniquement — `App.tsx` inchangé car déjà générique ; outil A = 2 curseurs 0–10 importance/confiance avec relance non culpabilisante ; outil C = tableau blanc de 6 cartes « Mes raisons » déplaçables par pointer events natifs + flèches clavier, éditables, création de nouvelles cartes ; aucune dépendance ajoutée, zéro persistance ; contenu de départ = proposition non sourcée à valider par Thibault). `npm`/`node` ne sont pas sur le `PATH` de cet environnement, mais `node.exe` existe sous `/c/Program Files/nodejs/` : `tsc -b`, `vite build` et `vitest run` ont pu être exécutés directement via `node_modules/.bin` pour R4-R9 (tous verts) — les exécutants suivants devraient tenter cette voie avant de déclarer l'auto-validation indisponible. Reste globalement : passe de validation visuelle par Thibault (dont arbitrage des amplitudes de stress R5 et du contenu de départ R9) + contenu non bloquant (sources, bonnes pratiques substituts, outils Module 1). `PLAN_corrections-v2.md` (R1-R9) est désormais entièrement exécuté. **Phase 4** (`plans/corrections-v3/`, captures Thibault 2026-07-01) : **V1** fait (Addiction : agrandissement du diagramme de Venn pour lisibilité à ~1 m — `.vennWrap` max-width 640→800px, `.venn` max-height 46vh→65vh, `.circleLabel` font-size 18→24px, `.circleKeywords` font-size 13→16px ; `tsc -b` + `vite build` OK ; interaction (hitAreas) conservée). **V2** fait (Addiction : le panneau `.explorePanel`/`.bubbleRow` en flux est remplacé par des bulles `.radialBubble` positionnées en arc (menu radial) autour du cercle sélectionné, en % du `.vennWrap` — secteur d'arc par pilier choisi pour s'éloigner des deux autres cercles (physique gauche, psychologique droite, comportementale bas), rayon `R + 60` ; titre « De quoi parle-t-on ? » conservé en flux au-dessus du diagramme ; panneau « Outils & stratégies » inchangé ; `tsc -b` + `vite build` OK). **V3** fait (Nicotine : suppression du balayage temporel automatique, du curseur bleu et de la courbe grisée fantôme — chaque clic sur un geste dépose désormais une prise sur une frise temporelle statique via `nextEventTime` (`EVENT_STEP` réduit 0.12→0.05 pour que les prises rapprochées se chevauchent) ; la courbe cumulée complète s'affiche d'un coup, segmentée et colorée par zone ; le chip « État actuel » reflète la zone du point le plus haut atteint ; `nicotineCurve.ts` retuné : `cigaretteKernel` abaissé à une amplitude ~0.50 avec décroissance plus lente (tau 0.03→0.07) pour qu'1 cigarette seule culmine en zone confort tandis que 3 cigarettes rapprochées ou patch+2 cigarettes cumulent au-delà du seuil haut ; hook `useReducedMotion` retiré, devenu inutile en l'absence d'animation continue ; 3 nouveaux tests d'invariants Vitest ; `tsc -b` + `vite build` + `vitest run` 20 tests verts). **V4** fait (Soulagement : même alignement — suppression du balayage continu/curseur/`useReducedMotion` ; « Fumer une cigarette » dépose désormais une prise sur la frise statique via `nextEventTime` ; les courbes nicotine (repère) et stress (plein) se tracent d'un coup sur toute la frise complète, `troughIndex`/annotation recalés en conséquence ; `nicotineCurve.ts` non touché ; `tsc -b` + `vite build` + `vitest run` 20 tests verts, aucune régression). **V5** fait (Craving : les cartes des 4 D ne basculent plus sous le graphe — un seul `.overlayZone` toujours ancré au-dessus de la zone du pic (centré sur `PEAK_X`, ~30 % de la largeur), disposition flexbox compacte (2×2 max, cartes réduites) ; `courbeAttenuee` binaire lié à « Distraire » remplacé par une opacité graduée du tracé (`picOpacity`, palette 1 / 0,55 / 0,35 / 0,2 / 0,1 selon `activeTools.size`) appliquée en style inline sur le `<path>` ; `.overlayZoneBelow` et la bascule `trop` supprimées ; `tsc -b` + `vite build` OK). **V6** fait (Nicotine-toxique : le `detail` du hotspot `melange` reformulé pour nommer la conséquence — « En brûlant, le tabac libère un cocktail d'environ 7 000 substances chimiques. C'est ce mélange issu de la combustion — et non la nicotine — qui rend malade. », cohérent avec §Module 4 de `docs/contenu-modules.md` ; `eyebrow` inchangé ; texte proposé par Claude à valider par Thibault ; `tsc -b` + `vite build` OK). **V7** fait (Motivation : le module empile désormais « Où en êtes-vous ? » et « Mes raisons » derrière une barre d'onglets (`role="tablist"`) au lieu de les afficher l'une sous l'autre ; les deux `<section>` restent montées, masquées via `hidden` selon l'onglet actif — aucun état perdu (curseurs/cartes) en changeant d'onglet ; navigation clavier flèches gauche/droite ; `tsc -b` + `vite build` OK). **V8** fait (Motivation : l'onglet « Mes raisons » ajoute un champ `placed: boolean` par carte — les 6 cartes seed et toute nouvelle carte démarrent dans une **réserve** (bac en flux, hors tableau) ; boutons « Placer »/« Retirer » (≥ 44 px) déplacent une carte entre réserve et tableau ; le drag au pointeur (poignée, inchangé) reste réservé au repositionnement **à l'intérieur** du tableau désormais agrandi (`min-height: min(70vh, 640px)`) — choix documenté dans le plan §Si bloqué : un drag inter-conteneurs natif casserait la capture de pointeur au remontage DOM ; relâcher un drag hors du cadre du tableau renvoie aussi la carte à la réserve, en plus du bouton « Retirer » ; `tsc -b` + `vite build` OK). **Phase 5** (`plans/corrections-v4/`, audit Playwright 2026-07-01, 12 correctifs exécutés le 2026-07-02 en 3 vagues parallèles) : **A1** fait (Substituts : contenu médical validé pour 5 formes — patch, gomme, pastille, comprimé sublingual, spray — avec bonnes pratiques + erreurs fréquentes ; inhaleur/vapoteuse en repli « en rédaction » ; bouton « + ¼ (tous les 3 jours) » + aide de condition + bannière surdosage détaillée ; `docs/contenu-modules.md` §3-A mis à jour). **A2** fait (Motivation : masquage onglets — `.section { display: flex }` écrasait la règle UA `[hidden] { display: none }` ; correctif une ligne CSS : `.section[hidden] { display: none }`). **A3** fait (Accueil : regroupement des 7 modules en 3 familles — « Comprendre » / « Agir » / « Se motiver » — portées par `ModuleDef.famille` dans `types.ts`/`registry.ts`, rendu en sections avec intertitres discrets dans `Home.tsx` ; résumés de cartes légèrement plus grands). **A4** fait (Coquille : libellé « Sources » visible en toutes lettres à côté de l'icône ; `:focus-visible` net sur boutons Sources + Retour — `outline: 3px solid, offset 2px`). **A5** fait (Addiction : « Touchez » → « Cliquez » ; titre composante physique sans « nicotinique » ; halo SVG blanc derrière les libellés des cercles pour réduire le chevauchement ; bloc légende couleur → symptômes → stratégies sous la scène Venn). **A6** fait (Nicotine : consigne d'amorce au-dessus des boutons ; chip « État actuel » renommé « Pic atteint » ; pictogrammes frise agrandis (18→22 px) ; liste des prises jouées sous le graphe). **A7** fait (Nicotine-toxique : atténuation de l'autre groupe réduite (opacity 0.2→0.38 / 0.26→0.42) ; double encodage non chromatique — étiquettes de scène avec icônes ⚠/🧠 + libellés de rôle en gras dans la légende « Ce qui rend malade » / « Ce qui crée la dépendance »). **A8** fait (Soulagement : consigne de lecture en 2 temps encadrée (bordure vigilance) ; annotation délai chute→remontée sur le SVG (`delayAnnotation`, segment + texte italique) ; « stress » → « tension liée au manque » partout dans le module). **A9** fait (Craving : 3 états distincts — idle/en cours/passée — pour la carte « Différer » ; `differActifPendantCourse` memo ; « C'est passé. » n'apparaît que si la vague s'est réellement terminée en cours de différé). **A10** fait (Motivation : `.carte` min-width 220 px/max-width 280 px ; sliders piste 8 px + pouce 28 px + bornes 0/10 sous chaque axe ; `.whiteboard` min-height réduite à min(40vh, 360px)). **A11** fait (Nicotine : drag natif HTML5 (`draggable` + handlers DragOver/Drop sur le SVG via `svgRef`/`getBoundingClientRect`) — l'utilisateur choisit le moment en déposant la prise sur la frise ; `t` calculé depuis la position X relative ; ligne fantôme `<line>` SVG en cours de drag ; retrait d'une prise par clic croix sur le pictogramme ou bouton X dans la liste texte ; fallback clic/clavier (`nextEventTime`) conservé). **A12** fait (Motivation : drag HTML5 réserve → tableau (`html5DragId` ref au lieu de dataTransfer) ; drop sur `.whiteboard` place la carte au point de dépôt (getBoundingClientRect + MARGIN clamp) ; drop sur `.reserve` ou hors tableau renvoie à la réserve ; boutons « Placer »/« Retirer » supprimés ; Entrée/Espace = placer depuis réserve, Suppr/Backspace = retirer depuis tableau ; drag pointeur intra-tableau + nudge flèches conservés). Fix apostrophes U+0027 dans strings single-quoted (CravingModule + SubstitutsModule) rejetées par Rolldown/oxc mais non détectées par tsc. `tsc --noEmit` + `vite build` verts (3 commits). **Phase 6** (`plans/refonte-ui/`, refonte visuelle sur la maquette Claude Design, cf. `docs/DESIGN_REFONTE.md`) : **S1** fait (socle, vague 1 bloquante — 3 tâches, 3 commits) : T1 auto-hébergement de Source Serif 4 + Work Sans (4 woff2 copiés dans `public/fonts/`, `@font-face` latin/latin-ext dans `global.css`, préchargement Work Sans dans `index.html`, plus de Google Fonts) ; T2 `tokens.css` — palette hex remplacée par les tokens `oklch` de la refonte (fonds crème, texte brun, sémantique confort/toxique/vigilance/nav, teintes soft en alpha, rayons `sm/·/lg/pill`, ombres `shadow-1/2/3`), noms sémantiques et alias historiques (`--color-accent`, `--color-warn`) conservés, `--font-size-lead`/`--font-size-small` gardés tels quels (non couverts par la refonte, toujours consommés par plusieurs modules) ; T3 base typographique (`body` en Work Sans, `h1/h2/h3` en Source Serif 4, tailles 34/26/20px) + primitives globales dans `global.css` (`.eyebrow`, `.btn`/`.btn--primary`/`.btn--ghost`/`.btn--tertiary`, `.chip` + modificateurs sémantiques, `.card`/`.panel`, `.callout`, `.alert`, classes `.zone-fill--*`/`.zone-label--*` pour les bandes de graphe SVG). `tsc -b` + `vite build` + `vitest run` (20 tests) verts après chaque tâche. Reste : validation visuelle humaine (police/couleurs/primitives, cf. `VALIDATION.md`), puis vague 2 (S2-S9, restyle des modules + réécriture Motivation) et S10 (nouvelle logique des courbes) qui consomment ce socle.

## Ce qui fonctionne

- **Scaffolding** Vite + React + TS ; navigation carte ↔ module (pile d'historique éphémère) ; accueil grille familles (Comprendre / Agir / Se motiver, A3) responsive.
- **Coquille partagée** : `ModuleShell`, `Sources` (libellé « Sources » visible + focus clavier A4), `ModuleCard`. Registre + `famille` par module (A3).
- **Les 6 modules du thème tabac + 1 module transverse** implémentés (plus aucun stub) :
  1. **Addiction** (`addiction/`) — diagramme de Venn à géométrie fixe, sélection par contour/opacité/échelle légère + panneau latéral unique signes/pistes et renvois (B1) ; message central toujours visible.
  2. **Nicotine** (`nicotine/`) — frise temporelle statique (V3) ; consigne d'amorce + chip « Pic atteint » + pictogrammes agrandis + liste prises (A6) ; drag-and-drop des prises sur la frise (position = temps), ligne fantôme, retrait par clic, fallback clic/clavier (A11).
  3. **Substituts & titration** (`substituts/`) — 7 formes ; contenu médical validé pour 5 formes (bonnes pratiques + erreurs), repli « en rédaction » pour inhaleur/vapoteuse (A1) ; titration : bouton « + ¼ (tous les 3 jours) » + aide de condition + bannière surdosage détaillée ; aucun dosage chiffré.
  4. **Nicotine ≠ toxique** (`nicotine-toxique/`) — scène SVG, hotspots, filtres ; atténuation légère de l'autre groupe + double encodage non chromatique (icônes ⚠/🧠 + libellés de rôle) (A7) ; contenu médical à valider.
  5. **Piège du soulagement** (`soulagement/`) — frise temporelle statique (V4) ; consigne 2 temps encadrée + annotation délai chute/remontée + « tension liée au manque » partout (A8) ; récit chiffré illustratif à valider.
  6. **Craving / 4D** (`craving/`) — vague de l'envie (~30 s), 4D en bascules (V5) ; « C'est passé » réservé à la fin réelle de la vague (3 états : idle/en cours/passée) (A9).
  7. **Explorer ma motivation** (`motivation/`) — 2 onglets (V7) ; réserve de cartes (V8) ; masquage onglets corrigé (A2) ; drag HTML5 réserve↔tableau + Entrée/Suppr (A12, remplace boutons Placer/Retirer) ; cartes 220–280 px + sliders épaissis + bornes 0/10 + tableau moins haut (A10).
- **Logique pure** : `src/lib/nicotineCurve.ts` (`sampleCurve`, `classifyZone`, `sampleStress`, `toSvgPath`), couverte par Vitest (`nicotineCurve.test.ts`, 20 tests verts — dont l'invariant R5 « creux fumeur > basal non-fumeur », la monotonie creux/rebond, et les 3 invariants de cumul V3).
- **Styles / accessibilité** : `tokens.css` (palette sémantique confort/toxique/vigilance/nav), `global.css` (cibles ≥ 44 px, `.activeDoubled`, `prefers-reduced-motion`).

## Ce qui n'est pas fait / à compléter (non bloquant)

- **Validation visuelle/UX humaine** des modules et de C8-C9 : pas encore faite (cf. `VALIDATION.md`).
- **C10 (partiel)** : sources exactes par module dans `registry.ts` — toujours à fournir par Thibault. Contenu bonnes pratiques/erreurs substituts : fait pour 5 formes (A1) ; reste inhaleur + vapoteuse.
- Contenus **en attente de validation Thibault** : récit chiffré du stress (C4), regroupements toxiques (C7), enrichissement des « outils & stratégies » du Module 1 au-delà des 2 items existants par pilier (R6), contenu de départ du Module 7 (libellés d'échelles + liste seed « Mes raisons », R9) — cf. `VALIDATION.md`.

## Dette technique / complexité

- Faible : 6 modules indépendants, logique pure isolée. Aucune zone difficile identifiée.
